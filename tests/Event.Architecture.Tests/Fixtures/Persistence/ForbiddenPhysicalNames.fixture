// ABOUTME: Synthetic architecture fixture representing forbidden physical table, column, and index literals.
// ABOUTME: Exists only to prove naming-convention and semantic-name scanners detect new violations.

internal static class ForbiddenPhysicalNamesFixture
{
    public static void Configure(EntityTypeBuilder<ExampleEntity> builder)
    {
        builder.ToTable("forbidden_table");
        builder.Property(entity => entity.Value).HasColumnName("forbidden_column");
        builder.HasIndex(entity => entity.Value).HasDatabaseName("forbidden_index");
    }
}
