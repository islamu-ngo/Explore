@using Explore.Blazor.Client.Clients
@using Explore.Blazor.Client.Validators
@using FluentValidation
@using MudBlazor

<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.Add" Class="mr-3 mb-n1" />
            Create New Location
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudForm @ref="form" @bind-IsValid="success" Model="locationDto">
            <AppTextField T="string" @bind-Value="locationDto.FullName" Label="Name" For="@(() => locationDto.FullName)" Required="true" RequiredError="Name is required." />
            <AppTextField T="string" @bind-Value="locationDto.Address" Label="Address" For="@(() => locationDto.Address)" />
            <AppTextField T="string" @bind-Value="locationDto.City" Label="City" For="@(() => locationDto.City)" />
            <AppTextField T="string" @bind-Value="locationDto.Postcode" Label="Postcode" For="@(() => locationDto.Postcode)" />
            <AppTextField T="string" @bind-Value="locationDto.Country" Label="Country" For="@(() => locationDto.Country)" />
            <MudNumericField @bind-Value="locationDto.Latitude" Label="Latitude" For="@(() => locationDto.Latitude)" />
            <MudNumericField @bind-Value="locationDto.Longitude" Label="Longitude" For="@(() => locationDto.Longitude)" />
            <AppTextField T="string" @bind-Value="locationDto.Timezone" Label="Timezone" For="@(() => locationDto.Timezone)" />
        </MudForm>
    </DialogContent>
    <DialogActions>
        <AppButton Variant="Variant.Text" Color="Color.Default" OnClick="Cancel">Cancel</AppButton>
        <AppButton OnClick="Submit">Create</AppButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter]
    private IDialogReference? DialogReference { get; set; }

    private CreateLocationDtoValidator validator = new();
    private MudForm form;
    private bool success;

    private CreateLocationDto locationDto = new();

    private async Task Submit()
    {
        await form.Validate();
        if (form.IsValid)
        {
            DialogReference?.Dismiss(DialogResult.Ok(locationDto));
        }
    }

    void Cancel() => DialogReference?.Dismiss(DialogResult.Cancel());
}
