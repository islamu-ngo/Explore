@using MudBlazor
@using Explore.Blazor.Client.Clients

<MudDialog>
    <DialogContent>
        <MudStack Spacing="3">
            <MudText>How was your experience with @OrganizationName?</MudText>
            <MudRating @bind-SelectedValue="Rating" MaxValue="5" Size="Size.Large" />
            <AppTextField T="string" @bind-Value="Comment" Label="Comment" Lines="4" />
        </MudStack>
    </DialogContent>
    <DialogActions>
        <AppButton OnClick="Cancel">Cancel</AppButton>
        <AppButton OnClick="Submit">Submit Review</AppButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] IMudDialogInstance MudDialog { get; set; }
    [Parameter] public string OrganizationName { get; set; } = string.Empty;

    public int Rating { get; set; } = 5;
    public string Comment { get; set; } = string.Empty;

    private void Cancel() => MudDialog.Cancel();

    private void Submit()
    {
        if (Rating == 0)
        {
            // Show error or require rating
            return;
        }
        MudDialog.Close(DialogResult.Ok(new { Rating, Comment }));
    }
}
