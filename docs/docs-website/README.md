ABOUTME: Deferral notice for the parked public documentation website folder.
ABOUTME: Prevents this folder from becoming a second source of truth before repository docs stabilize.

# Public Docs Website Deferral

This folder is parked for the current documentation implementation phase.

Repository Markdown under `docs/`, plus GitHub templates under `.github/` and handoff material under `dev/`, is the current source of truth. Do not add a docs-site generator, publishing workflow, or expanded public-site content here until the repository docs are stable and the project explicitly starts the public docs website phase.

Future work may mirror selected canonical pages from the repository into a hosted documentation site. Until then, treat files in this folder as legacy or placeholder material and prefer the canonical repository docs linked from `../index.md`.
