# ABOUTME: Public change fragment generated with its collision-resistant commit footer.
# ABOUTME: Records the breaking relational ATProto state and browser-proof cutover.
Change-Id: CHG-01M1TWE5JDEKX48GMJEJ3NWZ5C
Title: "Use database-backed ATProto login state"
Type: feat
Scope: access
Summary: "Complete Redis-free ATProto login and tenant handoff through the primary database with fixed-expiry browser proof and single-use consumption."
Group: database-backed-atproto-auth
Supersedes: []
Impacts:
  Breaking:
    Reference: docs/internal/adr/ADR-014-atproto-session-trust-bridge.md
    Disposition: documented
    Detail: Removes UseSingleNodeMemoryStore and HandoffLifetimeSeconds, requires HTTPS for local ATProto login, and requires in-flight legacy logins to restart.
  Security:
    Reference: docs/internal/SECURITY-MODEL.md
    Disposition: documented
    Detail: A fixed-expiry host-only proof cookie binds each flow independently; tenant, origin, browser proof and candidate identity are checked before consumption and again before cookie issuance.
  Migration:
    Reference: docs/internal/adr/ADR-014-atproto-session-trust-bridge.md
    Disposition: not-applicable
    Detail: Uses the transient relational schema introduced by the storage phase; no additional schema migration is introduced.
  Configuration:
    Reference: docs/internal/CONFIGURATION.md
    Disposition: documented
    Detail: Transient state always uses the private relational bridge; handoffs have a fixed two-minute limit and existing native Data Protection keys must persist across restarts and be shared by replicas.
  OpenAPI:
    Reference: docs/internal/adr/ADR-014-atproto-session-trust-bridge.md
    Disposition: not-applicable
    Detail: Private bridge contracts remain absent from public OpenAPI, generated browser clients and HAL discovery.
  Operator:
    Reference: docs/public/documentation/readme/federation-and-open-protocols/at-protocol-and-bluesky-jetstream.md
    Disposition: documented
    Detail: Redis-free Split and Standalone login require the primary database and preserved signing/protection keys; ambiguous consumption fails closed without retries or a local-memory downgrade.
