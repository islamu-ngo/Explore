{
  "_metadata": {
    "about": [
      "ABOUTME: Defines the closed value-safe machine response contract for event-setup commands.",
      "ABOUTME: Generated by the package-free SetupCliCommandSchemaGenerator; edit compiled CLI metadata."
    ],
    "generatedBy": "ISLAMU.Event.SetupAssistant.Cli.CommandSchemaGenerator",
    "commandOptions": [
      "--help",
      "--machine",
      "--text",
      "--dry-run",
      "--input",
      "--baseline",
      "--output",
      "--key",
      "--topology",
      "--capability",
      "--provider"
    ],
    "repeatableOptions": [
      "--capability",
      "--provider"
    ]
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.islamu.org/event/setup-command/v1/schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "invocation",
    "status",
    "exitCategory",
    "exitCode",
    "dryRun",
    "diagnostics",
    "artifacts",
    "coverage",
    "readiness"
  ],
  "properties": {
    "schemaVersion": {
      "const": "event-setup-command/v1"
    },
    "invocation": {
      "$ref": "#/$defs/invocation"
    },
    "status": {
      "enum": [
        "success",
        "validation",
        "incomplete",
        "blocked",
        "usage",
        "data",
        "internal",
        "io"
      ]
    },
    "exitCategory": {
      "enum": [
        "success",
        "validation",
        "incomplete",
        "blocked",
        "usage",
        "data",
        "internal",
        "io"
      ]
    },
    "exitCode": {
      "enum": [
        0,
        2,
        3,
        4,
        64,
        65,
        70,
        74
      ]
    },
    "dryRun": {
      "type": "boolean"
    },
    "diagnostics": {
      "type": "array",
      "maxItems": 128,
      "items": {
        "$ref": "#/$defs/diagnostic"
      }
    },
    "artifacts": {
      "type": "array",
      "maxItems": 32,
      "items": {
        "$ref": "#/$defs/artifact"
      }
    },
    "coverage": {
      "$ref": "#/$defs/coverage"
    },
    "readiness": {
      "$ref": "#/$defs/readiness"
    }
  },
  "$defs": {
    "invocation": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "commandFamily",
        "operation",
        "mode"
      ],
      "properties": {
        "commandFamily": {
          "enum": [
            "catalogue",
            "manifest",
            "tenant-package",
            "env",
            "legal",
            "doctor",
            "tui"
          ]
        },
        "operation": {
          "enum": [
            "list",
            "show",
            "describe",
            "create",
            "open",
            "validate",
            "format",
            "diff",
            "coverage",
            "export",
            "render",
            "preview",
            "doctor",
            "tui"
          ]
        },
        "mode": {
          "enum": [
            "machine"
          ]
        }
      }
    },
    "diagnostic": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "code",
        "path",
        "severity"
      ],
      "properties": {
        "code": {
          "type": "string",
          "minLength": 1,
          "maxLength": 96,
          "pattern": "^[a-z][a-z0-9]*(?:-[a-z0-9]\u002B)*$"
        },
        "path": {
          "type": "string",
          "minLength": 1,
          "maxLength": 256,
          "pattern": "^\\$(?:\\.[a-z][A-Za-z0-9-]*)*$"
        },
        "severity": {
          "enum": [
            "info",
            "warning",
            "error"
          ]
        }
      }
    },
    "artifact": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "mediaType",
        "digest",
        "sensitivity",
        "coverage",
        "readiness",
        "pathIntent",
        "writeStatus"
      ],
      "properties": {
        "kind": {
          "enum": [
            "catalogue",
            "configuration-manifest",
            "tenant-configuration-package",
            "dotenv-template",
            "legal-draft",
            "doctor-report"
          ]
        },
        "mediaType": {
          "type": "string",
          "minLength": 1,
          "maxLength": 128,
          "pattern": "^[a-z0-9][a-z0-9.\u002B-]*/[a-z0-9][a-z0-9.\u002B-]*(?:;v=[a-z0-9.-]\u002B)?$"
        },
        "digest": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "sensitivity": {
          "enum": [
            "public",
            "sensitive"
          ]
        },
        "coverage": {
          "$ref": "#/$defs/coverage"
        },
        "readiness": {
          "$ref": "#/$defs/readiness"
        },
        "pathIntent": {
          "enum": [
            "none",
            "input",
            "output",
            "stdout"
          ]
        },
        "writeStatus": {
          "enum": [
            "none",
            "planned",
            "written"
          ]
        }
      }
    },
    "coverage": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "coveredKeys",
        "missingKeys"
      ],
      "properties": {
        "coveredKeys": {
          "$ref": "#/$defs/keyList"
        },
        "missingKeys": {
          "$ref": "#/$defs/keyList"
        }
      }
    },
    "readiness": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "state",
        "missingKeys",
        "blockedKeys"
      ],
      "properties": {
        "state": {
          "enum": [
            "ready",
            "incomplete",
            "blocked"
          ]
        },
        "missingKeys": {
          "$ref": "#/$defs/keyList"
        },
        "blockedKeys": {
          "$ref": "#/$defs/keyList"
        }
      }
    },
    "keyList": {
      "type": "array",
      "maxItems": 256,
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 128,
        "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]\u002B)*$"
      }
    }
  }
}
