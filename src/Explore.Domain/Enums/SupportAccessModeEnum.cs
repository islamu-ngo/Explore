// ABOUTME: Lookup enum for support-access permission modes.
// ABOUTME: Read-only is the default safe mode; write mode is separately governed.

namespace Explore.Domain.Enums;

public enum SupportAccessModeEnum
{
    ReadOnly = 1,
    Write = 2
}
