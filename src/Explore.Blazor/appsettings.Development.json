// ABOUTME: Configures local BFF diagnostics without selecting an alternate authentication storage backend.
// ABOUTME: Keeps development logging explicit while ATProto transient state uses the primary database.
{
  "DetailedErrors": true,
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
