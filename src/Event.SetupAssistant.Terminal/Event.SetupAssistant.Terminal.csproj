<!-- ABOUTME: Defines the sole human terminal executable for the Setup Assistant. -->
<!-- ABOUTME: References shared presentation/Core state and the audited internal Terminal.Gui package only. -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <SetupTargetEnabled>true</SetupTargetEnabled>
    <SetupTargetRole>Terminal</SetupTargetRole>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Event.SetupAssistant\Event.SetupAssistant.csproj" />
    <ProjectReference Include="..\Event.Setup.Core\Event.Setup.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="ISLAMU.Terminal.Gui" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
  </ItemGroup>
</Project>
