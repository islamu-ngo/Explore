// ABOUTME: Composes the sole human Setup Assistant terminal target at the executable edge.
// ABOUTME: Rejects arguments and redirected process surfaces before Terminal.Gui or secret state is created.

using ISLAMU.Event.SetupAssistant.Terminal;

return SetupTerminalProgram.Run(args);
