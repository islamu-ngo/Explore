<!-- ABOUTME: Defines package-free wire contracts shared across server and isolated client runtimes. -->
<!-- ABOUTME: Hosts versioned codecs and value types without domain, framework, or transport dependencies. -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>ISLAMU.Wire.Contracts</RootNamespace>
  </PropertyGroup>
</Project>
