<!-- ABOUTME: Defines the networked outer Setup live adapter over the generated API client. -->
<!-- ABOUTME: Keeps HTTP and ephemeral capability custody outside the pure shared presentation assembly. -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Event.Setup.Core\Event.Setup.Core.csproj" />
    <ProjectReference Include="..\..\Explore.Blazor.Client\Explore.Blazor.Client.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Event.SetupAssistant.Tests" />
  </ItemGroup>
</Project>
