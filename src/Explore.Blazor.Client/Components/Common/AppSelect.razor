@* ABOUTME: Shared wrapper around MudSelect<T> with exact-width left-aligned popover placement defaults. *@
@* ABOUTME: Enforces AnchorOrigin.BottomLeft, TransformOrigin.TopLeft, and RelativeWidth.Width. *@
@typeparam T

<div class="app-select">
    <MudSelect T="T"
               Value="@Value"
               ValueChanged="@ValueChanged"
               SelectedValues="@SelectedValues"
               SelectedValuesChanged="@SelectedValuesChanged"
               MultiSelection="@MultiSelection"
               MultiSelectionTextFunc="@MultiSelectionTextFunc"
               Label="@Label"
               Variant="@Variant"
               Margin="@Margin"
               Dense="@Dense"
               Disabled="@Disabled"
               Required="@Required"
               Clearable="@Clearable"
               AnchorOrigin="@AnchorOrigin"
               TransformOrigin="@TransformOrigin"
               DropdownWidth="@DropdownWidth"
               Class="@Class"
               Style="@Style"
               @attributes="AdditionalAttributes">
        @ChildContent
    </MudSelect>
</div>

@code {
    [Parameter] public T? Value { get; set; }
    [Parameter] public EventCallback<T> ValueChanged { get; set; }

    [Parameter] public IReadOnlyCollection<T>? SelectedValues { get; set; }
    [Parameter] public EventCallback<IReadOnlyCollection<T>> SelectedValuesChanged { get; set; }

    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public bool MultiSelection { get; set; }
    [Parameter] public Func<List<string>, string>? MultiSelectionTextFunc { get; set; }

    [Parameter] public string? Label { get; set; }
    [Parameter] public Variant Variant { get; set; } = Variant.Outlined;
    [Parameter] public Margin Margin { get; set; } = Margin.None;
    [Parameter] public bool Dense { get; set; }
    [Parameter] public bool Disabled { get; set; }
    [Parameter] public bool Required { get; set; }
    [Parameter] public bool Clearable { get; set; }

    [Parameter] public Origin AnchorOrigin { get; set; } = Origin.BottomLeft;
    [Parameter] public Origin TransformOrigin { get; set; } = Origin.TopLeft;
    [Parameter] public DropdownWidth DropdownWidth { get; set; } = DropdownWidth.Relative;

    [Parameter] public string? Class { get; set; }
    [Parameter] public string? Style { get; set; }

    [Parameter(CaptureUnmatchedValues = true)]
    public IDictionary<string, object>? AdditionalAttributes { get; set; }
}
