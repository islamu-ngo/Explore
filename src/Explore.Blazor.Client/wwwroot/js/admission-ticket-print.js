// ABOUTME: Opens the native print dialog for an already-authorized admission ticket document.
// ABOUTME: Performs no network, storage, logging, or DOM mutation.

export function printTicket() {
    window.print();
}
