{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Principal attributes built by CerbosPrincipalBuilder. Validated on every Cerbos check request.",
  "type": "object",
  "properties": {
    "userId": {
      "type": "string",
      "description": "Internal domain user identifier for human principals. Allows policies to compare platform user ownership even when an external subject is used as the principal id."
    },
    "is_machine": {
      "type": "boolean",
      "description": "Whether the principal represents an API-key machine caller instead of an interactive human user."
    },
    "isInstanceAdmin": {
      "type": "boolean",
      "description": "Whether the principal is an instance-level administrator."
    },
    "tenantMemberships": {
      "type": "object",
      "description": "Map of tenantId to role string (e.g. 'admin') for tenant membership checks.",
      "additionalProperties": { "type": "string" }
    },
    "orgMemberships": {
      "type": "object",
      "description": "Map of organizationId to role string (e.g. 'admin') for organization membership checks.",
      "additionalProperties": { "type": "string" }
    },
    "groupMemberships": {
      "type": "object",
      "description": "Map of groupId to role string for group administration checks.",
      "additionalProperties": { "type": "string" }
    },
    "eventAssignments": {
      "type": "object",
      "description": "Map of eventId to effective assignment data. Only active, non-expired assignments for the request tenant are hydrated by CerbosPrincipalBuilder.EnrichWithEventAssignmentsAsync.",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "tenantId": {
            "type": "string",
            "description": "Tenant identifier used to bind an event assignment to the resource tenant."
          },
          "roles": {
            "type": "array",
            "items": { "type": "string" },
            "uniqueItems": true,
            "description": "Role master codes (e.g. 'event.owner', 'event.manager')."
          },
          "permissions": {
            "type": "array",
            "items": { "type": "string" },
            "uniqueItems": true,
            "description": "Permission codes (e.g. 'event:update', 'event:manage-team')."
          }
        },
        "required": ["tenantId", "roles", "permissions"],
        "additionalProperties": false
      }
    },
    "nowUtc": {
      "type": "string",
      "format": "date-time",
      "description": "Current UTC timestamp in ISO 8601 format for lifecycle time comparisons."
    }
  },
  "required": ["isInstanceAdmin", "tenantMemberships", "orgMemberships"]
}
