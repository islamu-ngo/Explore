{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Resource attributes for event_day resources. Populated by ResourceDescriptors.EventDay.",
  "type": "object",
  "properties": {
    "tenantId": {
      "type": "string",
      "description": "Owning tenant identifier. Used by islamuevent_tenant_admin derived role."
    },
    "organizationId": {
      "type": "string",
      "description": "Owning organization identifier. Used by islamuevent_org_admin derived role."
    }
  },
  "required": ["tenantId"]
}
