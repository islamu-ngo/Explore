{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Resource attributes for event resources. Populated by ResourceDescriptors.Event.",
  "type": "object",
  "properties": {
    "tenantId": {
      "type": "string",
      "description": "Owning tenant identifier. Used by islamuevent_tenant_admin derived role."
    },
    "eventId": {
      "type": "string",
      "description": "Event identifier. Used by event-role derived roles to look up principal's event assignments."
    },
    "actorId": {
      "type": "string",
      "description": "Actor (creator/owner) identifier."
    },
    "userId": {
      "type": "string",
      "description": "Owning user identifier for events published under a user actor."
    },
    "organizationId": {
      "type": "string",
      "description": "Owning organization identifier. Used by islamuevent_org_admin derived role."
    },
    "authorizationPhase": {
      "type": "string",
      "description": "Authorization phase marker. Event create checks use pre_create before an actor or event ID exists."
    },
    "groupId": {
      "type": "string",
      "description": "Owning group identifier for pre-create event requests. Publisher permissions are enforced by the handler."
    },
    "provenanceType": {
      "type": "string",
      "description": "Normalized event provenance code or stable lookup identifier."
    },
    "organizerActorId": {
      "type": "string",
      "description": "Verified organizer authority actor when one has been established."
    },
    "organizerUserId": {
      "type": "string",
      "description": "Verified organizer's user identifier, supplied only by server-side event loading."
    },
    "organizerOrganizationId": {
      "type": "string",
      "description": "Verified organizer's organization identifier, supplied only by server-side event loading."
    },
    "organizerGroupId": {
      "type": "string",
      "description": "Verified organizer's group identifier, supplied only by server-side event loading."
    },
    "submittedByUserId": {
      "type": "string",
      "description": "User that originally submitted a community-reported event when retained."
    },
    "organizerVerificationStatus": {
      "type": "string",
      "description": "Optional normalized organizer verification state when an authoritative source exists."
    }
  },
  "required": ["tenantId"]
}
