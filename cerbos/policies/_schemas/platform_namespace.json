{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Resource attributes for platform_namespace resources. Platform-reserved property namespace with explicit deny for non-instance-admin writes.",
  "type": "object",
  "properties": {
    "tenantId": {
      "type": "string",
      "description": "Owning tenant identifier. Used by tenant_admin derived role."
    }
  },
  "required": ["tenantId"]
}
