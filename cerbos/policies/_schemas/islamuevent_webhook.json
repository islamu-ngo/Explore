{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Resource attributes for outgoing webhook consumers, endpoints, messages, delivery attempts, and provider portal sessions.",
  "type": "object",
  "properties": {
    "tenantId": {
      "type": "string",
      "description": "Owning tenant identifier for tenant-bound owners or source-tenant delivery operations."
    },
    "instanceId": {
      "type": "string",
      "description": "Immutable owning instance identifier for instance-owned webhook configuration."
    },
    "ownerKindId": {
      "type": "integer",
      "enum": [1, 2, 3, 4, 5],
      "description": "Canonical owner kind: Tenant, Organization, Group, User, or Instance."
    },
    "ownerId": {
      "type": "string",
      "description": "Canonical identifier of the typed webhook owner."
    },
    "organizationId": {
      "type": "string",
      "description": "Owning organization identifier. Used by islamuevent_org_admin derived role for organization-owned webhook resources."
    },
    "groupId": {
      "type": "string",
      "description": "Owning group identifier for group-owned webhook resources."
    },
    "userId": {
      "type": "string",
      "description": "Owning user identifier for user-owned webhook resources."
    },
    "consumerId": {
      "type": "string",
      "description": "Optional webhook consumer identifier."
    },
    "endpointId": {
      "type": "string",
      "description": "Optional webhook endpoint identifier."
    },
    "messageId": {
      "type": "string",
      "description": "Optional webhook message identifier."
    },
    "provider": {
      "type": "string",
      "description": "Optional provider name, such as local or svix."
    }
  }
}
