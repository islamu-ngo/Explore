{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Resource attributes for organization resources. Populated by ResourceDescriptors.Organization.",
  "type": "object",
  "properties": {
    "tenantId": {
      "type": "string",
      "description": "Owning tenant identifier. Used by islamuevent_tenant_admin derived role."
    },
    "organizationId": {
      "type": "string",
      "description": "Organization identifier (same as resource ID). Used by islamuevent_org_admin derived role."
    },
    "authorizationPhase": {
      "type": "string",
      "description": "Authorization phase marker. Organization create checks use pre_create before an organization ID exists."
    }
  },
  "required": ["tenantId"]
}
