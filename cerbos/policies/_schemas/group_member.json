{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Resource attributes for group_member resources. Populated by ResourceDescriptors.GroupMember.",
  "type": "object",
  "properties": {
    "tenantId": {
      "type": "string",
      "description": "Owning tenant identifier. Used by tenant_admin derived role."
    },
    "organizationId": {
      "type": "string",
      "description": "Owning organization identifier. Used by org_admin derived role."
    },
    "groupId": {
      "type": "string",
      "description": "Parent group identifier."
    },
    "userId": {
      "type": "string",
      "description": "Member user identifier."
    }
  },
  "required": ["tenantId", "groupId", "userId"]
}
