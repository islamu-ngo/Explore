{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Resource attributes for event_session resources. Populated by ResourceDescriptors.EventSession.",
  "type": "object",
  "properties": {
    "tenantId": {
      "type": "string",
      "description": "Owning tenant identifier. Used by tenant_admin derived role."
    },
    "eventId": {
      "type": "string",
      "description": "Parent event identifier."
    },
    "organizationId": {
      "type": "string",
      "description": "Owning organization identifier. Used by org_admin derived role."
    }
  },
  "required": ["tenantId", "eventId"]
}
