{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Resource attributes for event_session_agenda_item resources. Populated by ResourceDescriptors.EventSessionAgendaItem.",
  "type": "object",
  "properties": {
    "tenantId": {
      "type": "string",
      "description": "Owning tenant identifier. Used by islamuevent_tenant_admin derived role."
    },
    "eventId": {
      "type": "string",
      "description": "Parent event identifier. Used by event-role derived roles to look up principal's event assignments."
    },
    "eventSessionId": {
      "type": "string",
      "description": "Parent event session identifier."
    },
    "organizationId": {
      "type": "string",
      "description": "Owning organization identifier. Used by islamuevent_org_admin derived role."
    }
  },
  "required": ["tenantId", "eventId", "eventSessionId"]
}
