{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Resource attributes for user resources. Populated by ResourceDescriptors.User and user self-service commands.",
  "type": "object",
  "properties": {
    "tenantId": {
      "type": "string",
      "description": "Owning tenant identifier. Used by islamuevent_tenant_admin derived role."
    },
    "actorId": {
      "type": "string",
      "description": "Associated actor identifier when the profile DTO or HAL link context has already loaded the actor. Optional for self-service update commands."
    }
  },
  "required": ["tenantId"]
}
