{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Resource attributes for tenant_member resources. Populated by ResourceDescriptors.TenantMember.",
  "type": "object",
  "properties": {
    "tenantId": {
      "type": "string",
      "description": "Owning tenant identifier. Used by tenant_admin derived role."
    },
    "userId": {
      "type": "string",
      "description": "Member user identifier."
    }
  },
  "required": ["tenantId", "userId"]
}
