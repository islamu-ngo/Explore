{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Resource attributes for custom_property_value resources. Runtime values set on entities by organizers.",
  "type": "object",
  "properties": {
    "tenantId": {
      "type": "string",
      "description": "Owning tenant identifier. Used by tenant_admin derived role."
    },
    "organizationId": {
      "type": "string",
      "description": "Owning organization identifier. Used by org_admin derived role."
    }
  },
  "required": ["tenantId"]
}
