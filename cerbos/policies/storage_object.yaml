# ABOUTME: Cerbos resource policy for storage_object resources.
# Storage objects: instance admins manage all, tenant admins manage objects in their tenant.

apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: "storage_object"
  version: "default"
  importDerivedRoles:
    - explore_admin_roles
  schemas:
    principalSchema:
      ref: cerbos:///principal.json
    resourceSchema:
      ref: cerbos:///storage_object.json
  rules:
    # Instance admins can do everything
    - actions: ["*"]
      effect: EFFECT_ALLOW
      derivedRoles:
        - instance_admin

    # Tenant admins can manage storage objects within their tenant
    - actions: ["view", "create", "update", "delete"]
      effect: EFFECT_ALLOW
      derivedRoles:
        - tenant_admin

    # All authenticated users can view and upload storage objects
    - actions: ["view", "create"]
      effect: EFFECT_ALLOW
      roles:
        - authenticated_user
