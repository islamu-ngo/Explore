<!-- ABOUTME: Risk register for backend/API health refactor contract and behavior changes. -->
<!-- ABOUTME: Links breaking changes to phase, tests, OpenAPI/client impact, docs, and risk disposition. -->

# Backend Contract Risk Register

Last Updated: 2026-06-14 Europe/Brussels

## Purpose

This register tracks known backend/API contract and behavior risks before behavior-changing implementation begins. Backward compatibility is not required before v1.0, but every breaking change still needs an owner, test path, OpenAPI impact, and documentation path.

## Risk Table

| ID | Owner | Decision Owner | Area | Risk | Current Evidence | Target Change | Phase | Tests | OpenAPI/Client Impact | Docs Impact | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| R-001 | Persistence | Persistence Lead | Tenant isolation | Missing runtime tenant must not broaden tenant-scoped query filters. | 2026-06-14 review found active `ExploreDbContext.QueryFilters.cs` already compares tenant-scoped rows to `TenantFilterTenantId` and broadens only through explicit bypass state; architecture tests prevent `TenantContext == null ||` from returning. | Keep fail-closed filters, preserve design-time/seeding through explicit bypass lanes, and add richer execution-mode logging later. | 1A/1B | `TenantQueryFilterFailClosedTests`; `PersistenceTenantFilterArchitectureTests`; 2026-06-14 `Event.Persistence.IntegrationTests` passed 155/155. | No response-shape change; absent tenant remains no-row/fail-closed. | `docs/MULTI_TENANCY.md`, active plan/context. | Partially mitigated |
| R-002 | Persistence | Persistence Lead | Filter bypass | `IgnoreAllFilters()` can disable tenant and soft-delete together. | 2026-06-14 search found no production runtime `IgnoreAllFilters(` call sites outside `QueryFilterExtensions`; raw runtime `IgnoreQueryFilters()` remains confined to seeding/infrastructure or named soft-delete-only filters. | Keep full-filter bypass out of normal runtime, restrict HTTP boundary code from bypass helpers, and move toward reason-coded/audited system scopes. | 1C | `ApiControllers_ShouldNotCall_TenantFilterBypassHelpersDirectly`; existing raw `IgnoreQueryFilters()` architecture guard; 2026-06-14 `Event.Architecture.Tests` passed 191 total, 190 succeeded, 1 skipped; semantic bypass tests remain. | None unless exposed behavior changes. | `docs/MULTI_TENANCY.md`, `docs/OPERATIONS.md`, active plan/context. | Partially mitigated |
| R-003 | Auth | Authorization Lead | Authorization | Privileged-sounding policies required only authentication and could grant too much access if used. | 2026-06-14 removed `template_admin` and mapped template sync/history to `CustomPropertyTemplate` request metadata plus HAL permissions. The unused auth-only `event_editor`, `property_governance_admin`, and `platform_namespace_editor` registrations were also removed after repo-wide C# search found no active `[Authorize(Policy = ...)]` usages. | Add real capability/resource/action policies only when endpoint inventory identifies active gaps, with handler metadata, Cerbos/fallback/HAL parity, and tests. | 1D | Application authorization metadata tests; API convention tests; AuthorizationParityTests. | 401/403 behavior changes only when real endpoint policies are introduced. | `docs/SECURITY-MODEL.md`, `docs/AUTHORIZATION.md`, changelog. | Partially mitigated |
| R-004 | Auth/Ops | Platform Operations Lead | Bootstrap/admin | First-admin/setup-secret behavior was ambiguous during policy hardening and could blur bootstrap credential authority with post-bootstrap admin authority. | 2026-06-14 contract now states `complete` requires authenticated identity plus setup secret; setup secret alone is not an identity; SingleTenant grants default tenant admin and branding defaults; MultiTenant grants platform admin only; setup-secret-protected endpoints return `410 Gone` after completion. | Add remaining audit emission, missing-provider ProblemDetails, and backup-confirmed Keycloak rotation/sync tests. | 1E | Application unit tests for SingleTenant/MultiTenant role grants (`Event.Application.UnitTests` passed 1356/1356); API setup-secret authority tests added but current `Event.API.IntegrationTests` run hung during host shutdown and was killed after 219s; Keycloak backup-confirmed sync tests remain. | Setup endpoint responses locked; no compatibility guarantee during development. | `tenant-execution-model.md`, `docs/SECURITY-MODEL.md`, `docs/OPERATIONS.md`, changelog. | In progress |
| R-005 | Backend/API | API Contract Lead | Error contract | Controllers/middleware use ad hoc strings/ProblemDetails shapes. | 2026-06-14 completed the initial `ApiProblemCodes` catalog and centralized `code` extension emission for global exceptions, validation exceptions, and model-validation failures. Follow-up slices added `authentication_required` and `forbidden` ProblemDetails helpers, migrated `GroupController`/`GroupMemberController` missing-user branches away from raw string `Unauthorized(...)`, normalized `EventDayController` create/update/ID-mismatch validation failures, normalized `EventSeriesController` create/update/not-found branches, normalized `ActorSubscriptionController` write failures, normalized `TenantStorageSettingsController` missing-identity/forbidden/validation failures, normalized taxonomy write validation in `TagController`/`CategoryController`/`LocationController`, and normalized `ActorController` create/update failures, and normalized `SyncStateController`/`IndexedDidController` update mismatches and command failures, and normalized `GroupMemberController` add/update/delete failures to the shared mapper/factory pattern; many other controller-local/ad hoc helpers still remain. | Continue migrating high-risk controllers/helpers to `ApiProblemFactory`/`CommandResponseResultMapper`, then update OpenAPI ProblemDetails metadata. | 2 | Catalog/writer guardrails passed with `Event.Architecture.Tests` (193 total, 192 succeeded, 1 skipped). Controller cleanup verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped) after EventDay/EventSeries/ActorSubscription/TenantStorage normalization; taxonomy cleanup verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped). ActorController readback/AFT diagnostics were clean, but architecture verification is blocked because the existing `bash-2d49c6b2` test task could not be queried/killed through the bridge and a fresh bounded architecture run also timed out while launching. SyncState/IndexedDid verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped); GroupMember architecture verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped). GroupController create/update/approval/delete command failures were also migrated to central validation helpers; readback/AFT were clean, but architecture verification is shell-bridge blocked for that slice. TenantUserRoleGrantController create failures now use the central validation helper; Event.Architecture.Tests passed 194 total, 193 succeeded, 1 skipped. ManagedProviderProvisioningController admin denial now uses catalog-backed forbidden ProblemDetails and provisioning command failures use the central validation helper; Event.Architecture.Tests passed 194 total, 193 succeeded, 1 skipped. API integration coverage still needs the host shutdown/Docker blockers resolved. | Error body changes. | `docs/API.md`, changelog, OpenAPI schemas. | In progress |
| R-006 | Contract/OpenAPI | API Contract Lead | Route names | Route-name catalog drift can break HATEOAS link generation. | 2026-06-15 R-006 cleanup pruned/retargeted stale HAL affordances and obsolete `RouteNames` constants; final read-only classification reported 452/452 constants backed by exactly one registered endpoint with no missing or duplicate route names. Both route-name coverage guardrails are active: endpoint-to-constant and constant-to-endpoint. | Keep `RouteNameCoverageTests` active and regenerate OpenAPI/client artifacts when route templates change. | 2 | Route-name coverage tests. | Future route identity changes require changelog/OpenAPI/client workflow when externally visible. | `docs/API.md`, OpenAPI/client regen. | Mitigated |
| R-007 | Backend/API | API Architecture Lead | Controller decomposition | Splitting fat controllers can break HAL links/routes if done too early. | `EventController` combines many resources/use cases. | Split only after Phase 2 contract stabilization. | 3 | API behavior tests around changed controllers. | Route/controller organization may change; route names should remain internally consistent. | `docs/API.md`, changelog. | Open |
| R-008 | Application | Application Lead | Event creation | Create flow is too large and duplicate-submit/concurrency semantics are unclear. | `CreateEventCommandHandler` large dependency/responsibility surface. | Narrow collaborators, transaction boundary, idempotency, audit, cache invalidation. | 4 | Application unit tests; idempotency/concurrency tests. | Command response/problem codes may change. | `docs/API.md`, changelog. | Open |
| R-009 | Persistence/Contract | API Contract Lead | Pagination | Offset pagination on high-volume feeds is unstable/expensive. | Event/session/list repositories use `Skip/Take`. | Opaque versioned cursor contract and supporting indexes for high-volume feeds. | 5D/5E | API + persistence pagination tests. | Response shape changes. | `docs/API.md`, OpenAPI/client regen, changelog. | Open |
| R-010 | Persistence | Persistence Lead | Persistence cancellation | EF async calls often lack cancellation token propagation. | Event/Organization repositories and related findings. | Repository CT contracts and EF async token forwarding. | 5A | Architecture/persistence tests. | No intended API contract impact. | `docs/ARCHITECTURE.md` if convention updated. | Open |
| R-011 | Operations/Security | Security/Operations Lead | Auditability | Security-sensitive actions may lack consistent audit events. | Tenant bypass, presigned URL, lifecycle, governance, module/template actions. | Audit taxonomy and consistent event emission. | 1C/4/6 | Audit coverage tests. | No response shape impact unless surfaced IDs added. | `docs/OPERATIONS.md`, `docs/SECURITY-MODEL.md`. | Open |
| R-012 | Auth/API | Authorization Lead | Public data exposure | User/registration/storage endpoints may expose user-specific or sensitive download data as public. | Generated inventory marks `/api/eventregistration/by-user/{userId}` public; `StorageObjectController` exposes anonymous presigned URL paths. | Reclassify to authenticated/resource-protected unless public aggregate/object visibility is explicit. | 0A/1D/2 | API auth tests; endpoint classification tests. | 401/403/cache behavior may change. | `docs/API.md`, `docs/SECURITY-MODEL.md`, changelog. | Open |
| R-013 | Auth | Authorization Lead | Fallback authorization | Local fallback provider has broad view allowances and instance-admin bypass semantics that may not match policy matrix. | `FallbackAuthorizationService` allows instance-admin all resources/actions; some authenticated view/create paths are broad. | Align fallback provider with authorization-policy matrix and Cerbos semantics; add parity tests. | 1D/6 | AuthorizationParityTests; API integration auth tests. | 401/403/HAL link changes. | `docs/AUTHORIZATION.md`, `docs/SECURITY-MODEL.md`. | Open |
| R-014 | Auth/Multi-tenancy | Multi-Tenancy Lead | API-key tenant execution | Instance-admin API keys may continue without bound tenant context. | `ApiTenantPostAuthenticationMiddleware` allows instance-admin API keys to proceed without tenant context. | Model as `HostAdministration` with explicit reason/audit, or bind to tenant when operation is tenant-scoped. | 1A/1C | API key auth/tenant tests; audit tests. | Possible 404/403/tenant binding changes. | `docs/SECURITY-MODEL.md`, `docs/MULTI_TENANCY.md`. | Open |
| R-015 | Operations/Auth | Platform Operations Lead | Admin migration endpoint | Runtime database migration endpoint is protected only by authorization metadata. | `Explore.API/Program.cs` `/admin/migrate` endpoint prior evidence. | Require host-admin/migration capability, environment guard, audit, or remove dev endpoint. | 1D/1E/6 | API integration auth tests; operations tests. | Endpoint may be removed or return stricter 403/404. | `docs/OPERATIONS.md`, changelog. | Open |
| R-016 | Auth/API | Authorization Lead | Event registration PII | Anonymous registration reads expose user identity and event/session registration data. | `EventRegistrationController` anonymous reads; DTOs include `UserId`, `UserFullName`, `UserEmail`; repositories include `User.Pii`. | Reclassify/split DTOs; protect identity-bearing reads; public reads return only approved aggregate data. | 1P | API integration auth/field-shape tests. | Auth metadata, schemas, clients, cache variance change. | `docs/API.md`, `docs/SECURITY-MODEL.md`, changelog. | Open |
| R-017 | Auth/API | Authorization Lead | Tenant role grant exposure | Anonymous read endpoints expose user email/name, role, tenant, grant, and revocation metadata. | `TenantUserRoleGrantController` overrides class `[Authorize]` with `[AllowAnonymous]`; grant DTOs include identity/role metadata. | Reclassify as admin/resource-protected or create safe projection. | 1P | API integration auth tests; endpoint classification tests. | 401/403 and schema changes. | `docs/AUTHORIZATION.md`, `docs/API.md`, changelog. | Open |
| R-018 | Auth/API | Authorization Lead | Organization member exposure | Anonymous organization member reads expose user email/name/role/position. | `OrganizationMemberController` anonymous read actions; `OrganizationMemberDto` and mapping include identity/role fields. | Protect member reads or split safe public organization profile projection. | 1P | API integration auth/field-shape tests. | Auth/schema/cache changes. | `docs/API.md`, `docs/SECURITY-MODEL.md`, changelog. | Open |
| R-019 | Auth/API | Authorization Lead | Footer write authorization | Footer management writes require only authentication and may use `Guid.Empty` actor fallback. | `FooterController` write endpoints pass `CurrentUserId ?? Guid.Empty`; footer commands lack secure request/resource metadata. | Add `Footer.Manage`, fail closed on missing actor, align handlers/HAL/audit. | 1P/1D | API auth tests; AuthorizationParityTests. | 403 behavior and HAL rel changes. | `docs/AUTHORIZATION.md`, `docs/API.md`. | Open |
| R-020 | Blazor/API Contract | Presentation Lead | HAL affordance bypass | Blazor shows action buttons from local roles/empty-list fallback instead of HAL `_links`. | `MyEvents`, `MyOrganizations`, `EventTemplateListPage` findings. | Gate actions by HAL links/collection affordances only. | 1P/6 | Blazor client tests; architecture guardrail. | UI behavior changes; API may need collection-level rel. | `docs/BLAZOR.md`, `docs/API.md`. | Open |
| R-021 | Domain/Persistence | Architecture Lead | Domain mapping annotations | Domain carries persistence attributes and DataAnnotations imports. | 80 matches across 25 Domain files; `Event` has many `[ForeignKey]`. | Move mapping into Persistence EF configuration; add architecture test. | 6 | Architecture tests; persistence model tests. | No API impact intended. | `docs/ARCHITECTURE.md`, `docs/GOVERNANCE.md`. | Open |
| R-022 | Application | Application Lead | Query response envelopes | Queries return `BaseCommandResponse<T>` and use command `Id` slot for data. | Email dispatch status, event aggregate view, custom-property projection query findings. | Queries return DTO/list/page/null results; API maps not-found/errors separately. | 4 | Application unit tests; CQRS architecture tests. | Response schemas may change. | `docs/API.md`, changelog, OpenAPI/client regen. | Open |
| R-023 | Application/Persistence | Architecture Lead | Repository DTO input coupling | Repository contract accepts Application DTO filter. | `IEventAggregateViewRepository` accepts `AggregateViewFilterDto`. | Replace with specification/query object/read-model port. | 5B | Architecture tests; repository tests. | No external API impact intended. | `docs/ARCHITECTURE.md`. | Open |
| R-024 | Persistence | Persistence Lead | Tracked generic reads | Generic read helpers track entities by default and `Exists` materializes via `FindAsync`. | `GenericRepository.GetById`; `Exists` calls `GetById`. | Split tracked mutation loads from no-tracking reads/existence checks. | 5C | Persistence tests; performance review. | No external API impact intended. | `docs/ARCHITECTURE.md`. | Open |
| R-025 | Persistence/Security | Security Lead | Broad hard delete | `HardDelete` is globally exposed with comment-only admin constraint. | `IGenericRepository.HardDelete`; implementation removes and saves. | Quarantine/delete/restrict behind explicit admin lifecycle API and tests. | 5B/6 | Architecture tests; API auth tests for callers. | Possible behavior hardening. | `docs/SECURITY-MODEL.md`, `docs/OPERATIONS.md`. | Open |
| R-026 | Application/Persistence | Performance Lead | Tenant settings N+1 | Tenant policy settings read path performs dozens of sequential per-key lookups. | `TenantPolicySettingService.Read`; `GetAllForTenant` unused; per-key repo lacks `AsNoTracking`. | Batch settings reads and use no-tracking read path. | 5C | Application unit tests; persistence query tests. | No API contract impact intended. | `docs/OPERATIONS.md` if operational behavior changes. | Open |
| R-027 | Application | Application Lead | Duplicated template pipelines | Event/session template diff/sync/instantiation and projection filters duplicate algorithms. | AFT duplicate findings and manual reads. | Consolidate shared algorithm or defer with owner/rationale. | 4/6 | Application unit regression tests. | No API impact intended. | `docs/ARCHITECTURE.md` if pattern added. | Open |
| R-028 | Application | Application Lead | AI handler bloat | AI command handlers mix validation/auth/settings/provider/idempotency/quota/mutation concerns. | `SendAiMessageCommandHandler`, `ProcessAiRunCommandHandler`. | Decompose into narrow collaborators preserving MediatR boundaries. | 4 | Application unit tests; API integration for behavior. | Error/response contracts may change via Phase 2. | `docs/API.md`, changelog if changed. | Open |
| R-029 | Hygiene/API | API Architecture Lead | Production TODO/stubs | User-facing TODOs/stubs and mock data remain in production paths. | `ImageStorageService`, `EventSessionSpeakerService`, `AtprotoAuthenticationHandler`, `UserController`, `TenantMembersSection`. | Implement, remove, or explicitly defer behind risk rows. | 6 | Targeted tests per stub. | May add/alter API/UI behavior. | Relevant feature docs/changelog. | Open |
| R-030 | Reliability | Presentation Lead | Empty catch | Component cleanup swallows exceptions silently. | `CreateOrganization.razor.cs` `catch { }`. | Log or handle intentionally; add test if behavior important. | 6 | Blazor client tests if touched. | No API impact. | None unless convention doc updated. | Open |
| R-031 | Auth/Multi-tenancy | Multi-Tenancy Lead | Semantic tenant bypass proof | Reason-string bypass tests do not prove restrictive predicates after disabling tenant filter. | 2026-06-14 added first semantic proof for `TenantSettingsDocumentRepository.GetByTenantAndDocumentKey`: ambient tenant B plus explicit tenant A must return tenant A's same-key document. Controller/raw bypass usage is guarded; each remaining bypass call site still needs this proof. Verification of the new persistence test is blocked by Docker/Testcontainers unavailability at `unix:///home/amir/.docker/desktop/docker.sock`. | Add semantic tests/reviews for each bypass call site and introduce audited system-scope API if Phase 1C formalizes execution modes. | 1C/6 | Persistence/API integration tests; repository-specific bypass predicate tests; rerun `Event.Persistence.IntegrationTests` when Docker is available. | Potential 403/404/no-row changes. | `docs/MULTI_TENANCY.md`, active context/tasks. | In progress |

### R-005 Implementation Notes
- 2026-06-14: `EventTemplateSyncController` and `EventSessionTemplateSyncController` diff failure branches now use sync-specific RFC7807 validation helpers instead of raw command envelopes. Readback and AFT diagnostics were clean; architecture verification is blocked because `bash-df9852dc` could not be inspected through the shell bridge.
- 2026-06-14: `TenantOnboardingController` complete/update-settings/save-step failures now use catalog-backed RFC7807 helpers: missing resolved user identity maps to `authentication_required`, tenant-admin denial maps to `forbidden`, and remaining command failures use operation-specific validation descriptors. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.

- 2026-06-14: `TenantSettingsDocumentsController` branding replacement failures now use the shared RFC7807 mapper/factory pattern: missing branding documents return `resource_not_found` through `ApiNotFoundProblemDescriptor` + `ToNotFoundProblem`, and replacement validation failures use `ApiValidationProblemDescriptor` + `ToCommandValidationProblem`. Readback and AFT diagnostics were clean; architecture verification is blocked by shell-bridge timeouts while checking the prior run and launching a fresh bounded run.
- 2026-06-14: `SettingsController` single-setting/reset/lock/unlock command failures now use the shared RFC7807 mapper/factory pattern, and administrator denials return catalog-backed `forbidden`; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.

## Status Legend

- `Open`: known risk not yet implemented.
- `In Progress`: implementation slice active.
- `Mitigated`: implementation and tests landed.
- `Deferred`: intentionally delayed with rationale and owner.

- 2026-06-14: `EventSessionTemplateController` create/update validation failures and update ID mismatch now use central RFC7807 validation helpers; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `EventTemplateController` update ID mismatch now uses central RFC7807 validation details through `ApiValidationProblemDescriptor` plus `ToValidationProblem`; readback/AFT clean, architecture verification blocked by shell-bridge timeouts while inspecting `bash-9aacb3b8` and launching a fresh bounded run.
- 2026-06-14: shared, event-local, and event-session-local custom-property definition update ID mismatches now use central RFC7807 validation details through resource-specific `ApiValidationProblemDescriptor` values plus `ToValidationProblem`; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: shared, event-local, and event-session-local custom-property purge fallback branches now use central RFC7807 not-found/validation helpers instead of raw command envelopes; readback confirmed the target branches are normalized, but architecture verification is blocked because `bash-3ad2ba99` could not be inspected through the shell bridge.
- 2026-06-14: `OrganizationController` create/update/delete and missing-identity failures now use central RFC7807 helpers instead of raw command envelopes or a controller-local unauthorized helper; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `UserExternalLoginController` create failures now use the central RFC7807 validation helper instead of returning the raw command envelope; existing update/not-found local helpers remain future consolidation debt. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `TenantController` create/update and tenant-navigation create/update/delete/reorder failures now use central RFC7807 validation/not-found helpers instead of raw command envelopes or anonymous mismatch payloads; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `LocalizationAdminController` TMS connection, governance update, and TMS export failures now use operation-specific RFC7807 validation helpers instead of raw command envelopes; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `FooterController` tenant footer link-group/link/settings write failures now use central RFC7807 validation helpers and missing current-user identity uses the shared `authentication_required` helper; readback/AFT were clean, but architecture verification is blocked because `bash-a6ec862c` could not be inspected through the shell bridge.
- 2026-06-14: `ContactShareConsentController` missing current-user, unresolved recipient-organization, and export failure paths now use central RFC7807 helpers instead of raw `Unauthorized()`, `NotFound()`, or `BadRequest(result)` responses; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `UiThemeAdminController` create/update validation failures, route/payload ID mismatch, and authorization-denial paths now use central RFC7807 helpers instead of raw `BadRequest(response)`, anonymous command-envelope mismatch responses, or raw `Unauthorized(response)` envelopes; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `StorageObjectController` authenticated upload-session missing-auth checks and storage-object update failures now use central RFC7807 helpers instead of raw `Unauthorized()`, anonymous ID-mismatch JSON, or raw `BadRequest(response)` responses; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `EventTeamController` missing current-user branches now use `ToAuthenticationRequiredProblem` instead of raw `Unauthorized()` responses on current-user permission, assignable-preset, role-assignment, and revocation endpoints; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `UserController` sync/current-user/admin-authority/organization/update/delete local auth and validation responses now use central RFC7807 helpers instead of raw command envelopes, strings, local ProblemDetails helpers, or string `Forbid(...)`; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `InstanceSettingsController` repeated setup/admin denials, invalid identity branches, deployment-mode operator-control refusal, and shared command-response helper now use central RFC7807 helpers instead of raw `Forbid()`, `BadRequest(InvalidIdentityResponse())`, and command-envelope responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `OrganizationMemberController` member add, role update, invitation accept/decline, and delete missing-identity paths now use the shared `authentication_required` RFC7807 helper instead of a controller-local unauthorized ProblemDetails helper; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `AiAssistantController` model-discovery provider JSON/HTTP/timeout failures now use central 502 ProblemDetails through `ToBadGatewayProblem`, `ApiProblemTypes.BadGateway`, and `ApiProblemCodes.ProviderGateway` instead of controller-local ad hoc gateway ProblemDetails; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `EventAgendaItemController` and `EventDayController` read-side missing-resource branches now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` on agenda item detail, agenda projection, and event day detail endpoints; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `EventSessionLanguageController` create/delete missing-session and missing-language branches now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `LocationRoomController` room detail missing-resource handling now uses a location-room RFC7807 not-found helper instead of a bare `NotFound()` response; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `ActorSubscriptionController` actor-subscription detail missing-resource handling now uses an actor-subscription RFC7807 not-found helper instead of a bare `NotFound()` response; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `GroupController` group detail missing-resource handling now uses a group RFC7807 not-found helper instead of a bare `NotFound()` response; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `GroupMemberController` member detail and `EventSessionGroupController` program-section detail missing-resource handling now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `TenantUserRoleGrantController`, `OrganizationMemberController`, and `IndexedDidController` missing-resource handling now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Shared, event-local, and event-session-local custom-property definition detail endpoints now use definition-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Event template, event-session template, and event-session detail endpoints now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Actor-by-id, actor-by-DID, and organization detail endpoints now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Storage metadata/content/public image, tenant branding document, and UI theme detail/delete endpoints now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: External API key detail, auth context probe, AI conversation/run detail, and event session-create/program/detail/calendar missing-resource branches now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: The remaining raw controller-return cluster was migrated to central RFC7807 helpers. Event delete and organization invitation email checks now use catalog-backed authentication-required responses; event publish concurrency uses a central conflict helper; setup-secret validation after completion uses a central `Gone` ProblemDetails helper with `setup_already_completed` code; authorization policy package download failures use a central service-unavailable helper; and rejected analytics relay events use a validation descriptor with `analytics_relay_rejected`. A direct controller grep no longer finds raw `BadRequest`, `Unauthorized`, `Forbid`, `NotFound`, `Conflict`, `StatusCode`, or `Problem` calls; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `ExternalApiKeyController` was consolidated away from its controller-local ProblemDetails mapper. Create/update validation, ID mismatch, and not-found outcomes now use central RFC7807 descriptors and mapper helpers, and the local validation/not-found code constants plus hand-built `ProblemDetails`/`ValidationProblemDetails` helpers were removed; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `UserExternalLoginController` was consolidated away from its controller-local ProblemDetails mapper. Create/update validation, ID mismatch, and not-found outcomes now use central RFC7807 descriptors and mapper helpers, and the local validation/not-found code constants plus hand-built `ProblemDetails`/`ValidationProblemDetails` helpers were removed; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `AtprotoRecordController` was consolidated away from its controller-local validation ProblemDetails mapper. Update ID mismatch and update command failures now use central RFC7807 descriptors and mapper helpers, and the local validation code constant plus hand-built `ValidationProblemDetails` helper were removed; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `NotificationController` was consolidated away from its controller-local not-found ProblemDetails builder. Archive and snooze not-found outcomes now use central RFC7807 descriptors and mapper helpers, and the local hand-built `ProblemDetails`/`application/problem+json` helper was removed; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `CustomPropertyProjectionAdminController` was consolidated away from its remaining controller-local projection-status ProblemDetails helper. Event and session projection status lookup failures now use central RFC7807 descriptors and mapper helpers, and the hand-built projection `ValidationProblemDetails` helper was removed; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `EventSessionController`, `EventSessionGroupController`, and `EventSessionLanguageController` were consolidated away from the shared controller-local `ProgramValidationProblemDetails` helper. Program/session validation failures now use central RFC7807 descriptors and mapper helpers, `ProgramValidationProblemDetails.cs` was deleted, and `ToProgramValidationProblem` has no references; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `StorageUploadProblemDetails.cs` was removed from `Explore.API/Controllers`. Storage upload-session failures now map through `CommandResponseResultMapper.ToStorageUploadProblem` and `ApiProblemFactory.CreateProblem`, preserving storage-specific 404/409/413/422/503 mappings while using the central RFC7807 result path; verification was limited to readback, direct grep, and AFT diagnostics per user direction.
- 2026-06-14: `EmailDispatchAdminController` and `AiAssistantProblemDetails` were consolidated into central RFC7807 mapper/factory paths. Email status/command failures now use `CommandResponseResultMapper.ToEmailDispatchValidationProblem`/`ToEmailDispatchProblem`; AI assistant command failures now use central `ToAiAssistantProblem` with provider, rate-limit, quota, status, and quota-extension metadata mappings preserved. Controller-local ProblemDetails construction was removed, and verification was limited to readback, direct grep, and AFT diagnostics per user direction.
- 2026-06-14: `OrganizationController` and `GroupController` approval-status route templates were normalized from action-word `updatestatustype/{id:guid}` to resource-oriented `{id:guid}/approval-status` while preserving the existing `RouteNames.UpdateOrganizationApprovalStatus` and `RouteNames.UpdateGroupApprovalStatus` constants. Direct organization integration tests were updated; generated OpenAPI/client artifacts remain stale until the later regeneration workflow.
- 2026-06-14: The same approval-status endpoints also received explicit OpenAPI error payload metadata. Organization approval status now declares ProblemDetails for 401/403/404; group approval status now declares `ValidationProblemDetails` for 400 and ProblemDetails for 401/403/404, reducing R-005 response-metadata drift before the broader metadata guardrail is enabled.
- 2026-06-14: `OrganizationController` and `GroupController` list/detail/create/update/delete metadata was tightened without changing action behavior. Validation failures now advertise `ValidationProblemDetails`, auth/forbidden/not-found outcomes advertise `ProblemDetails`, and group delete advertises its `BaseCommandResponse<Guid>` success payload, reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventSeriesController` and `EventSessionAgendaItemController` response metadata was tightened without changing action behavior. Event-series success responses now advertise concrete DTO/command-response payloads, validation failures advertise `ValidationProblemDetails`, not-found failures advertise `ProblemDetails`, and the agenda-item list validation metadata now advertises `ValidationProblemDetails` instead of a bare 400.
- 2026-06-14: `AnalyticsRelayController.Relay` and `CustomPropertyGovernanceController.GetGovernanceReport` response metadata was tightened without changing action behavior. Both endpoints now advertise `ValidationProblemDetails` for their existing 400 validation responses instead of bare status-only metadata.
- 2026-06-14: `LocationController` and `LocationRoomController` response metadata was tightened without changing action behavior. Existing validation paths now advertise `ValidationProblemDetails`, while authentication and not-found paths advertise `ProblemDetails`, matching the controllers' central RFC7807 mapper usage and reducing R-005 OpenAPI response drift.
- 2026-06-14: Seven read-only lookup detail endpoints had their existing 404 metadata tightened without changing behavior. `GroupPositionController`, `FileTypeController`, `EventStatusController`, `AudienceGenderController`, `AudienceAgeController`, `VisibilityTypeController`, and `ActorTypeController` now advertise `ProblemDetails` instead of bare status-only 404 responses, reducing R-005 OpenAPI response drift.
- 2026-06-14: Three additional detail endpoints had their existing 404 metadata tightened without changing behavior. `DidCustodyTypeController`, `RoleController`, and `ActorKeyStoreController.GetById` now advertise `ProblemDetails` instead of bare status-only 404 responses, further reducing R-005 OpenAPI response drift.
- 2026-06-14: Three more read-only lookup detail endpoints had their existing 404 metadata tightened without changing behavior. `RegistrationModeController`, `OrganizationPositionController`, and `MadhabController` now advertise `ProblemDetails` instead of bare status-only 404 responses, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `UserAuthenticationTokenController`, `UserExternalLoginController`, and `EventTeamController` response metadata was tightened without changing behavior. Existing not-found metadata now advertises `ProblemDetails`, external-login create validation advertises `ValidationProblemDetails`, and event-team missing-auth metadata advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `TenantOnboardingController` and `ModuleController` response metadata was tightened without changing behavior. Tenant onboarding command validation paths now advertise `ValidationProblemDetails`, tenant admin denial paths advertise `ProblemDetails`, module schema not-found metadata advertises `ProblemDetails`, and module enable/disable validation/auth/forbidden metadata now matches central RFC7807 helper usage, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `ExternalApiKeyController`, `TenantUserRoleGrantController`, and `ManagedProviderProvisioningController` response metadata was tightened without changing behavior. Existing not-found and forbidden metadata now advertises `ProblemDetails`, and command validation metadata now advertises `ValidationProblemDetails`, matching central RFC7807 helper usage and further reducing R-005 OpenAPI response drift.
- 2026-06-14: `TagController`, `CategoryController`, and `OrganizationMemberController` response metadata was tightened without changing behavior. Taxonomy validation paths now advertise `ValidationProblemDetails`, taxonomy authentication/not-found metadata advertises `ProblemDetails`, and organization-member detail not-found metadata now matches central RFC7807 helper usage, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventTemplateController` and `EventSessionTemplateController` response metadata was tightened without changing behavior. Template list/create/update validation paths now advertise `ValidationProblemDetails`, authentication/not-found metadata advertises `ProblemDetails`, and existing event-template quota responses continue to advertise `ProblemDetails` 422, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventSessionLanguageController`, `EventSessionGroupController`, and `EventAgendaItemController` response metadata was tightened without changing behavior. Existing authentication and not-found metadata now advertises `ProblemDetails`, while validation metadata remains `ValidationProblemDetails`, matching central RFC7807 helper usage and further reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventSessionController` and `EventDayController` response metadata was tightened without changing behavior. Event-session authentication/not-found metadata now advertises `ProblemDetails`; event-day create/update validation metadata now advertises `ValidationProblemDetails`, and event-day authentication/not-found metadata now advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `CustomPropertyDefinitionController`, `EventCustomPropertyController`, and `EventSessionCustomPropertyController` response metadata was tightened without changing behavior. Custom-property list/create/update/purge/value validation metadata now advertises `ValidationProblemDetails`; authentication, authorization, and not-found metadata now advertises `ProblemDetails`; existing quota/conflict responses remain typed as `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `TenantStorageSettingsController`, `ActorSubscriptionController`, and `TenantSettingsDocumentsController` response metadata was tightened without changing behavior. Tenant storage and actor-subscription command validation now advertises `ValidationProblemDetails`; tenant branding document authentication/authorization/not-found metadata now advertises `ProblemDetails`; existing conflict metadata remains typed as `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `GroupMemberController` and `ContactShareConsentController` response metadata was tightened without changing behavior. Group-member authentication/not-found metadata now advertises `ProblemDetails`; contact-share withdraw authentication metadata advertises `ProblemDetails`; shared-contact list/export validation metadata advertises `ValidationProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventFormatController` and `EventTemplateSyncController` response metadata was tightened without changing behavior. Event-format detail not-found metadata now advertises `ProblemDetails`, and template-sync diff/apply/history authentication and authorization metadata now advertises `ProblemDetails` while existing validation/not-found/conflict metadata remains typed, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `SettingsController` and `StorageObjectController` response metadata was tightened without changing behavior. Settings validation metadata now advertises `ValidationProblemDetails`, settings authentication/authorization metadata advertises `ProblemDetails`, and storage list/upload/object validation plus authentication/not-found metadata now advertises the central RFC7807 payload types, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventSessionTemplateSyncController` and `UiThemeAdminController` response metadata was tightened without changing behavior. Event-session template sync authentication and authorization metadata now advertises `ProblemDetails`; UI theme admin validation metadata now advertises `ValidationProblemDetails`, and authentication/authorization/not-found metadata now advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `NotificationController` response metadata was tightened without changing behavior. Notification list validation metadata now advertises `ValidationProblemDetails`, and existing authentication/not-found metadata now advertises `ProblemDetails`, including the read, archive, snooze, delete, unread-count, and refresh-stream endpoints, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `ActorController` response metadata was tightened without changing behavior. Actor list and tenant-list validation metadata now advertises `ValidationProblemDetails`; actor detail/DID not-found metadata and create/update/delete authentication, authorization, and not-found metadata now advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `LocalizationAdminController` and `EventRegistrationController` response metadata was tightened without changing behavior. Localization test/governance/export validation metadata now advertises `ValidationProblemDetails`, localization admin authentication metadata advertises `ProblemDetails`, and event-registration list/create validation plus authentication/not-found metadata now advertises central RFC7807 payload types, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `CustomPropertyProjectionAdminController` response metadata was tightened without changing behavior. Projection rebuild, single-rebuild, dirty-scope, and projection lookup validation metadata now advertises `ValidationProblemDetails`, while existing quota responses remain typed as `ProblemDetails` 422, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `FooterController` response metadata was tightened without changing behavior. Footer link-group, link, reorder, and tenant footer settings validation metadata now advertises `ValidationProblemDetails`, while existing link-group/link not-found metadata now advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `InstanceOnboardingController` command response metadata was tightened without changing behavior. Onboarding completion, auth-provider save/bootstrap, authorization-provider save/sync, and endpoint verification validation failures now advertise `ValidationProblemDetails` instead of command envelopes, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `TenantController` response metadata was tightened without changing behavior. Tenant create/update and tenant-navigation create/update/reorder validation metadata now advertises `ValidationProblemDetails`; tenant and navigation-link not-found metadata now advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `UserAppearanceController` response metadata was tightened without changing behavior. Appearance profile creation validation now advertises `ValidationProblemDetails`; existing authenticated/not-found metadata for appearance reads, profile actions, palette generation, archiving, and duplication now advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventController` response metadata was tightened without changing behavior. Event list/auth, creation, publish, draft/status update, delete, calendar/program/detail, readiness, and Islamic/tech aspect endpoints now advertise `ValidationProblemDetails` for validation failures and `ProblemDetails` for authentication, authorization, not-found, and publish-conflict failures, further reducing R-005 OpenAPI response drift.
- 2026-06-15: `OpenApiStringEnumSchemaCatalog` was expanded to cover the DTO-exposed Domain enum set plus Application DTO/model enums discovered by the new guard (`SettingSource`, `CustomPropertyFilterOperator`, `ManagedProviderOrganizerKindDto`, `BatchUpdateMode`, public-experience CTA/home-block enums, `PublicExperienceMode`, `PublicExperiencePrimaryOrganizationState`, and `KeycloakBootstrapMode`), fixing the string-enum schema root cause behind OpenAPI contract drift. `ApiContractArchitectureTests.DtoEnumProperties_MustBeRegisteredIn_OpenApiStringEnumSchemaCatalog` now scans public DTO contract enum properties, excludes validator implementation types, and compares the discovered enum set to the internal API catalog by reflection, so future missing registrations fail architecture tests before generated OpenAPI falls back to integer enum schemas. Verification: scoped AFT diagnostics were clean; project-level `Event.Architecture.Tests` no longer reports the enum-catalog test as failing, but still exits non-zero because of pre-existing agent-context manifest/dead-link failures under `.claude/`.
- 2026-06-15: `InstanceSettingsController` response metadata was tightened without changing behavior. Instance admin/setup-secret forbidden paths now advertise `ProblemDetails`, and settings, authentication, storage, provider, authorization, and deployment command-validation paths now advertise `ValidationProblemDetails` instead of command envelopes. Focused and broad controller greps for stale 4xx/command-envelope metadata now return no matches under `Explore.API/Controllers`, closing the current R-005 response-metadata sweep target.
- 2026-06-15: Route-template cleanup is closed for the current inventory: the only confirmed action-word route risk was the organization/group approval-status route already normalized from `updatestatustype/{id:guid}` to `{id:guid}/approval-status`; generated OpenAPI/client regeneration remains pending for that recorded contract change. The preview/v1.0 contract stance is now explicit in `docs/API.md`: pre-v1.0 breaking contract changes may be accepted for cleaner API/HAL/OpenAPI contracts only with `docs/API_CHANGELOG.md`, regenerated OpenAPI/inventory/generated-client workflows as applicable, and contract-governance evidence; generated artifacts must not be hand-edited, and v1.0 breaking schema diffs become blocking per governance.
- 2026-06-15: R-005 metadata/classification review is now backed by OpenAPI posture vendor extensions. `EndpointClassificationTransformer` emits `x-endpoint-class`, `x-rate-limit-policy`, `x-output-cache-policy`, and `x-tenant-mode` from endpoint metadata, and `ContractInvariantsTests.OpenApiDocument_EndpointPostureExtensionsExposeRateCacheAndTenantMetadata` guards representative rate-limit/cache/tenant emission. Broad per-endpoint inventory completeness remains future governance, but the Phase 2 contract mechanism now exposes these values in runtime OpenAPI.
- 2026-06-15: Phase 2 verification remains blocked by unrelated environment/context failures, not by the latest R-005 metadata or enum-catalog slices. Scoped AFT diagnostics are clean and the enum-catalog architecture guard passed in the supported project-level architecture run, but `Event.Architecture.Tests` still exits non-zero because of pre-existing `.claude` agent-context manifest/dead-link failures. API integration tests remain blocked by Docker/Testcontainers/host-shutdown instability, and full build/Blazor verification remains blocked by the pre-existing `AiActionResultCard.razor` Razor syntax error.
- 2026-06-15: Missing-identity authorization response risk is closed for the original anti-pattern. Controllers now use catalog-backed `authentication_required` ProblemDetails helpers instead of raw missing-user 401 strings; focused grep found no raw `Unauthorized(...)`, `Forbid()`, or `"User ID not found"` controller returns, and `ApiContractArchitectureTests.Controllers_MustNotReturn_RawMissingUserUnauthorizedStrings` guards against reintroducing the old string response. Remaining architecture failures are unrelated `.claude` context issues.
- 2026-06-15: R-006 reverse `RouteNames` cleanup began by pruning truly unused constants from `RouteNames.cs` after classifying candidate orphan names by reference usage. Legacy lookup aliases, unused approval-status/event-type CRUD names, stale user and organization-member CRUD names, stale current-user appearance update, and unused event-team ownership/window names were removed. Policy-used constants that no longer resolve to endpoints remain a separate design decision and must be reattached or removed from stale HAL affordances before `RouteNames_EveryConstantResolvesToExactlyOneEndpoint` can be unskipped.
- 2026-06-15: R-006 cleanup continued by realigning stale HATEOAS policy route names. ATProto record policies now use the registered `*AtprotoRecordEntry` route names and no longer emit the unsupported `by-uri` affordance; event-registration policies now use the registered `*EventRegistration` route names. The obsolete ATProto/registration aliases were removed from `RouteNames.cs`, focused grep found no remaining references, and AFT diagnostics were clean. Remaining policy-used/no-endpoint candidates still need classification before reverse route-name coverage can be enabled.
- 2026-06-15: R-006 cleanup removed unsupported arbitrary-user and tenant-by-slug HAL affordances. User, tenant-user-role, organization-review, organization-member, group-member, event-registration, and tenant policies no longer emit links backed by missing `GetUserById`, `GetUsers`, `UpdateUser`, or `GetTenantBySlug` endpoints; supported current-user, actor, user organization/registration, tenant-by-id, tenant collection, and tenant create links remain. The obsolete constants were removed from `RouteNames.cs`, focused grep found no remaining references, and AFT diagnostics were clean. Remaining policy-used/no-endpoint candidates still need classification before reverse route-name coverage can be enabled.
- 2026-06-15: R-006 cleanup removed unsupported relationship/review HAL affordances that had no registered endpoints. Organization, actor, event, category, tag, and organization-review policies no longer emit links backed by missing `GetOrganizationEvents`, `GetActorEvents`, `GetEventCategories`, `GetEventTags`, `GetCategoryChildren`, `GetCategoryEvents`, `GetTagEvents`, `GetTagTagTypes`, `GetOrganizationReviewById`, `UpdateOrganizationReview`, or `DeleteOrganizationReview` routes. The obsolete constants were removed from `RouteNames.cs`, focused grep found no remaining references, and AFT diagnostics were clean. Remaining policy-used/no-endpoint candidates still need classification before reverse route-name coverage can be enabled.
- 2026-06-15: R-006 cleanup retargeted or removed the remaining event-session, organization-member, and user-registration affordance drift. `EventSessionLinkPolicy` no longer emits the unsupported `GetEventSessionSpeakers` link, `OrganizationLinkPolicy` now targets registered `GetOrganizationMembersByOrganization` with the expected route parameter, and `UserLinkPolicy` now targets registered `GetRegistrationsByUser`. The obsolete `GetEventSessionSpeakers`, `GetOrganizationMembers`, and `GetUserRegistrations` constants were removed from `RouteNames.cs`, focused grep found no remaining references, and AFT diagnostics were clean. Remaining policy-used/no-endpoint candidates still need classification before reverse route-name coverage can be enabled.
- 2026-06-15: R-006 reverse route-name coverage was closed after the final read-only RouteNames classification reported 452/452 constants backed by exactly one registered endpoint with no missing or duplicate route names. `RouteNames_EveryConstantResolvesToExactlyOneEndpoint` is now unskipped, making the reverse guard active alongside endpoint-to-constant coverage. Full API integration execution remains deferred to the blocked verification item.
- 2026-06-15: Phase 2 implementation-side contract stabilization is complete enough to enter Phase 3 with caveats: RFC7807 cleanup, route-template cleanup, bidirectional route-name guardrails, response metadata, endpoint classification/auth alignment, OpenAPI enum catalog coverage, and OpenAPI rate/cache/tenant posture extensions are in place. Remaining verification risk is explicitly external/pre-existing: architecture tests still fail on `.claude` context issues, API integration execution remains blocked by Docker/Testcontainers/host-shutdown instability, and full build/Blazor verification remains blocked by the pre-existing `AiActionResultCard.razor` syntax error.
- 2026-06-14: `ModuleController` missing-schema and module enable/disable failures now use central RFC7807 not-found/validation helpers instead of controller-local `Problem(...)` calls; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `EventRegistrationController` create/update/delete failure paths now use central RFC7807 validation, authentication, and not-found helpers instead of controller-local ProblemDetails construction; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `TagController`, `CategoryController`, and `LocationController` detail-read missing-resource branches now use central RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `EventController.GetMyEvents` missing-identity handling now uses the shared `authentication_required` RFC7807 helper instead of raw `Unauthorized()`; per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: selected `CustomPropertyProjectionAdminController` command-failure branches now use central RFC7807 helpers instead of raw `BadRequest(result)` command envelopes. Single-event rebuild, dirty-scope drain, and single-session rebuild failures use resource-specific validation descriptors plus `ToCommandValidationProblem`; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: user direction changed the continuation loop to stop launching redundant test suites and prioritize implementation progress. Later scoped controller-normalization entries should rely on direct readback/AFT diagnostics until the plan reaches a dedicated verification phase.
- 2026-06-14: `AiAssistantController` model-discovery authorization and validation failures now use central RFC7807 helpers instead of raw `Forbid()` or `BadRequest(CreateProblem(...))` 400s. Tenant/platform-admin denial maps to `forbidden`; missing/invalid provider endpoint input maps to validation ProblemDetails via the `aiAssistantModelDiscovery` descriptor. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: `CustomPropertyProjectionAdminController` event/session projection lookup failures now use central RFC7807 helpers instead of raw command envelopes. Event and session lookup branches use dedicated validation descriptors plus `ToCommandValidationProblem`; direct readback corrected a descriptor mix-up before recording. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: `InstanceOnboardingController` completion and setup-secret command failures now use central RFC7807 helpers instead of raw command envelopes. Missing resolved user identity maps to `authentication_required`, preflight blockers use validation ProblemDetails, and auth-provider/keycloak-bootstrap/authorization-provider/policy-sync/endpoint-verify failures use operation-specific descriptors plus `ToCommandValidationProblem`. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: `EventController` create, publish, draft update, status update, Islamic aspect upsert, and tech aspect upsert failures now use central RFC7807 helpers instead of raw command envelopes. Event aspect not-found outcomes use a shared not-found descriptor; publish concurrency conflict remains a later conflict-mapping slice. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: `SettingsController` user and tenant batch update failures now use the existing `settings` validation descriptor plus `ToValidationProblem` instead of raw `BadRequest(result)` DTO envelopes. Per user direction, verification was limited to readback and AFT diagnostics.
