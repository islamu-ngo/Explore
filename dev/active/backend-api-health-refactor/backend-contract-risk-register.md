<!-- ABOUTME: Risk register for backend/API health refactor contract and behavior changes. -->
<!-- ABOUTME: Links breaking changes to phase, tests, OpenAPI/client impact, docs, and risk disposition. -->

# Backend Contract Risk Register

Last Updated: 2026-07-04 Europe/Brussels

## Purpose

This register tracks known backend/API contract and behavior risks before behavior-changing implementation begins. Backward compatibility is not required before v1.0, but every breaking change still needs an owner, test path, OpenAPI impact, and documentation path.

## Risk Table

| ID | Owner | Decision Owner | Area | Risk | Current Evidence | Target Change | Phase | Tests | OpenAPI/Client Impact | Docs Impact | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| R-001 | Persistence | Persistence Lead | Tenant isolation | Missing runtime tenant must not broaden tenant-scoped query filters. | 2026-06-14 review found active `ExploreDbContext.QueryFilters.cs` already compares tenant-scoped rows to `TenantFilterTenantId` and broadens only through explicit bypass state; architecture tests prevent `TenantContext == null ||` from returning. | Keep fail-closed filters, preserve design-time/seeding through explicit bypass lanes, and add richer execution-mode logging later. | 1A/1B | `TenantQueryFilterFailClosedTests`; `PersistenceTenantFilterArchitectureTests`; 2026-06-14 `Event.Persistence.IntegrationTests` passed 155/155. | No response-shape change; absent tenant remains no-row/fail-closed. | `docs/MULTI_TENANCY.md`, active plan/context. | Partially mitigated |
| R-002 | Persistence | Persistence Lead | Filter bypass | `IgnoreAllFilters()` can disable tenant and soft-delete together. | 2026-06-14 search found no production runtime `IgnoreAllFilters(` call sites outside `QueryFilterExtensions`; raw runtime `IgnoreQueryFilters()` remains confined to seeding/infrastructure or named soft-delete-only filters. | Keep full-filter bypass out of normal runtime, restrict HTTP boundary code from bypass helpers, and move toward reason-coded/audited system scopes. | 1C | `ApiControllers_ShouldNotCall_TenantFilterBypassHelpersDirectly`; existing raw `IgnoreQueryFilters()` architecture guard; 2026-06-14 `Event.Architecture.Tests` passed 191 total, 190 succeeded, 1 skipped; semantic bypass tests remain. | None unless exposed behavior changes. | `docs/MULTI_TENANCY.md`, `docs/OPERATIONS.md`, active plan/context. | Partially mitigated |
| R-003 | Auth | Authorization Lead | Authorization | Privileged-sounding policies required only authentication and could grant too much access if used. | 2026-06-14 removed `template_admin` and mapped template sync/history to `CustomPropertyTemplate` request metadata plus HAL permissions. The unused auth-only `event_editor`, `property_governance_admin`, and `platform_namespace_editor` registrations were also removed after repo-wide C# search found no active `[Authorize(Policy = ...)]` usages. 2026-07-04 custom-property projection admin reconciliation confirmed the active route family uses `ResourceKinds.CustomPropertyProjection` with `view/update` actions; requests now carry tenant/event/session authorization context, `AuthorizationBehavior` enriches event/session routes to tenant context, and Cerbos/local fallback deny missing or mismatched tenant context. | Add real capability/resource/action policies only when endpoint inventory identifies active gaps, with handler metadata, Cerbos/fallback/HAL parity, and tests. Do not revive role-sounding projection/governance policies when a dedicated resource/action family already exists. | 1D | Application authorization metadata tests; API convention tests; AuthorizationParityTests; `ProjectionQueryAuthorizationMetadataTests`; `ProjectionCommandAuthorizationMetadataTests`; `AuthorizationBehaviorTests`; `FallbackAuthorizationServiceTests`; custom-property projection Cerbos policy tests. | 401/403 behavior changes only when real endpoint policies are introduced. Projection admin tenant-context hardening is an authorization decision change for callers lacking matching tenant context. | `docs/SECURITY-MODEL.md`, `docs/AUTHORIZATION.md`, changelog, active plan/context/tasks. | Partially mitigated |
| R-004 | Auth/Ops | Platform Operations Lead | Bootstrap/admin | First-admin/setup-secret behavior was ambiguous during policy hardening and could blur bootstrap credential authority with post-bootstrap admin authority. | 2026-06-14 contract states `complete` requires authenticated identity plus setup secret; setup secret alone is not an identity; SingleTenant grants default tenant admin and branding defaults; MultiTenant grants platform admin only. 2026-07-04 mitigations: all setup-secret-gated `InstanceOnboarding` actions use the named `SetupSecret` rate limiter, advertise typed RFC 7807 `403`/`410`/`429` responses in OpenAPI, and the setup-secret filter returns central `ProblemDetails` instead of ad hoc JSON for invalid secrets and completed setup mode. Structured bootstrap audit emission exists at setup-secret accepted/rejected/inactive checks, Keycloak bootstrap validation/start/failure/success, and setup-mode disablement through `IInstanceBootstrapAuditLogger`/`InstanceBootstrapAuditLogger`; it logs bounded metadata and intentionally excludes submitted secrets, runtime secrets, admin passwords, tokens, raw provider payloads, and provider endpoint URLs. Keycloak bootstrap provider-failure mapping keeps operator input/configuration failures as validation ProblemDetails, maps timeout/unreachable failures to `503 Service Unavailable`, and maps invalid/upstream Keycloak Admin API failures to `502 Bad Gateway`. Official Keycloak docs and Tavily keycloak.org research confirm rotation/sync should use explicit Admin API actions without destructive realm reimport/delete workflows. Current tests prove sync apply blocks without backup confirmation and uses only additive GET/POST/PUT Admin API calls, while client-secret rotation persists the replacement runtime secret and reloads auth schemes only after Keycloak reports `rotated`; deployment-managed secrets return operator instructions without contacting Keycloak. | Mitigated for the verified setup/bootstrap/admin safety surface. Do not add a vague "missing provider" task unless a distinct post-activation missing-auth-provider scenario is verified from source. | 1E | SingleTenant/MultiTenant role grant unit tests; `SetupSecretRateLimitMetadataTests` 3/3; `SetupSecretRequiredFilterTests` 6/6; `SetupSecretFlowTests` 10/10; `InstanceOnboardingControllerTests` 24/25 succeeded with 1 existing skip after adding provider 502/503 coverage; full `Event.Application.UnitTests` 1905/1905 from the audit slice; R-004 setup filter/flow audit additions passed; `RotateKeycloakClientSecretCommandHandlerTests` focused lane 5/5; `KeycloakBootstrapServiceTests` focused lane 18/18; OpenAPI inventory generation 1/1; NSwag client generation passed; architecture tests 240 total / 239 succeeded / 1 intentional skip; full Release build 25 projects / 0 errors; full API integration suite has 6 unrelated `StorageObjectHateoasTests` failures. | Generated OpenAPI/client metadata includes `SetupSecret` rate-limit policy, typed setup failure ProblemDetails, and Keycloak bootstrap `502`/`503` provider ProblemDetails. Generated clients deserialize setup 403/410/429 and bootstrap 502/503 as `ProblemDetails`. No change to bootstrap authority model; audit emission is structured log output, not a public API contract change. Keycloak maintenance contracts are safe DTOs: sync plan/operation results do not echo temporary credentials/tokens, and rotation results do not return new client secrets. | `docs/API.md`, `docs/OPERATIONS.md`, `docs/API_CHANGELOG.md`, active plan/context/tasks. | Mitigated |
| R-005 | Backend/API | API Contract Lead | Error contract | Controllers/middleware must not use ad hoc strings, raw helper results, or undocumented ProblemDetails shapes. | 2026-06-14 through 2026-06-15 established `ApiProblemCodes`, `ApiProblemFactory`, `CommandResponseResultMapper`, central `code` extension emission, route/response metadata sweeps, and many controller migrations. 2026-07-04 Phase 2.1 re-audit used Context7 ASP.NET Core ProblemDetails docs, Tavily OWASP error-handling research, CodeGraph, and `rg` source sweeps. The only remaining direct controller helper was `EventSessionLanguageController.Update` returning `ValidationProblem(ModelState)` for missing/invalid `If-Match`; that branch now uses `ToValidationProblem` with an `If-Match` descriptor. Current `rg` sweeps find no direct controller calls to raw 4xx/helper returns or 4xx `BaseCommandResponse` metadata under `Explore.API/Controllers`. 2026-07-04 Phase 2.2 added middleware/runtime behavior coverage: authorization challenge/forbid responses now return central RFC 7807 bodies with `authentication_required`/`forbidden`, rate-limit rejections now emit `code=rate_limited`, and `ProblemDetailsContractTests` covers representative 400, 401, 403, 404, 409, 429, and 500 shapes. | Treat direct controller migration and representative behavior coverage as complete for the audited patterns; reopen only for a concrete new source-backed gap or a new ProblemDetails family. | 2 | `dotnet build Explore.API/Explore.API.csproj --configuration Release --verbosity quiet --no-restore -maxcpucount:1`; `EventSessionLanguageControllerTests` 2/2; `ProblemDetailsContractTests` 20/20; `Event.Architecture.Tests` 240 total / 239 succeeded / 1 intentional skip; `rg` sweeps for direct helpers, `ModelState.AddModelError`, and 4xx `BaseCommandResponse` metadata. | Endpoint-owned validation now preserves `code=validation_failed` and field errors; authorization middleware now returns stable 401/403 ProblemDetails bodies instead of status-only responses; rate-limit middleware now returns `code=rate_limited` plus retry/rate-limit headers. No OpenAPI/client regeneration required because typed `ProblemDetails` metadata was already advertised and schema shape remains the shared RFC 7807 contract. | `api-error-catalog.md`, active plan/context/tasks; future docs/API changelog updates only when public error semantics change beyond the shared ProblemDetails envelope. | Mitigated |
| R-006 | Contract/OpenAPI | API Contract Lead | Route names | Route-name catalog drift can break HATEOAS link generation. | 2026-06-15 R-006 cleanup pruned/retargeted stale HAL affordances and obsolete `RouteNames` constants; final read-only classification reported 452/452 constants backed by exactly one registered endpoint with no missing or duplicate route names. 2026-07-04 revalidation confirmed both active guardrails still pass on the current dirty branch: endpoint-to-constant and constant-to-endpoint coverage. | Keep `RouteNameCoverageTests` active and regenerate OpenAPI/client artifacts when route templates change. | 2 | `RouteNameCoverageTests` passed 3/3 after `Event.API.IntegrationTests` Release build passed with 0 errors. | Future route identity changes require changelog/OpenAPI/client workflow when externally visible. | `docs/API.md`, OpenAPI/client regen. | Mitigated |
| R-007 | Backend/API | API Architecture Lead | Controller decomposition | Splitting fat controllers can break HAL links/routes if done too early. | 2026-07-04 first slice: event Islamic/tech aspect subresources moved from `EventController` into `EventAspectController`; `/api/event/{id}/aspects/islamic`, `/api/event/{id}/aspects/tech`, all six `RouteNames`, auth/classification metadata, response metadata, read `DetailData` output-cache metadata, and `Event` OpenAPI tag grouping are preserved by focused tests. `EventController` still combines other event CRUD/lifecycle/read concerns, so the overall fat-controller risk is only reduced. | Continue decomposing only one source-selected resource family at a time behind route/HAL/OpenAPI/auth metadata characterization tests; do not combine decomposition with behavior or contract redesign unless the change is explicitly documented and regenerated. | 3 | `EventAspectControllerTests` 5/5; `RouteNameCoverageTests` 3/3 after split; endpoint-classification architecture tests 3/3. | No OpenAPI/client regeneration required for the event-aspect split because route templates, operation IDs, DTO schemas, response metadata, auth metadata, and cache metadata stayed stable. Future route/metadata/schema changes require generated artifact regeneration and changelog. | Active workstream docs; product API docs/changelog only when external contract changes. | Partially mitigated |
| R-008 | Application | Application Lead | Event creation | Create flow is too large and duplicate-submit/concurrency semantics are unclear. | `CreateEventCommandHandler` large dependency/responsibility surface. | Narrow collaborators, transaction boundary, idempotency, audit, cache invalidation. | 4 | Application unit tests; idempotency/concurrency tests. | Command response/problem codes may change. | `docs/API.md`, changelog. | Open |
| R-009 | Persistence/Contract | API Contract Lead | Pagination | Offset pagination on high-volume feeds is unstable/expensive. | Event/session/list repositories use `Skip/Take`. | Opaque versioned cursor contract and supporting indexes for high-volume feeds. | 5D/5E | API + persistence pagination tests. | Response shape changes. | `docs/API.md`, OpenAPI/client regen, changelog. | Open |
| R-010 | Persistence | Persistence Lead | Persistence cancellation | EF async calls often lack cancellation token propagation. | 2026-07-04 first slice: organization repository contracts now accept optional `CancellationToken` parameters; `OrganizationRepository` forwards them to `FirstOrDefaultAsync`, `CountAsync`, `ToListAsync`, and `ExecuteDeleteAsync`; organization and public-experience query handlers pass MediatR tokens to organization repository calls. Second slice: `IInstanceBootstrapStateRepository.GetCurrent` and `InstanceBootstrapStateRepository.GetCurrent` now accept a `CancellationToken`, forward it to `FirstOrDefaultAsync`, and five onboarding handlers pass their MediatR tokens to the bootstrap-state lookup. Third slice: `IEventSessionSpeakerRepository` and `EventSessionSpeakerRepository` now accept cancellation tokens for session/actor/list/duplicate-check reads, forward them into `ToListAsync`, `CountAsync`, and `FirstOrDefaultAsync`, and EventSessionSpeaker query/update handlers pass MediatR tokens through. Other repository families still have no-token EF async calls, so the broader risk remains open. | Continue repository-family-by-repository-family cancellation propagation from source evidence; avoid repo-wide signature churn without focused tests. | 5A | Organization handler unit tests; `OrganizationRepositoryCancellationTests`; onboarding handler unit tests; `InstanceBootstrapStateRepositoryCancellationTests`; EventSessionSpeaker handler cancellation tests; `EventSessionSpeakerRepositoryCancellationTests`; architecture tests. | No public API contract impact; persistence/application internal contract only. | Active plan/context/tasks; `docs/ARCHITECTURE.md` only if project-wide convention text changes. | Partially mitigated |
| R-011 | Operations/Security | Security/Operations Lead | Auditability | Security-sensitive actions may lack consistent audit events. | Tenant bypass, presigned URL, lifecycle, governance, module/template actions. | Audit taxonomy and consistent event emission. | 1C/4/6 | Audit coverage tests. | No response shape impact unless surfaced IDs added. | `docs/OPERATIONS.md`, `docs/SECURITY-MODEL.md`. | Open |
| R-012 | Auth/API | Authorization Lead | Public data exposure | User/registration/storage endpoints may expose user-specific or sensitive download data as public. | 2026-07-04 mitigation: R-016 already moved generic event-registration reads to authenticated self-service; this slice moved storage metadata/list/detail, content, and presigned URL routes to `[Authorize]` plus MediatR `islamuevent_storage_object` resource actions. Metadata uses `view`, content uses `download`, presigned URLs use `presigned_download`, Cerbos/local fallback deny regular metadata view, HAL protected links carry permission metadata, and the presigned route is no-store/no-output-cache. `GET /api/storageobject/{id}/public` remains the explicit anonymous route and is limited to active `public_image` content by the reader. | Keep only explicitly public aggregate/object projections anonymous; do not reintroduce arbitrary-key or cached presigned URL routes. | 0A/1D/2 | `StorageObjectControllerTests`; `StorageUploadSessionControllerTests`; `StorageAdminHateoasTests`; `StorageObjectQueryAuthorizationMetadataTests`; `FallbackAuthorizationServiceTests`; `CerbosPolicyContractTests`; storage Cerbos YAML tests. | Breaking 401/403/cache behavior change for storage metadata/content/presigned reads; no DTO minimization in this slice because metadata is now administrative/resource-protected. | `docs/API.md`, `docs/SECURITY-MODEL.md`, changelog. | Mitigated |
| R-013 | Auth | Authorization Lead | Fallback authorization | Local fallback provider has broad view allowances and instance-admin bypass semantics that may not match policy matrix. | 2026-07-04 bounded fallback parity slice fixed concrete tenant-context drift: generic tenant-scoped single decisions now deny invalid or explicit cross-tenant `tenantId` attributes instead of checking the caller as tenant admin in the supplied tenant while the request is executing under another ambient tenant. The optimized batch path now mirrors that tenant check for tenant resources, tenant role grants, taxonomy/location, custom-property governance, EmailDispatch, and webhook resources; requires explicit matching tenant context for custom-property projection and support-access session read evidence; and no longer allows storage metadata `view` broadly in batch while preserving active public/authenticated/private-owner content download semantics. | Continue aligning fallback provider with the authorization-policy matrix and Cerbos semantics by resource family; remaining broad authenticated view/create cases must be audited with source-specific intent before changing behavior. | 1D/6 | `FallbackAuthorizationServiceTests` 88/88 for single and optimized-batch parity; future AuthorizationParity/API integration tests for each remaining resource family. | 401/403/HAL link changes only where fallback is active and callers relied on cross-tenant attributes or broad batch allowances. | `docs/AUTHORIZATION.md`, `docs/SECURITY-MODEL.md`, active plan/context/tasks. | Partially mitigated |
| R-014 | Auth/Multi-tenancy | Multi-Tenancy Lead | API-key tenant execution | Instance-admin API keys may continue without bound tenant context. | 2026-07-04 mitigation: `ApiTenantPostAuthenticationMiddleware` now treats `InstanceAdmin` as a nullable platform credential, not a blanket tenantless execution mode. Tenant-scoped API paths bind an explicit requested tenant hint when present; tenant-scoped API/MCP paths without resolved tenant fail closed with `404` and `code=tenant_required`; only explicit host-administration API routes may continue without tenant context. | Keep the host-administration allowlist narrow and source-reviewed; any new tenantless API-key execution path must be explicit, documented, logged, and tested. | 1A/1C | `ExternalApiOwnerTypeIntegrationTests` 14/14; API/test project Release builds. | Breaking behavior for multi-tenant InstanceAdmin API-key calls to tenant-scoped API/MCP paths without a tenant hint: now `404 tenant_required`; explicit tenant hints bind the request tenant. No OpenAPI schema/client regeneration required because route shapes and DTO schemas did not change. | `docs/API.md`, `docs/SECURITY-MODEL.md`, `docs/MULTI_TENANCY.md`, `docs/ADMIN_HIERARCHY.md`, active plan/context/tasks. | Mitigated |
| R-015 | Operations/Auth | Platform Operations Lead | Admin migration endpoint | Runtime database migration endpoint was protected only by authorization metadata and was mapped in Development/Testing outside the build-time public contract. | 2026-07-04 fix: removed the `POST /admin/migrate` Minimal API mapping from `Explore.API/Program.cs` and removed the orphan `RouteNames.ApplyDatabaseMigrations` constant. Startup migrations remain the supported migration path outside `Testing`, and the endpoint remains absent from `docs/API_CONTRACT_INVENTORY.md`. `AdminMigrationEndpointRetirementTests.AdminMigrate_Post_IsNotMappedInTestingHost` proves the retired path returns `404` in the Testing host. | Keep runtime database migration execution in startup/migration-service paths. Do not reintroduce HTTP migration execution without explicit host-admin capability, audit, redacted errors, environment constraints, and a documented local-dev/operator use case. | 1D/1E/6 | `AdminMigrationEndpointRetirementTests`; route-name coverage; architecture tests. | No public API schema/client impact; the runtime Development/Testing utility path is removed. | Active plan/context/tasks; no product-doc change required because supported migration paths were already documented. | Mitigated |
| R-016 | Auth/API | Authorization Lead | Event registration PII | Generic registration reads could expose user identity and another user's event/session registration state. | 2026-07-04 mitigation: `GET /api/eventregistration`, `GET /api/eventregistration/by-session/{eventSessionId}`, and `GET /api/eventregistration/{id}` are current-user scoped in Application query handlers; `GET /api/eventregistration/by-user/{userId}` rejects mismatched route/current-user IDs at the API boundary and again in the handler; read DTO schemas no longer expose `userId`, `userFullName`, or `userEmail`; generic registration repositories no longer include `User.Pii`; MCP registration context no longer returns registrant names. | Keep generic registration reads as self-service only. Add a separate event/session organizer projection later if product needs attendee management, guarded by event/session resource authorization and HAL affordances. | 1P | `EventRegistrationControllerTests`; `EventRegistrationSelfReadQueryHandlerTests`; `EventRegistrationReadDtoPrivacyTests`; Blazor registration service/component tests; OpenAPI/client regeneration. | Breaking read-schema and generated-client change: removed identity fields from `EventRegistrationDto`/`EventRegistrationListDto`; `by-session` now returns only the current user's registration for the session; `by-user` returns `403` for other user IDs. | `docs/API.md`, changelog, active plan/context/tasks. | Mitigated |
| R-017 | Auth/API | Authorization Lead | Tenant role grant exposure | Tenant role grant reads expose user email/name, role, tenant, grant, and revocation metadata and must not be available to arbitrary authenticated users or anonymous callers. | 2026-07-04 mitigation: `TenantUserRoleGrantController` read actions are authenticated and now dispatch `ISecureRequest` queries carrying the resolved tenant id; both read query types use `[AuthorizeResource(ResourceKinds.TenantUserRoleGrant, AuthorizationActions.TenantUserRoleGrants.View)]`; the broad authenticated Cerbos `view` rule was removed; local fallback only allows tenant admins for the resolved tenant plus instance admins; generated inventory shows the read endpoints as `Authenticated`. | Keep tenant role grant DTOs identity-bearing but admin/resource protected. Add a separate safe public projection only if product requirements later justify one. | 1P | `TenantControllerTests`; `TenantUserRoleGrantAuthorizationTests`; `GetTenantUserRoleGrantListRequestHandlerTests`; `FallbackAuthorizationServiceTests`; `CerbosPolicyContractTests`; generated inventory/client workflow. | 401/403 metadata change on read routes; no DTO schema minimization in this slice because the endpoint is deliberately administrative. | `docs/AUTHORIZATION.md`, `docs/API.md`, changelog, active plan/context/tasks. | Mitigated |
| R-018 | Auth/API | Authorization Lead | Organization member exposure | Organization member reads expose user email/name/role/position and must not be available to arbitrary authenticated users or anonymous callers. | 2026-07-04 mitigation: `OrganizationMemberController` read actions are authenticated and now dispatch `ISecureRequest` queries carrying the resolved tenant id plus organization/member context; both read query types use `[AuthorizeResource(ResourceKinds.OrganizationMember, AuthorizationActions.OrganizationMembers.View)]`; direct member-id reads are enriched with tenant, organization, and user attributes before provider evaluation; the broad authenticated Cerbos `view` rule was removed; local fallback only allows tenant admins for the resolved tenant and organization admins for the target organization; generated inventory shows the read endpoints as `Authenticated`. | Keep organization member DTOs identity-bearing but admin/resource protected. Add a separate safe public organization profile/member projection only if product requirements later justify one. | 1P | `OrganizationMemberAuthorizationTests`; `OrganizationMemberAuthorizationTests` Application request/descriptor tests; `OrganizationMemberHateoasTests`; `AuthorizationBehaviorTests`; `FallbackAuthorizationServiceTests`; `CerbosPolicyContractTests`; generated inventory/client workflow. | 401/403 metadata change on read routes; additive `tenantId` field on `OrganizationMemberDto`; no DTO schema minimization in this slice because the endpoint is deliberately administrative. | `docs/AUTHORIZATION.md`, `docs/API.md`, `docs/SECURITY-MODEL.md`, changelog, active plan/context/tasks. | Mitigated |
| R-019 | Auth/API | Authorization Lead | Footer write authorization | Footer management writes previously risked relying on authentication-only access or incomplete tenant resource metadata. | 2026-07-04 mitigation: `FooterController` fails closed on missing actor and now sends the resolved tenant id into every footer link-group/link/reorder/settings write command; footer commands expose `ISecureRequest` resource id/attributes for `ResourceKinds.Tenant` + `AuthorizationActions.Update`; local fallback allows tenant admins only for the resolved tenant and denies regular authenticated users; focused API/Application/Infrastructure tests prove 403 and metadata behavior. | Keep footer writes under the tenant-update convention unless a future dedicated `Footer.Manage` family is added with resource/action, Cerbos, fallback, HAL, and tests atomically. | 1P/1D | `FooterAuthorizationTests`; `FooterCommandAuthorizationMetadataTests`; `FallbackAuthorizationServiceTests`; broader auth parity remains open for other families. | 403 behavior hardening on footer writes; no route/schema/client change. Future HAL rels must use the same tenant update decision. | `docs/AUTHORIZATION.md`, `docs/API.md`, `docs/API_CHANGELOG.md`, active plan/context/tasks. | Mitigated |
| R-020 | Blazor/API Contract | Presentation Lead | HAL affordance bypass | Blazor shows action buttons from local roles/empty-list fallback instead of HAL `_links`. | 2026-07-04 mitigation: `EventTemplateListPage` no longer infers create from the first row and instead reads collection `_links.create`; event-template HAL mapping preserves collection and row links in typed client models; the API event-template collection policy now emits item `edit`/`delete` rels with tenant-resource authorization metadata. `MyOrganizations` was inspected and already gates create-event from row HAL links. | Keep per-resource and collection actions gated by HAL `_links`; do not add client-side role/claim or first-row fallback gates for mutation affordances. | 1P/6 | `TemplateSyncHateoasTests`; `EventTemplateHalResourceExtensionsTests`; `EventTemplateListPageTests`; `Event.Application.UnitTests`; `Event.API.IntegrationTests`; `Explore.Blazor.Client.Tests`; `Event.Architecture.Tests`. | UI behavior hardening; no route or schema/client regeneration required because HAL metadata and client mapping changed without public DTO shape changes. | `docs/API.md`, active plan/context/tasks. | Mitigated |
| R-021 | Domain/Persistence | Architecture Lead | Domain mapping annotations | Domain carries persistence attributes and DataAnnotations imports. | 80 matches across 25 Domain files; `Event` has many `[ForeignKey]`. | Move mapping into Persistence EF configuration; add architecture test. | 6 | Architecture tests; persistence model tests. | No API impact intended. | `docs/ARCHITECTURE.md`, `docs/GOVERNANCE.md`. | Open |
| R-022 | Application | Application Lead | Query response envelopes | Queries return `BaseCommandResponse<T>` and use command `Id` slot for data. | Email dispatch status, event aggregate view, custom-property projection query findings. | Queries return DTO/list/page/null results; API maps not-found/errors separately. | 4 | Application unit tests; CQRS architecture tests. | Response schemas may change. | `docs/API.md`, changelog, OpenAPI/client regen. | Open |
| R-023 | Application/Persistence | Architecture Lead | Repository DTO input coupling | Repository contract accepted an Application DTO filter. | 2026-07-04 mitigation: `IEventAggregateViewRepository.GetPagedAsync` now accepts `EventAggregateViewFilter`, a persistence-neutral Application contract under `Contracts/Persistence`, instead of `AggregateViewFilterDto`. `GetEventListAggregateViewQueryHandler` maps the external query DTO to that repository filter before calling Persistence, and `EventAggregateViewRepository` no longer imports `Explore.Application.DTOs.EventAggregateView`. A new Clean Architecture guard blocks future Persistence dependencies on `Explore.Application.DTOs`. | Keep repository inputs as entity/read-model query contracts or specifications, never API/Application DTO mapping types. Use handler-level mapping when a public query DTO differs from repository filter shape. | 5B | `GetEventListAggregateViewQueryHandlerTests` 5/5; `Event.Architecture.Tests` 258 total / 257 succeeded / 1 existing documented skip; `Explore.Persistence` Release build passed; `Event.Persistence.IntegrationTests` Release build passed. | No external API impact; route, DTO schema, OpenAPI, and generated client shape are unchanged because the public query DTO remains intact. | Active context/tasks updated; product docs not needed because this is an internal Clean Architecture boundary fix. | Mitigated |
| R-024 | Persistence | Persistence Lead | Tracked generic reads | Generic read helpers track entities by default and `Exists` materializes via `FindAsync`. | 2026-07-04 first slice: `GenericRepository.Exists` no longer calls `GetById`/`FindAsync`; it builds a primary-key predicate from EF metadata and executes `AsNoTracking().AnyAsync(...)`. `GenericRepositoryTests.Exists_WhenEntityExists_ReturnsTrueWithoutTrackingEntity` proves the existence check returns true without leaving a tracked `Tenant` entry. `GetById` intentionally remains tracked until read/mutation contracts are split, because mutation paths still rely on tracked aggregate loads. | Continue separating generic read-only and mutation-loading contracts before changing `GetById`; do not globally convert tracked aggregate loads without caller-specific tests. | 5C | `GenericRepositoryTests` 1/1; `Event.Architecture.Tests` 257 total / 256 succeeded / 1 existing skip; `Explore.Persistence` Release build passed. | No external API impact; persistence internal behavior only. | Active plan/context/tasks; `docs/ARCHITECTURE.md` only if project-wide repository convention text changes. | Partially mitigated |
| R-025 | Persistence/Security | Security Lead | Broad hard delete | `HardDelete` was globally exposed on the shared generic repository surface with only a comment-level admin warning. | 2026-07-04 mitigation: `IGenericRepository<,>` and `GenericRepository<,>` no longer expose `HardDelete`. Source recheck showed current production callers target non-`ISoftDeletable` entities, so the handlers now call the existing soft-delete-aware `Delete` path without changing lifecycle semantics. Repository fakes were updated, and `CleanArchitectureTests.GenericRepository_ShouldNotExpose_IrreversibleDeleteMethod` guards the generic contract/implementation against reintroducing the method. | Keep irreversible or lifecycle-specific deletion behind explicit domain/application flows with authorization, audit, and tests; do not add blanket hard-delete helpers to shared repositories. | 5B/6 | `rg` found no remaining `HardDelete(` C# call sites under Application/Persistence/API/tests/Infrastructure; `DeleteUserCommandHandlerTests` 4/4; `Event.Architecture.Tests` 259 total / 258 succeeded / 1 existing documented skip. | No external API impact; no route, DTO schema, OpenAPI, generated-client, migration, or config change. | Active context/tasks updated; product docs not needed because this is an internal repository lifecycle-contract hardening. | Mitigated |
| R-026 | Application/Persistence | Performance Lead | Tenant settings N+1 | Tenant policy settings read path performs dozens of sequential per-key lookups. | 2026-07-04 mitigation: `TenantPolicySettingService.ReadEffectiveTenantSettingsAsync` now loads system settings once through `GetAllSettings()` and tenant overrides once through `GetAllForTenant(tenantId)`, then resolves effective values from in-memory key dictionaries. The read path no longer calls `GetByKey` or `GetByTenantAndKey` per setting. `TenantSettingRepository.GetAllForTenant` remains the no-tracking, exact-tenant batch read path, and `TenantSettingRepositoryBypassTests.GetAllForTenant_WithAmbientTenant_ReturnsExplicitTenantRowsWithoutTracking` proves it returns only the requested tenant and leaves no tracked `TenantSetting` entries. | Keep tenant-policy effective reads on batch settings APIs; use per-key tenant setting reads only for mutation/update paths that need tracked entities. | 5C | `TenantPolicySettingServiceTests` 4/4; `TenantSettingRepositoryBypassTests` 2/2; Application/Persistence builds. | No external API contract impact; effective values are unchanged. | Active plan/context/tasks; product docs only if operator-visible behavior changes. | Mitigated |
| R-027 | Application | Application Lead | Duplicated template pipelines | Event/session template diff/sync/instantiation and projection filters duplicate algorithms. | AFT duplicate findings and manual reads. | Consolidate shared algorithm or defer with owner/rationale. | 4/6 | Application unit regression tests. | No API impact intended. | `docs/ARCHITECTURE.md` if pattern added. | Open |
| R-028 | Application | Application Lead | AI handler bloat | AI command handlers mix validation/auth/settings/provider/idempotency/quota/mutation concerns. | `SendAiMessageCommandHandler`, `ProcessAiRunCommandHandler`. | Decompose into narrow collaborators preserving MediatR boundaries. | 4 | Application unit tests; API integration for behavior. | Error/response contracts may change via Phase 2. | `docs/API.md`, changelog if changed. | Open |
| R-029 | Hygiene/API | API Architecture Lead | Production TODO/stubs | User-facing TODOs/stubs and mock data remain in production paths. | 2026-07-04 source recheck found `Explore.Blazor.Client/Services/EventSessionSpeakerService.cs` still returns empty/null/false, `IEventSessionSpeakerService` exposes `object` payloads, and `ManageSpeakersDialog.razor` discards loaded results while showing remove success without checking the service result. Application/Persistence already have EventSessionSpeaker CQRS handlers/repository pieces. The Application prerequisite is complete: create/delete commands authorize the owning event session and carry tenant/event resource attributes; create validates same-tenant session/actor ownership, duplicate assignment, tenant stamping, and cache invalidation; delete refuses mismatched owning-session/tenant requests before deletion and cache invalidation; update rejects route/session mismatch and carries tenant/event metadata. The API/HAL management contract is also complete: authenticated nested routes under `/api/eventsessionspeaker/management/by-session/{eventSessionId}`, stable `RouteNames`, typed response and ProblemDetails metadata, EventSession `speakers` HAL affordance, EventSessionSpeaker edit/delete HAL policy, generated OpenAPI/inventory rows, and generated `EventApiClient` methods. Remaining source evidence: the Blazor service/dialog still need typed implementation and HAL-gated UI behavior. Other listed stubs still require separate source rechecks. | Do not hand-roll Blazor HTTP calls or guess routes. Replace the Blazor stub with typed generated-client service methods and HAL-gated dialog behavior, using the new callable generated methods. If speaker display must be public, add a separate minimized public projection instead of exposing the management assignment DTO anonymously. Track each remaining stub as its own source-backed sub-risk. | 6 / Blazor follow-up slice | Completed Application/API/HAL verification: Application and API test projects build; focused EventSessionSpeaker API tests passed 3/3; route-name coverage passed 3/3; Application unit suite passed 1997/1997 under the EventSessionSpeaker/AuthorizationBehavior lane; full `Event.Architecture.Tests` passed 259 total / 258 succeeded / 1 existing documented skip; API inventory generation passed 1/1; generated Blazor client regeneration passed. Remaining validation: Blazor service/dialog tests proving generated client use, HAL affordance gating, loaded-speaker rendering, and failed mutation handling. | New generated API operations and Blazor client methods are present in `schemas/openapi.json`, `docs/API_CONTRACT_INVENTORY.md`, and `EventApiClient.g.cs`. If a public minimized speaker projection is added, document it separately from the management relationship contract. | Active workstream docs, `docs/API.md`, `docs/API_CHANGELOG.md`, and Blazor docs if UI behavior changes. | In Progress |
| R-030 | Reliability | Presentation Lead | Empty catch | Component cleanup swallows exceptions silently. | 2026-07-04 mitigation: `CreateOrganization.ClearLogoUploadState` now catches `Exception ex` from the optional `ImageUpload.RemoveImage()` cleanup call and writes a bounded `ILogger<CreateOrganization>.LogWarning` message before continuing the existing state reset. `rg` confirms the empty `catch { }` is gone from the component. | Keep cleanup best-effort for the user flow, but do not silently swallow component cleanup failures. Add a focused component test only if this cleanup path gains user-visible behavior. | 6 | `Explore.Blazor.Client.Tests` 1485 total / 1484 succeeded / 1 existing documented skip; `Event.Architecture.Tests` 257 total / 256 succeeded / 1 existing documented skip; Blazor client test project Release build passed. | No API impact. | Active context/tasks updated; no product docs needed because behavior is internal logging only. | Mitigated |
| R-031 | Auth/Multi-tenancy | Multi-Tenancy Lead | Semantic tenant bypass proof | Reason-string bypass tests do not prove restrictive predicates after disabling tenant filter. | 2026-06-14 added first semantic proof for `TenantSettingsDocumentRepository.GetByTenantAndDocumentKey`: ambient tenant B plus explicit tenant A must return tenant A's same-key document. Controller/raw bypass usage is guarded; each remaining bypass call site still needs this proof. Verification of the new persistence test is blocked by Docker/Testcontainers unavailability at `unix:///home/amir/.docker/desktop/docker.sock`. | Add semantic tests/reviews for each bypass call site and introduce audited system-scope API if Phase 1C formalizes execution modes. | 1C/6 | Persistence/API integration tests; repository-specific bypass predicate tests; rerun `Event.Persistence.IntegrationTests` when Docker is available. | Potential 403/404/no-row changes. | `docs/MULTI_TENANCY.md`, active context/tasks. | In progress |
| R-032 | Contract/OpenAPI | API Contract Lead | Runtime/build-time inventory drift | Generated Markdown inventory and checked-in OpenAPI schema disagreed, so row-level endpoint planning could follow the wrong source. | 2026-07-03 fix: `ApiContractInventoryGeneratorTests` now reads `schemas/openapi.json` instead of the Testing runtime `/openapi/event-api.json`. Regenerated `docs/API_CONTRACT_INVENTORY.md` now has `365` paths/`506` operations/`0` missing endpoint classes, includes webhook message/delivery-attempt operations, excludes Testing-only `/admin/migrate`, and records route/tenant/rate/cache posture columns. | Keep `schemas/openapi.json` as the committed contract source for inventory/client workflows; use runtime OpenAPI tests for parity/invariant assertions only. Never hand-edit generated artifacts. | 0/2 | `ApiContractInventoryGeneratorTests`; `OpenApiParityTests`; `ContractInvariantsTests`; route-name coverage; `dotnet build Explore.API/Explore.API.csproj --configuration Release --verbosity minimal --no-restore -maxcpucount:1` when regenerating schema. | Generated inventory changed; public API schema/client did not change in this slice. | Active inventory/context/tasks. | Mitigated |
| R-033 | API/Auth/Webhooks | Integration Lead | Webhook management and audit surface | Webhook consumer/endpoint/message/delivery endpoints carry secrets, provider state, delivery metadata, and retry actions that must stay tenant-scoped and least-privilege. | 2026-07-04 mitigation: `Event.API.IntegrationTests/Features/WebhooksControllerTests.cs` now covers message/delivery route metadata, `webhook:view-delivery`/`webhook:retry` resource-action metadata, no output caching on audit reads, tenant-scoped MediatR mapping, NotFound/Conflict ProblemDetails, safe DTO shape, and HAL retry affordance gating. `Event.Application.UnitTests/Features/Webhooks/WebhookConsumerHandlersTests.cs` covers query/command auth metadata, handler limit/filter normalization, safe DTO mapping without raw payload/body properties, empty-id fail-closed behavior, and retry outcome mapping. `Event.Persistence.IntegrationTests/Repositories/WebhookPersistenceTests.cs` proves explicit tenant predicates for message and delivery-attempt audit repository reads. | Keep webhook audit/retry changes behind the same CQRS/MediatR and HAL policies; regenerate OpenAPI/client artifacts only after an intentional source contract change. | 0/2 | Focused R-033 tests: Webhooks controller API integration; WebhookConsumerHandlers application unit tests; WebhookPersistence PostgreSQL tenant-predicate test; generated OpenAPI inventory parity remains covered by R-032. | No public API shape changed in the mitigation slice. Future webhook route/client changes still require regeneration and changelog review. | Active plan/context/tasks updated. | Mitigated |
| R-034 | API/Security/Webhooks | Integration Security Lead | Anonymous incoming webhook ingestion | `IncomingWebhooksController.RecordSvixOperationalCallback` is intentionally anonymous, so signature verification is the authentication boundary. | 2026-07-04 mitigation: `Event.API.IntegrationTests/Features/IncomingWebhookFrameworkTests.cs` now covers the documented public-ingestion route metadata, `[AllowAnonymous]`, `EndpointClass.Public`, dedicated `PublicIngestion` rate-limiter, safe verification-failure ProblemDetails, missing/invalid Svix signature failures, explicit body-size rejection before verifier dispatch, duplicate replay acknowledgement without reprocessing, redacted persisted headers, and duplicate log redaction. Source hardening makes `IncomingWebhookIntakeService` enforce byte limits independent of stream implementation, removes tenant/provider-message IDs from duplicate logs, and stops tagging Svix operational callback metrics with the resolved tenant ID. | Keep Svix operational callbacks classified as `PublicIngestion`: raw-body signature verification is the auth boundary; verified tenant-addressed callbacks may enter the idempotency ledger; duplicates are acknowledged without side effects; response/log/metric output remains bounded and redacted. | 1/2 | `IncomingWebhookFrameworkTests` focused R-034 lane; `Event.API.IntegrationTests` and architecture tests remain the owning projects for future route/metadata changes. | Route template and response body shape did not change. The OpenAPI governance extension changed from `Write` to `PublicIngestion`, and generated schema/inventory/client artifacts were regenerated through the documented workflow. | `docs/API.md`, `docs/SECURITY-MODEL.md`, `docs/OPERATIONS.md`, active plan/context/tasks. | Mitigated |
| R-035 | API/Auth/Privacy | Moderation Lead | Event report and moderation privacy | Event-report intake, current-user report reads, and moderation queue/actions can expose reporter evidence, user identity, event moderation state, and provider workflow metadata if resource auth or DTO shaping drifts. | 2026-07-04 mitigation: `EventReportsController` keeps public options as the only anonymous report route and requires authentication for submission/current-user report reads. `GetMyReportsRequestHandler` and `GetMyReportRequestHandler` scope reporter reads by tenant plus `ICurrentUserService.UserId`; moderator read/write requests use `AuthorizeResource(ResourceKinds.Event, view-management/moderate-light)` and handlers recheck tenant/report/event/case/concurrency. Moderation queue/detail DTOs now exclude stable reporter user/actor IDs, evidence creator IDs, decision moderator IDs, provider case/signal IDs, provider URLs, provider correlation IDs, and signal external IDs while preserving safe state, reason, priority, evidence text for authorized detail reads, counts, sync state, and HAL action links. | Keep public options anonymous-only and content-light; protect submission/readback by current user/tenant/event context; protect moderation queue/detail/actions by event moderation authority; gate UI actions through HAL links; keep moderation read projections data-minimized and never expose raw provider payloads, raw provider pointers, reporter hashes, or unsafe moderation notes. | 1/2 | `EventReportsControllerTests`; `ModerationReportControllerTests`; `EventReportHateoasTests`; `GetModerationReportQueueRequestHandlerTests`; `GetModerationReportDetailRequestHandlerTests`; generated OpenAPI/inventory/client workflow. | Breaking response-shape hardening for generated clients: removed identity/provider-pointer fields from moderation read DTO schemas and regenerated `schemas/openapi.json`, `docs/API_CONTRACT_INVENTORY.md`, and `EventApiClient.g.cs` from current source. | `docs/AUTHORIZATION.md`, `docs/SECURITY-MODEL.md`, `docs/API_CHANGELOG.md`, active plan/context/tasks. | Mitigated |
| R-036 | API/Auth/Ops | Authorization Lead | Email dispatch admin authorization parity | Email dispatch status, tenant pause/resume, park, and replay endpoints are authenticated operator APIs. Coarse `[Authorize]` or a generic update action is not enough because these operations inspect and mutate durable delivery control state. | 2026-07-04 mitigation: `GetEmailDispatchStatusQuery`, `SetEmailDispatchTenantPauseStateCommand`, `ParkEmailDispatchCommand`, and `ReplayEmailDispatchCommand` now implement `ISecureRequest` and use `[AuthorizeResource(ResourceKinds.EmailDispatch, view/manage_tenant/park/replay)]` with tenant/outbox attributes. HAL replay/park links use the same split actions. Cerbos policy/schema and local fallback both evaluate `islamuevent_email_dispatch` as tenant-scoped operator authority; regular authenticated users are denied, tenant admins are limited to their tenant, and instance admins retain provider authority. Controller metadata advertises 401/403 ProblemDetails. | Keep EmailDispatch as a tenant-scoped operational resource: status uses `view`, pause/resume use `manage_tenant`, parking uses `park`, and replay uses `replay`. Never expose email recipients, body, subject, provider message IDs, or raw provider errors; gate UI actions through HAL `_links`. | 1/6 | `EmailDispatchAuthorizationMetadataTests`; `EmailDispatchAdminControllerTests`; `EmailDispatchAdminHateoasTests`; `MachineScopeMappingTests`; `CerbosPolicyContractTests`; `cerbos/tests/islamuevent_email_dispatch_test.yaml`; generated OpenAPI/inventory/client workflow. | Breaking authorization hardening and response-metadata update: authenticated users without email-dispatch resource authority now receive `403`; generated artifacts include explicit 401/403 ProblemDetails metadata. | `docs/AUTHORIZATION.md`, `docs/API.md`, `docs/SECURITY-MODEL.md`, `docs/API_CHANGELOG.md`, active plan/context/tasks. | Mitigated |
| R-037 | Auth/API | Authorization Lead | User organization self-service read | `GET /api/user/{userId}/organizations` exposes a user's organization memberships and roles. A controller-local route/current-user check with an admin TODO could drift from the Application authorization boundary or be bypassed by non-HTTP callers. | 2026-07-04 mitigation: `GetUserOrganizationsRequestHandler` now injects `ICurrentUserService`, fails closed when no current user is resolved, and throws `AuthorizationException` before repository access when `request.UserId` differs from the authenticated current user. `UserController.GetUserOrganizations` keeps only the transport identity resolution for clean `401`, removes the stale admin TODO, and advertises `401`/`403` `ProblemDetails` metadata. OpenAPI/inventory/client artifacts were regenerated; the generated client method shape remains unchanged. | Keep this endpoint self-service only. Add a separate admin/organization-owner projection only if product requirements need it, guarded by explicit resource authorization, DTO minimization, HAL affordances, and tests. | 0/1/2 | `GetUserOrganizationsRequestHandlerTests`; `UserControllerTests.GetUserOrganizations_WhenRequestedUserDiffersFromCurrentUser_ShouldReturnForbiddenProblemDetails`; generated OpenAPI/inventory/client workflow; full build and architecture tests. | Breaking authorization/error-metadata hardening only: cross-user reads now fail in Application as `403` before repository access, and generated artifacts include `401`/`403` metadata. DTO shape and generated client method signature did not change. | `docs/AUTHORIZATION.md`, `docs/API.md`, active plan/context/tasks. | Mitigated |
| R-038 | Auth/API | Authorization Lead | Module governance writes | `POST /api/module/{moduleKey}/enable` and `/disable` change tenant module capabilities. Prior posture risked controller-level `[Authorize]` plus direct `IModuleService` calls, bypassing the MediatR resource-authorization pipeline and missing explicit write-rate metadata. | 2026-07-04 mitigation: `ModuleController` now dispatches `EnableTenantModuleCommand` and `DisableTenantModuleCommand` with the resolved `ITenantContext.TenantId`; both commands implement `ISecureRequest` and `[AuthorizeResource(ResourceKinds.Tenant, AuthorizationActions.Update)]` with tenant/module/action attributes. `EnableTenantModuleCommandHandler` resolves the audit actor through `IAdminContext.ResolveUserIdAsync`; both handlers delegate to `IModuleService` after authorization. The write routes declare the `Write` rate-limit policy, OpenAPI/inventory metadata was regenerated, and Local RBAC tests prove tenant-admin allow plus regular-user `403`. | Keep tenant module capability changes under tenant update authority unless a future dedicated `Modules.Manage` family is introduced with resource/action constants, Cerbos policy/schema, local fallback, machine scope mapping, HAL metadata, and tests atomically. | 0/1D | `ModuleCommandAuthorizationMetadataTests`; `TenantModuleCommandHandlerTests`; `ModuleControllerTests`; `LocalRbacAuthorizationTests`; API build and inventory generation. | No route, DTO schema, or generated client method signature change. Generated OpenAPI/inventory now records `x-rate-limit-policy` / `RateLimitPolicy` as `Write` for enable/disable. Authorization behavior hardens regular authenticated users to `403`. | Active plan/context/tasks; `endpoint-inventory.md`; `authorization-policy-matrix.md`; generated `docs/API_CONTRACT_INVENTORY.md`. | Mitigated |
| R-039 | API/Security/Analytics | Integration Security Lead | Anonymous analytics relay ingestion | `POST /api/a/t` is intentionally anonymous so first-party browser analytics can relay through the server, but that means route classification, limiter, and server-side governance must stay explicit. | 2026-07-04 mitigation: `AnalyticsRelayController.Relay` is `[AllowAnonymous]`, controller-classified as `EndpointClass.Public`, named `RouteNames.RelayAnalyticsEvent`, and protected by the dedicated `AnalyticsRelay` rate-limit policy. It delegates to `RelayAnalyticsEventCommand` through MediatR, where `RelayAnalyticsEventCommandHandler` resolves tenant analytics configuration, adds tenant context server-side, accepts only `pageview` or allow-listed `ui.`/`public.` track names, bounds property names/counts, and applies `AnalyticsGovernanceService` before provider calls. | Keep analytics relay as public ingestion only: no bearer/API-key requirement, no HAL mutation affordance, no public output cache, dedicated limiter, server-side tenant governance, and bounded/sanitized provider payloads. | 0/1/2 | `AnalyticsRelayControllerTests.RelayRoute_UsesStableAnonymousPublicIngestionMetadata`; `RelayAnalyticsEventCommandHandlerTests.Handle_PageViewRelay_SendsGovernedPageView`; existing `ProblemDetailsContractTests` malformed/unsupported/unknown-body coverage on `/api/a/t`. | No route, DTO, or generated client change. This slice adds missing API-boundary evidence only. | `endpoint-inventory.md`, `authorization-policy-matrix.md`, active plan/context/tasks. | Mitigated |
| R-040 | API/Auth/Privacy | Authorization Lead | User authentication token privacy | `UserAuthenticationToken` routes persisted OAuth/ATProto-style credential material and previously risked broad tenant reads, client-supplied ownership, output caching, and secret-bearing read DTOs. | 2026-07-04 mitigation: read handlers now resolve `ICurrentUserService.UserId` and use user-scoped repository methods before mapping; create/update handlers stamp `UserId` and `TenantId` from server context instead of trusting the request body; update/delete load by token id plus current user; read DTOs expose only `id`, `provider`, `pdsHost`, and `expiresAt`; create/update DTOs keep credential fields as write-only inputs and no longer accept `userId` or `tenantId`; repository detail/list methods no longer include `User.Pii`; read routes are authenticated, advertise `401`/`403`, return `404` for missing/non-owned IDs, and use `ResponseCache(NoStore=true, Location=None)` with no shared output cache. | Keep generic token routes self-service only. Never echo access, refresh, DPoP, or ID tokens in read contracts. Any future admin/session-support projection must be a separate resource-authorized, data-minimized API with explicit HAL affordances and tests. | 0/1/2 | `UserAuthenticationTokenSelfReadHandlerTests`; `UserAuthenticationTokenCommandHandlerTests`; `UserAuthenticationTokenDtoPrivacyTests`; `UserAuthenticationTokenControllerMetadataTests`; API build; generated OpenAPI/inventory/client workflow; Blazor client build. | Breaking schema/client hardening: `UserAuthenticationTokenDto` and `UserAuthenticationTokenListDto` removed user/tenant identity and credential fields; create/update DTOs removed client-supplied `userId` and `tenantId`; generated clients now treat credentials as write-only inputs and read only session metadata. | `docs/API_CHANGELOG.md`, `authorization-policy-matrix.md`, active plan/context/tasks. | Mitigated |
| R-041 | API/Auth/Privacy | Authorization Lead | External API-key management hardening | `ExternalApiKey` routes manage long-lived machine credentials and usage reports. The verified defects were shared `OutputCache(ListData/DetailData)` on key list/detail/usage-report reads, missing rate-limit metadata, owner-authority denials surfaced as validation `400`, unauthorized usage reports returned empty `200`, delete ignored failed revoke results, and platform-scoped update-name uniqueness used the tenant-filtered exists path. | 2026-07-04 mitigation: the existing owner-authority model is hardened without adding a partial `ApiKeys.*` policy family. Management reads/reports are no-store/no-output-cache and use the `Authenticated` limiter; create/update/delete use the `Write` limiter; controller metadata advertises typed 401/403/404 ProblemDetails where applicable; create/report denials throw `AuthorizationException` and return 403; missing/non-owned revokes return 404; platform key-name uniqueness uses `ExistsByOwnerAndNameIgnoringTenantFilter`; changed repository methods accept and pass cancellation tokens; OpenAPI/inventory/client artifacts and `docs/API_CHANGELOG.md` were regenerated/updated. | 1/2 | `ExternalApiKeyIntegrationTests`; `ExternalApiKeyControllerMetadataTests`; `ExternalApiKeyObservabilityTests`; API build; generated API contract inventory; NSwag client generation. | Breaking behavior/metadata hardening: unauthorized create/report calls return 403 instead of validation 400 or empty 200; non-owned/missing delete returns 404 instead of 204; key-management reads are no-store/no-output-cache; generated artifacts gain explicit 401/403/rate/cache metadata. Generated `docs/API_CONTRACT_INVENTORY.md` rows 231-236 now show no cache policy and `Authenticated`/`Write` rate-limit policies. | `docs/API_CHANGELOG.md`, `authorization-policy-matrix.md`, `endpoint-inventory.md`, active plan/context/tasks. | Mitigated |

### R-005 Implementation Notes
- 2026-06-14: `EventTemplateSyncController` and `EventSessionTemplateSyncController` diff failure branches now use sync-specific RFC7807 validation helpers instead of raw command envelopes. Readback and AFT diagnostics were clean; architecture verification is blocked because `bash-df9852dc` could not be inspected through the shell bridge.
- 2026-06-14: `TenantOnboardingController` complete/update-settings/save-step failures now use catalog-backed RFC7807 helpers: missing resolved user identity maps to `authentication_required`, tenant-admin denial maps to `forbidden`, and remaining command failures use operation-specific validation descriptors. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.

- 2026-06-14: `TenantSettingsDocumentsController` branding replacement failures now use the shared RFC7807 mapper/factory pattern: missing branding documents return `resource_not_found` through `ApiNotFoundProblemDescriptor` + `ToNotFoundProblem`, and replacement validation failures use `ApiValidationProblemDescriptor` + `ToCommandValidationProblem`. Readback and AFT diagnostics were clean; architecture verification is blocked by shell-bridge timeouts while checking the prior run and launching a fresh bounded run.
- 2026-06-14: `SettingsController` single-setting/reset/lock/unlock command failures now use the shared RFC7807 mapper/factory pattern, and administrator denials return catalog-backed `forbidden`; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.

## Status Legend

- `Open`: known risk not yet implemented.
- `In Progress`: implementation slice active.
- `Mitigated`: implementation and tests landed.
- `Deferred`: intentionally delayed with rationale and owner.

- 2026-06-14: `EventSessionTemplateController` create/update validation failures and update ID mismatch now use central RFC7807 validation helpers; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `EventTemplateController` update ID mismatch now uses central RFC7807 validation details through `ApiValidationProblemDescriptor` plus `ToValidationProblem`; readback/AFT clean, architecture verification blocked by shell-bridge timeouts while inspecting `bash-9aacb3b8` and launching a fresh bounded run.
- 2026-06-14: shared, event-local, and event-session-local custom-property definition update ID mismatches now use central RFC7807 validation details through resource-specific `ApiValidationProblemDescriptor` values plus `ToValidationProblem`; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: shared, event-local, and event-session-local custom-property purge fallback branches now use central RFC7807 not-found/validation helpers instead of raw command envelopes; readback confirmed the target branches are normalized, but architecture verification is blocked because `bash-3ad2ba99` could not be inspected through the shell bridge.
- 2026-06-14: `OrganizationController` create/update/delete and missing-identity failures now use central RFC7807 helpers instead of raw command envelopes or a controller-local unauthorized helper; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `UserExternalLoginController` create failures now use the central RFC7807 validation helper instead of returning the raw command envelope; existing update/not-found local helpers remain future consolidation debt. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `TenantController` create/update and tenant-navigation create/update/delete/reorder failures now use central RFC7807 validation/not-found helpers instead of raw command envelopes or anonymous mismatch payloads; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `LocalizationAdminController` TMS connection, governance update, and TMS export failures now use operation-specific RFC7807 validation helpers instead of raw command envelopes; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `FooterController` tenant footer link-group/link/settings write failures now use central RFC7807 validation helpers and missing current-user identity uses the shared `authentication_required` helper; readback/AFT were clean, but architecture verification is blocked because `bash-a6ec862c` could not be inspected through the shell bridge.
- 2026-06-14: `ContactShareConsentController` missing current-user, unresolved recipient-organization, and export failure paths now use central RFC7807 helpers instead of raw `Unauthorized()`, `NotFound()`, or `BadRequest(result)` responses; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `UiThemeAdminController` create/update validation failures, route/payload ID mismatch, and authorization-denial paths now use central RFC7807 helpers instead of raw `BadRequest(response)`, anonymous command-envelope mismatch responses, or raw `Unauthorized(response)` envelopes; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `StorageObjectController` authenticated upload-session missing-auth checks and storage-object update failures now use central RFC7807 helpers instead of raw `Unauthorized()`, anonymous ID-mismatch JSON, or raw `BadRequest(response)` responses; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `EventTeamController` missing current-user branches now use `ToAuthenticationRequiredProblem` instead of raw `Unauthorized()` responses on current-user permission, assignable-preset, role-assignment, and revocation endpoints; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `UserController` sync/current-user/admin-authority/organization/update/delete local auth and validation responses now use central RFC7807 helpers instead of raw command envelopes, strings, local ProblemDetails helpers, or string `Forbid(...)`; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: `InstanceSettingsController` repeated setup/admin denials, invalid identity branches, deployment-mode operator-control refusal, and shared command-response helper now use central RFC7807 helpers instead of raw `Forbid()`, `BadRequest(InvalidIdentityResponse())`, and command-envelope responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `OrganizationMemberController` member add, role update, invitation accept/decline, and delete missing-identity paths now use the shared `authentication_required` RFC7807 helper instead of a controller-local unauthorized ProblemDetails helper; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `AiAssistantController` model-discovery provider JSON/HTTP/timeout failures now use central 502 ProblemDetails through `ToBadGatewayProblem`, `ApiProblemTypes.BadGateway`, and `ApiProblemCodes.ProviderGateway` instead of controller-local ad hoc gateway ProblemDetails; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `EventAgendaItemController` and `EventDayController` read-side missing-resource branches now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` on agenda item detail, agenda projection, and event day detail endpoints; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `EventSessionLanguageController` create/delete missing-session and missing-language branches now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `LocationRoomController` room detail missing-resource handling now uses a location-room RFC7807 not-found helper instead of a bare `NotFound()` response; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `ActorSubscriptionController` actor-subscription detail missing-resource handling now uses an actor-subscription RFC7807 not-found helper instead of a bare `NotFound()` response; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `GroupController` group detail missing-resource handling now uses a group RFC7807 not-found helper instead of a bare `NotFound()` response; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `GroupMemberController` member detail and `EventSessionGroupController` program-section detail missing-resource handling now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `TenantUserRoleGrantController`, `OrganizationMemberController`, and `IndexedDidController` missing-resource handling now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Shared, event-local, and event-session-local custom-property definition detail endpoints now use definition-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Event template, event-session template, and event-session detail endpoints now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Actor-by-id, actor-by-DID, and organization detail endpoints now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Storage metadata/content/public image, tenant branding document, and UI theme detail/delete endpoints now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: External API key detail, auth context probe, AI conversation/run detail, and event session-create/program/detail/calendar missing-resource branches now use resource-specific RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: The remaining raw controller-return cluster was migrated to central RFC7807 helpers. Event delete and organization invitation email checks now use catalog-backed authentication-required responses; event publish concurrency uses a central conflict helper; setup-secret validation after completion uses a central `Gone` ProblemDetails helper with `setup_already_completed` code; authorization policy package download failures use a central service-unavailable helper; and rejected analytics relay events use a validation descriptor with `analytics_relay_rejected`. A direct controller grep no longer finds raw `BadRequest`, `Unauthorized`, `Forbid`, `NotFound`, `Conflict`, `StatusCode`, or `Problem` calls; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `ExternalApiKeyController` was consolidated away from its controller-local ProblemDetails mapper. Create/update validation, ID mismatch, and not-found outcomes now use central RFC7807 descriptors and mapper helpers, and the local validation/not-found code constants plus hand-built `ProblemDetails`/`ValidationProblemDetails` helpers were removed; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `UserExternalLoginController` was consolidated away from its controller-local ProblemDetails mapper. Create/update validation, ID mismatch, and not-found outcomes now use central RFC7807 descriptors and mapper helpers, and the local validation/not-found code constants plus hand-built `ProblemDetails`/`ValidationProblemDetails` helpers were removed; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `AtprotoRecordController` was consolidated away from its controller-local validation ProblemDetails mapper. Update ID mismatch and update command failures now use central RFC7807 descriptors and mapper helpers, and the local validation code constant plus hand-built `ValidationProblemDetails` helper were removed; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `NotificationController` was consolidated away from its controller-local not-found ProblemDetails builder. Archive and snooze not-found outcomes now use central RFC7807 descriptors and mapper helpers, and the local hand-built `ProblemDetails`/`application/problem+json` helper was removed; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `CustomPropertyProjectionAdminController` was consolidated away from its remaining controller-local projection-status ProblemDetails helper. Event and session projection status lookup failures now use central RFC7807 descriptors and mapper helpers, and the hand-built projection `ValidationProblemDetails` helper was removed; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `EventSessionController`, `EventSessionGroupController`, and `EventSessionLanguageController` were consolidated away from the shared controller-local `ProgramValidationProblemDetails` helper. Program/session validation failures now use central RFC7807 descriptors and mapper helpers, `ProgramValidationProblemDetails.cs` was deleted, and `ToProgramValidationProblem` has no references; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `StorageUploadProblemDetails.cs` was removed from `Explore.API/Controllers`. Storage upload-session failures now map through `CommandResponseResultMapper.ToStorageUploadProblem` and `ApiProblemFactory.CreateProblem`, preserving storage-specific 404/409/413/422/503 mappings while using the central RFC7807 result path; verification was limited to readback, direct grep, and AFT diagnostics per user direction.
- 2026-06-14: `EmailDispatchAdminController` and `AiAssistantProblemDetails` were consolidated into central RFC7807 mapper/factory paths. Email status/command failures now use `CommandResponseResultMapper.ToEmailDispatchValidationProblem`/`ToEmailDispatchProblem`; AI assistant command failures now use central `ToAiAssistantProblem` with provider, rate-limit, quota, status, and quota-extension metadata mappings preserved. Controller-local ProblemDetails construction was removed, and verification was limited to readback, direct grep, and AFT diagnostics per user direction.
- 2026-06-14: `OrganizationController` and `GroupController` approval-status route templates were normalized from action-word `updatestatustype/{id:guid}` to resource-oriented `{id:guid}/approval-status` while preserving the existing `RouteNames.UpdateOrganizationApprovalStatus` and `RouteNames.UpdateGroupApprovalStatus` constants. Direct organization integration tests were updated; generated OpenAPI/client artifacts remain stale until the later regeneration workflow.
- 2026-06-14: The same approval-status endpoints also received explicit OpenAPI error payload metadata. Organization approval status now declares ProblemDetails for 401/403/404; group approval status now declares `ValidationProblemDetails` for 400 and ProblemDetails for 401/403/404, reducing R-005 response-metadata drift before the broader metadata guardrail is enabled.
- 2026-06-14: `OrganizationController` and `GroupController` list/detail/create/update/delete metadata was tightened without changing action behavior. Validation failures now advertise `ValidationProblemDetails`, auth/forbidden/not-found outcomes advertise `ProblemDetails`, and group delete advertises its `BaseCommandResponse<Guid>` success payload, reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventSeriesController` and `EventSessionAgendaItemController` response metadata was tightened without changing action behavior. Event-series success responses now advertise concrete DTO/command-response payloads, validation failures advertise `ValidationProblemDetails`, not-found failures advertise `ProblemDetails`, and the agenda-item list validation metadata now advertises `ValidationProblemDetails` instead of a bare 400.
- 2026-06-14: `AnalyticsRelayController.Relay` and `CustomPropertyGovernanceController.GetGovernanceReport` response metadata was tightened without changing action behavior. Both endpoints now advertise `ValidationProblemDetails` for their existing 400 validation responses instead of bare status-only metadata.
- 2026-06-14: `LocationController` and `LocationRoomController` response metadata was tightened without changing action behavior. Existing validation paths now advertise `ValidationProblemDetails`, while authentication and not-found paths advertise `ProblemDetails`, matching the controllers' central RFC7807 mapper usage and reducing R-005 OpenAPI response drift.
- 2026-06-14: Seven read-only lookup detail endpoints had their existing 404 metadata tightened without changing behavior. `GroupPositionController`, `FileTypeController`, `EventStatusController`, `AudienceGenderController`, `AudienceAgeController`, `VisibilityTypeController`, and `ActorTypeController` now advertise `ProblemDetails` instead of bare status-only 404 responses, reducing R-005 OpenAPI response drift.
- 2026-06-14: Three additional detail endpoints had their existing 404 metadata tightened without changing behavior. `DidCustodyTypeController`, `RoleController`, and `ActorKeyStoreController.GetById` now advertise `ProblemDetails` instead of bare status-only 404 responses, further reducing R-005 OpenAPI response drift.
- 2026-06-14: Three more read-only lookup detail endpoints had their existing 404 metadata tightened without changing behavior. `RegistrationModeController`, `OrganizationPositionController`, and `MadhabController` now advertise `ProblemDetails` instead of bare status-only 404 responses, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `UserAuthenticationTokenController`, `UserExternalLoginController`, and `EventTeamController` response metadata was tightened without changing behavior. Existing not-found metadata now advertises `ProblemDetails`, external-login create validation advertises `ValidationProblemDetails`, and event-team missing-auth metadata advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `TenantOnboardingController` and `ModuleController` response metadata was tightened without changing behavior. Tenant onboarding command validation paths now advertise `ValidationProblemDetails`, tenant admin denial paths advertise `ProblemDetails`, module schema not-found metadata advertises `ProblemDetails`, and module enable/disable validation/auth/forbidden metadata now matches central RFC7807 helper usage, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `ExternalApiKeyController`, `TenantUserRoleGrantController`, and `ManagedProviderProvisioningController` response metadata was tightened without changing behavior. Existing not-found and forbidden metadata now advertises `ProblemDetails`, and command validation metadata now advertises `ValidationProblemDetails`, matching central RFC7807 helper usage and further reducing R-005 OpenAPI response drift.
- 2026-06-14: `TagController`, `CategoryController`, and `OrganizationMemberController` response metadata was tightened without changing behavior. Taxonomy validation paths now advertise `ValidationProblemDetails`, taxonomy authentication/not-found metadata advertises `ProblemDetails`, and organization-member detail not-found metadata now matches central RFC7807 helper usage, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventTemplateController` and `EventSessionTemplateController` response metadata was tightened without changing behavior. Template list/create/update validation paths now advertise `ValidationProblemDetails`, authentication/not-found metadata advertises `ProblemDetails`, and existing event-template quota responses continue to advertise `ProblemDetails` 422, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventSessionLanguageController`, `EventSessionGroupController`, and `EventAgendaItemController` response metadata was tightened without changing behavior. Existing authentication and not-found metadata now advertises `ProblemDetails`, while validation metadata remains `ValidationProblemDetails`, matching central RFC7807 helper usage and further reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventSessionController` and `EventDayController` response metadata was tightened without changing behavior. Event-session authentication/not-found metadata now advertises `ProblemDetails`; event-day create/update validation metadata now advertises `ValidationProblemDetails`, and event-day authentication/not-found metadata now advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `CustomPropertyDefinitionController`, `EventCustomPropertyController`, and `EventSessionCustomPropertyController` response metadata was tightened without changing behavior. Custom-property list/create/update/purge/value validation metadata now advertises `ValidationProblemDetails`; authentication, authorization, and not-found metadata now advertises `ProblemDetails`; existing quota/conflict responses remain typed as `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `TenantStorageSettingsController`, `ActorSubscriptionController`, and `TenantSettingsDocumentsController` response metadata was tightened without changing behavior. Tenant storage and actor-subscription command validation now advertises `ValidationProblemDetails`; tenant branding document authentication/authorization/not-found metadata now advertises `ProblemDetails`; existing conflict metadata remains typed as `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `GroupMemberController` and `ContactShareConsentController` response metadata was tightened without changing behavior. Group-member authentication/not-found metadata now advertises `ProblemDetails`; contact-share withdraw authentication metadata advertises `ProblemDetails`; shared-contact list/export validation metadata advertises `ValidationProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventFormatController` and `EventTemplateSyncController` response metadata was tightened without changing behavior. Event-format detail not-found metadata now advertises `ProblemDetails`, and template-sync diff/apply/history authentication and authorization metadata now advertises `ProblemDetails` while existing validation/not-found/conflict metadata remains typed, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `SettingsController` and `StorageObjectController` response metadata was tightened without changing behavior. Settings validation metadata now advertises `ValidationProblemDetails`, settings authentication/authorization metadata advertises `ProblemDetails`, and storage list/upload/object validation plus authentication/not-found metadata now advertises the central RFC7807 payload types, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventSessionTemplateSyncController` and `UiThemeAdminController` response metadata was tightened without changing behavior. Event-session template sync authentication and authorization metadata now advertises `ProblemDetails`; UI theme admin validation metadata now advertises `ValidationProblemDetails`, and authentication/authorization/not-found metadata now advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `NotificationController` response metadata was tightened without changing behavior. Notification list validation metadata now advertises `ValidationProblemDetails`, and existing authentication/not-found metadata now advertises `ProblemDetails`, including the read, archive, snooze, delete, unread-count, and refresh-stream endpoints, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `ActorController` response metadata was tightened without changing behavior. Actor list and tenant-list validation metadata now advertises `ValidationProblemDetails`; actor detail/DID not-found metadata and create/update/delete authentication, authorization, and not-found metadata now advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `LocalizationAdminController` and `EventRegistrationController` response metadata was tightened without changing behavior. Localization test/governance/export validation metadata now advertises `ValidationProblemDetails`, localization admin authentication metadata advertises `ProblemDetails`, and event-registration list/create validation plus authentication/not-found metadata now advertises central RFC7807 payload types, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `CustomPropertyProjectionAdminController` response metadata was tightened without changing behavior. Projection rebuild, single-rebuild, dirty-scope, and projection lookup validation metadata now advertises `ValidationProblemDetails`, while existing quota responses remain typed as `ProblemDetails` 422, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `FooterController` response metadata was tightened without changing behavior. Footer link-group, link, reorder, and tenant footer settings validation metadata now advertises `ValidationProblemDetails`, while existing link-group/link not-found metadata now advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `InstanceOnboardingController` command response metadata was tightened without changing behavior. Onboarding completion, auth-provider save/bootstrap, authorization-provider save/sync, and endpoint verification validation failures now advertise `ValidationProblemDetails` instead of command envelopes, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `TenantController` response metadata was tightened without changing behavior. Tenant create/update and tenant-navigation create/update/reorder validation metadata now advertises `ValidationProblemDetails`; tenant and navigation-link not-found metadata now advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `UserAppearanceController` response metadata was tightened without changing behavior. Appearance profile creation validation now advertises `ValidationProblemDetails`; existing authenticated/not-found metadata for appearance reads, profile actions, palette generation, archiving, and duplication now advertises `ProblemDetails`, further reducing R-005 OpenAPI response drift.
- 2026-06-14: `EventController` response metadata was tightened without changing behavior. Event list/auth, creation, publish, draft/status update, delete, calendar/program/detail, readiness, and Islamic/tech aspect endpoints now advertise `ValidationProblemDetails` for validation failures and `ProblemDetails` for authentication, authorization, not-found, and publish-conflict failures, further reducing R-005 OpenAPI response drift.
- 2026-06-15: `OpenApiStringEnumSchemaCatalog` was expanded to cover the DTO-exposed Domain enum set plus Application DTO/model enums discovered by the new guard (`SettingSource`, `CustomPropertyFilterOperator`, `ManagedProviderOrganizerKindDto`, `BatchUpdateMode`, public-experience CTA/home-block enums, `PublicExperienceMode`, `PublicExperiencePrimaryOrganizationState`, and `KeycloakBootstrapMode`), fixing the string-enum schema root cause behind OpenAPI contract drift. `ApiContractArchitectureTests.DtoEnumProperties_MustBeRegisteredIn_OpenApiStringEnumSchemaCatalog` now scans public DTO contract enum properties, excludes validator implementation types, and compares the discovered enum set to the internal API catalog by reflection, so future missing registrations fail architecture tests before generated OpenAPI falls back to integer enum schemas. Verification: scoped AFT diagnostics were clean; project-level `Event.Architecture.Tests` no longer reports the enum-catalog test as failing, but still exits non-zero because of pre-existing agent-context manifest/dead-link failures under `.claude/`.
- 2026-06-15: `InstanceSettingsController` response metadata was tightened without changing behavior. Instance admin/setup-secret forbidden paths now advertise `ProblemDetails`, and settings, authentication, storage, provider, authorization, and deployment command-validation paths now advertise `ValidationProblemDetails` instead of command envelopes. Focused and broad controller greps for stale 4xx/command-envelope metadata now return no matches under `Explore.API/Controllers`, closing the current R-005 response-metadata sweep target.
- 2026-06-15: Route-template cleanup is closed for the current inventory: the only confirmed action-word route risk was the organization/group approval-status route already normalized from `updatestatustype/{id:guid}` to `{id:guid}/approval-status`; generated OpenAPI/client regeneration remains pending for that recorded contract change. The preview/v1.0 contract stance is now explicit in `docs/API.md`: pre-v1.0 breaking contract changes may be accepted for cleaner API/HAL/OpenAPI contracts only with `docs/API_CHANGELOG.md`, regenerated OpenAPI/inventory/generated-client workflows as applicable, and contract-governance evidence; generated artifacts must not be hand-edited, and v1.0 breaking schema diffs become blocking per governance.
- 2026-06-15: R-005 metadata/classification review is now backed by OpenAPI posture vendor extensions. `EndpointClassificationTransformer` emits `x-endpoint-class`, `x-rate-limit-policy`, `x-output-cache-policy`, and `x-tenant-mode` from endpoint metadata, and `ContractInvariantsTests.OpenApiDocument_EndpointPostureExtensionsExposeRateCacheAndTenantMetadata` guards representative rate-limit/cache/tenant emission. Broad per-endpoint inventory completeness remains future governance, but the Phase 2 contract mechanism now exposes these values in runtime OpenAPI.
- 2026-06-15: Phase 2 verification was blocked by unrelated environment/context failures, not by the latest R-005 metadata or enum-catalog slices. Scoped AFT diagnostics were clean and the enum-catalog architecture guard passed in the supported project-level architecture run, but `Event.Architecture.Tests` exited non-zero because of then-existing `.claude` agent-context manifest/dead-link failures. API integration tests were blocked by Docker/Testcontainers/host-shutdown instability, and full build/Blazor verification was blocked by the then-existing `AiActionResultCard.razor` Razor syntax error. 2026-07-03 recheck: the Blazor/Docker/no-Keycloak/Testcontainers smoke lanes pass, and the agent-context architecture failures are resolved.
- 2026-06-15: Missing-identity authorization response risk is closed for the original anti-pattern. Controllers now use catalog-backed `authentication_required` ProblemDetails helpers instead of raw missing-user 401 strings; focused grep found no raw `Unauthorized(...)`, `Forbid()`, or `"User ID not found"` controller returns, and `ApiContractArchitectureTests.Controllers_MustNotReturn_RawMissingUserUnauthorizedStrings` guards against reintroducing the old string response. 2026-07-03 recheck: the unrelated `.claude` context architecture failures referenced at the time are resolved.
- 2026-06-15: R-006 reverse `RouteNames` cleanup began by pruning truly unused constants from `RouteNames.cs` after classifying candidate orphan names by reference usage. Legacy lookup aliases, unused approval-status/event-type CRUD names, stale user and organization-member CRUD names, stale current-user appearance update, and unused event-team ownership/window names were removed. Policy-used constants that no longer resolve to endpoints remain a separate design decision and must be reattached or removed from stale HAL affordances before `RouteNames_EveryConstantResolvesToExactlyOneEndpoint` can be unskipped.
- 2026-06-15: R-006 cleanup continued by realigning stale HATEOAS policy route names. ATProto record policies now use the registered `*AtprotoRecordEntry` route names and no longer emit the unsupported `by-uri` affordance; event-registration policies now use the registered `*EventRegistration` route names. The obsolete ATProto/registration aliases were removed from `RouteNames.cs`, focused grep found no remaining references, and AFT diagnostics were clean. Remaining policy-used/no-endpoint candidates still need classification before reverse route-name coverage can be enabled.
- 2026-06-15: R-006 cleanup removed unsupported arbitrary-user and tenant-by-slug HAL affordances. User, tenant-user-role, organization-review, organization-member, group-member, event-registration, and tenant policies no longer emit links backed by missing `GetUserById`, `GetUsers`, `UpdateUser`, or `GetTenantBySlug` endpoints; supported current-user, actor, user organization/registration, tenant-by-id, tenant collection, and tenant create links remain. The obsolete constants were removed from `RouteNames.cs`, focused grep found no remaining references, and AFT diagnostics were clean. Remaining policy-used/no-endpoint candidates still need classification before reverse route-name coverage can be enabled.
- 2026-06-15: R-006 cleanup removed unsupported relationship/review HAL affordances that had no registered endpoints. Organization, actor, event, category, tag, and organization-review policies no longer emit links backed by missing `GetOrganizationEvents`, `GetActorEvents`, `GetEventCategories`, `GetEventTags`, `GetCategoryChildren`, `GetCategoryEvents`, `GetTagEvents`, `GetTagTagTypes`, `GetOrganizationReviewById`, `UpdateOrganizationReview`, or `DeleteOrganizationReview` routes. The obsolete constants were removed from `RouteNames.cs`, focused grep found no remaining references, and AFT diagnostics were clean. Remaining policy-used/no-endpoint candidates still need classification before reverse route-name coverage can be enabled.
- 2026-06-15: R-006 cleanup retargeted or removed the remaining event-session, organization-member, and user-registration affordance drift. `EventSessionLinkPolicy` no longer emits the unsupported `GetEventSessionSpeakers` link, `OrganizationLinkPolicy` now targets registered `GetOrganizationMembersByOrganization` with the expected route parameter, and `UserLinkPolicy` now targets registered `GetRegistrationsByUser`. The obsolete `GetEventSessionSpeakers`, `GetOrganizationMembers`, and `GetUserRegistrations` constants were removed from `RouteNames.cs`, focused grep found no remaining references, and AFT diagnostics were clean. Remaining policy-used/no-endpoint candidates still need classification before reverse route-name coverage can be enabled.
- 2026-06-15: R-006 reverse route-name coverage was closed after the final read-only RouteNames classification reported 452/452 constants backed by exactly one registered endpoint with no missing or duplicate route names. `RouteNames_EveryConstantResolvesToExactlyOneEndpoint` is now unskipped, making the reverse guard active alongside endpoint-to-constant coverage. Full API integration execution remains deferred to the blocked verification item.
- 2026-06-15: Phase 2 implementation-side contract stabilization is complete enough to enter Phase 3 with caveats: RFC7807 cleanup, route-template cleanup, bidirectional route-name guardrails, response metadata, endpoint classification/auth alignment, OpenAPI enum catalog coverage, and OpenAPI rate/cache/tenant posture extensions are in place. Remaining verification risk is explicitly external/pre-existing: architecture tests still fail on `.claude` context issues, API integration execution remains blocked by Docker/Testcontainers/host-shutdown instability, and full build/Blazor verification remains blocked by the pre-existing `AiActionResultCard.razor` syntax error.
- 2026-07-04: R-007 first controller-decomposition slice is complete and partial-risk-reducing, not full closure. `EventAspectController` now owns the Islamic/tech aspect subresource actions under `[Route("api/event")]` and `[Tags("Event")]`, preserving the existing `/api/event/{id}/aspects/...` external contract and `RouteNames` while removing those aspect responsibilities from `EventController`. Verification passed: `Explore.API` Release build, `Event.API.IntegrationTests` Release build, focused `EventAspectControllerTests` 5/5, post-split `RouteNameCoverageTests` 3/3, and focused endpoint-classification/context architecture tests 3/3.
- 2026-06-14: `ModuleController` missing-schema and module enable/disable failures now use central RFC7807 not-found/validation helpers instead of controller-local `Problem(...)` calls; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `EventRegistrationController` create/update/delete failure paths now use central RFC7807 validation, authentication, and not-found helpers instead of controller-local ProblemDetails construction; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `TagController`, `CategoryController`, and `LocationController` detail-read missing-resource branches now use central RFC7807 not-found helpers instead of bare `NotFound()` responses; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: `EventController.GetMyEvents` missing-identity handling now uses the shared `authentication_required` RFC7807 helper instead of raw `Unauthorized()`; per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: selected `CustomPropertyProjectionAdminController` command-failure branches now use central RFC7807 helpers instead of raw `BadRequest(result)` command envelopes. Single-event rebuild, dirty-scope drain, and single-session rebuild failures use resource-specific validation descriptors plus `ToCommandValidationProblem`; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- 2026-06-14: user direction changed the continuation loop to stop launching redundant test suites and prioritize implementation progress. Later scoped controller-normalization entries should rely on direct readback/AFT diagnostics until the plan reaches a dedicated verification phase.
- 2026-06-14: `AiAssistantController` model-discovery authorization and validation failures now use central RFC7807 helpers instead of raw `Forbid()` or `BadRequest(CreateProblem(...))` 400s. Tenant/platform-admin denial maps to `forbidden`; missing/invalid provider endpoint input maps to validation ProblemDetails via the `aiAssistantModelDiscovery` descriptor. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: `CustomPropertyProjectionAdminController` event/session projection lookup failures now use central RFC7807 helpers instead of raw command envelopes. Event and session lookup branches use dedicated validation descriptors plus `ToCommandValidationProblem`; direct readback corrected a descriptor mix-up before recording. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: `InstanceOnboardingController` completion and setup-secret command failures now use central RFC7807 helpers instead of raw command envelopes. Missing resolved user identity maps to `authentication_required`, preflight blockers use validation ProblemDetails, and auth-provider/keycloak-bootstrap/authorization-provider/policy-sync/endpoint-verify failures use operation-specific descriptors plus `ToCommandValidationProblem`. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: `EventController` create, publish, draft update, status update, Islamic aspect upsert, and tech aspect upsert failures now use central RFC7807 helpers instead of raw command envelopes. Event aspect not-found outcomes use a shared not-found descriptor; publish concurrency conflict remains a later conflict-mapping slice. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: `SettingsController` user and tenant batch update failures now use the existing `settings` validation descriptor plus `ToValidationProblem` instead of raw `BadRequest(result)` DTO envelopes. Per user direction, verification was limited to readback and AFT diagnostics.
