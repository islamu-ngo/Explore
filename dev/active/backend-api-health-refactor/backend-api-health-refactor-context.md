<!-- ABOUTME: Research and repository context for the backend/API health refactor plan. -->
<!-- ABOUTME: Captures verified hotspots, governing conventions, and external documentation findings. -->

# Backend API Health Refactor Context

Last Updated: 2026-06-13 Europe/Brussels

## 1. Request Summary

Create and refine implementation planning docs for a backend/API refactor using repo conventions, Clean Architecture, enterprise-grade maintainability, industry best practices, Tavily research, and Context7 documentation. Backward compatibility is intentionally not required because the codebase is still in development mode, but breaking changes must be inventoried, documented, and reflected in OpenAPI/generated-client workflows before implementation.

CTO feedback approved the plan direction but required stricter scope control, mandatory artifacts, enforceable phase gates, tenant/auth precision, bootstrap/admin behavior, idempotency, optimistic concurrency, audit logging, rate-limit/cache classification, cursor-contract decisions, and database/index review.

## 2. Scope

In scope:
- `Explore.API` (including `AiAssistantController`, `StorageObjectController`, `EmailDispatchAdminController`, and `TenantStorageSettingsController`)
- `Explore.Application` (including CQRS requests and handlers for AI conversations, upload sessions, and outbox/replay actions)
- `Explore.Persistence` (including repositories, entity configurations, and the `ApiTickerQDbContext` operational scheduler store)
- `Explore.Infrastructure` (including local-first and S3-compatible storage providers, RabbitMQ consumer/dead-letter services, and outbox drain services)
- `Explore.Domain` (including storage metadata, email dispatch receipts, and messaging settings)
- backend test projects
- API/HAL/OpenAPI/security/operations docs
- authorization policy assets, Cerbos mappings, and HAL authorization metadata
- architecture and contract guardrails

Out of scope:
- Blazor visual redesign, styling, and unrelated user-flow redesign
- frontend client refactors except documenting downstream impact of API/HAL contract changes and fixing HAL action-affordance contract violations
- production migration rollout choreography beyond verification, changelog, OpenAPI, and documentation tasks

## 3. Governing Repo Rules

From `AGENTS.md`, `.claude/contract/intents.yaml`, `.claude/rules/*`, and canonical docs:

- Domain has no EF Core, ASP.NET Core, MediatR, or infrastructure dependencies.
- Application references Domain and owns CQRS requests, handlers, DTO mapping, validators, specifications, use-case orchestration, idempotency semantics, and authorization metadata.
- Persistence implements repositories and EF Core details; repositories return entities and do not expose `IQueryable`.
- API is the composition/transport boundary: controllers dispatch MediatR, assemble HAL, return typed `ActionResult`, and use `RouteNames.Xxx`.
- Validators are manually instantiated in handlers/services; do not inject `IValidator<T>` through DI unless repo convention changes.
- GET endpoints are usually `[AllowAnonymous]` only for intentionally public data; anonymous reads must not expose user IDs, emails, full names, memberships, roles, grants, revocations, or identity metadata without an explicit product/security decision and tests.
- HATEOAS links are the client action source of truth and fail closed; Blazor affordances must check `_links`, not roles, claims, or local organization role caches.
- Named EF Core filters should preserve tenant isolation when only soft-deleted rows are included.
- Chained `IExceptionHandler` plus RFC 7807 ProblemDetails is the expected API error model.
- Outbox/background dispatchers must be idempotent and retry-safe when used for durable side effects.

## 4. Related Active Workstreams

- `dev/active/api-contract-stabilization`: endpoint/OpenAPI inventory and route-name/contract stabilization. This backend health plan should consume its inventory and avoid duplicating generation ownership.
- `dev/active/openapi-modernization`: build-time OpenAPI generation modernization. This plan should align contract/error/route changes with it.
- `dev/active/eav-custom-properties`: overlaps with custom-property persistence, governance, and projection behavior.
- `dev/active/event-scoped-operational-roles`: overlaps with authorization policies and resource actions.
- `dev/pause/performance-optimization`: overlaps with keyset pagination, indexing, and query-shape improvements.

## 5. CTO Refinement Decisions

### Mandatory artifacts before behavior changes

Phase 0 must create implementation inputs, not afterthought documentation:

- `endpoint-inventory.md`
- `endpoint-classification.md`
- `backend-contract-risk-register.md`
- `authorization-policy-matrix.md`
- `tenant-execution-model.md`
- `api-error-catalog.md`

Endpoint inventory must include route, route name, current auth, target classification, HAL links, OpenAPI operation ID, rate-limit policy, cache policy, auth classification, tenant mode, risk, and action.

### Tenant execution model

The plan will use this design vocabulary:

```csharp
public enum TenantExecutionMode
{
    RuntimeTenantRequired,
    RuntimeTenantOptionalPublicRead,
    HostAdministration,
    BackgroundSystem,
    MigrationOrSeeding,
    DesignTime
}
```

`RuntimeTenantOptionalPublicRead` is not an all-tenant mode. It is valid only when tenant resolution comes from host/domain/path, the data is globally public platform-level data, or an explicit cross-tenant read model is invoked with authorization and audit logging.

Tenant bypass must be explicit, reason-coded, logged, tested, and unavailable from controllers as raw LINQ helpers. Preferred APIs are use-case-shaped, such as `crossTenantEventReadStore.GetTenantSummariesAsync(reason, cancellationToken)` or a scoped runner such as `tenantScope.RunAsHostAdministratorAsync(...)`.

Background workers, including the `OutboxProcessor` (RabbitMQ consumer/replay service), storage reconciliation workers, and TickerQ scheduler jobs (outbox drains, reminders), must execute explicitly in `BackgroundSystem` or `MigrationOrSeeding` mode, ensuring they do not bypass tenant logic without audited logging and reason-coded scopes.

### Authorization policy naming

Role-sounding policies should be replaced by capability/resource/action policies:

- `Templates.Manage`
- `Events.Edit`
- `Events.Publish`
- `CustomProperties.Govern`
- `PlatformNamespaces.Edit`
- `Modules.Manage`
- `StorageObjects.Read` / `StorageObjects.Write` / `StorageObjects.ReadPresigned`
- `TenantSettings.Manage` / `TenantStorage.Manage`
- `AiAssistant.Interact` / `AiAssistant.Manage`
- `Email.Dispatch` / `Email.Manage`
- `InstanceSettings.Manage`

The policy matrix must map API policy, handler attribute, Cerbos resource/action, HAL rel, and default roles.

### Bootstrap/admin story

Self-hosted deployments need a first-admin/bootstrap story covering setup secret, Keycloak group/role mapping, Keycloak client secret rotation workflow, backup-confirmed sync apply flows, bootstrap disablement, auditability, SingleTenant/MultiTenant behavior, and missing auth-provider configuration behavior.

### Controller decomposition order

Controller splitting must happen after inventory, tenant/auth hardening, error/result mapping, route-name cleanup, OpenAPI metadata, and HAL guardrails. Splitting by method count alone is explicitly rejected.

### Enterprise reliability additions

The implementation must treat audit logging, idempotency (e.g., `Idempotency-Key` headers for AI assistant prompts and storage upload session registration), optimistic concurrency, transaction boundaries (e.g., RabbitMQ outbox transition and TickerQ operational state), rate-limit/cache posture, cursor contract design, and database/index review as first-class concerns.

## 6. Verified Codebase Hotspots

### 2026-06-13 audit update — confirmed P0/P1 hotspots

The June 2026 architecture audit supersedes the older priority order. The workstream should start with confirmed P0 data exposure and authorization/HAL contract issues before broad controller decomposition.

P0 confirmed findings:

- `Explore.API/Controllers/EventRegistrationController.cs` exposes anonymous registration list/detail/by-session/by-user reads. DTOs and mappings include `UserId`, `UserFullName`, `UserEmail`, status, event/session, and tenant data through repository includes of `User.Pii`.
- `Explore.API/Controllers/TenantUserRoleGrantController.cs` has class-level `[Authorize]`, but read endpoints override with `[AllowAnonymous]` and return user email/full name, role, tenant, grant, revocation, and audit metadata.
- `Explore.API/Controllers/OrganizationMemberController.cs` exposes anonymous organization member reads returning user email/full name, role, and position metadata.
- `Explore.API/Controllers/FooterController.cs` write endpoints rely on generic `[Authorize]`, pass `CurrentUserId ?? Guid.Empty`, and footer command requests lack resource authorization metadata.
- `Explore.Blazor.Client/Pages/Events/MyEvents.*`, `Explore.Blazor.Client/Pages/Organizations/MyOrganizations.*`, and `Explore.Blazor.Client/Pages/Admin/EventTemplates/EventTemplateListPage.razor` gate action affordances from local role state or empty-list fallback instead of HAL `_links`.

P1 confirmed findings:

- Domain files contain pervasive `System.ComponentModel.DataAnnotations`/`Schema` usage, including many `[ForeignKey]` attributes on `Event`. This violates the intended pure Domain / Persistence-mapping split and lacks an architecture test.
- Several query requests return `BaseCommandResponse<T>` and handlers place read data in the command-response `Id` slot: `GetEmailDispatchStatusQuery`, event aggregate view queries, and event/session custom-property projection queries.
- `IEventAggregateViewRepository` accepts `AggregateViewFilterDto`, leaking Application DTO shape into a repository contract.
- `GenericRepository.GetById` uses tracked `FindAsync`, `Exists` calls it, and `HardDelete` is globally available with only comment-level administrative restriction.
- `TenantPolicySettingService.Read` performs dozens of sequential per-key settings queries; `GetAllForTenant` exists but is unused, and the per-key repository read lacks `AsNoTracking`.
- Event/session template diff/sync/instantiation services and custom-property projection filters are duplicated across event and session variants.
- `SendAiMessageCommandHandler` and `ProcessAiRunCommandHandler` are oversized use-case coordinators similar to the already-known `CreateEventCommandHandler` hotspot.

Audit-cleared areas that should not be treated as primary defects without new evidence: no production Application direct `ExploreDbContext`, no repository interface returning DTOs or `IQueryable`, no validator DI injection, no sampled query-handler mutations, and direct raw runtime `.IgnoreQueryFilters()` is already guarded by architecture tests.

### Tenant filters

2026-06-14 review supersedes the original audit suspicion for the active query-filter implementation. `Explore.Persistence/ExploreDbContext.QueryFilters.cs` now uses fail-closed predicates shaped as `IsTenantFilterBypassed || e.TenantId == TenantFilterTenantId`; when tenant context is absent, tenant-scoped rows do not broaden to all tenants unless an explicit bypass reason is enabled. `PersistenceTenantFilterArchitectureTests` guards against reintroducing the old `TenantContext == null || ...` shape. Phase 1C still needs richer execution-mode logging and semantic predicate proof for every tenant-filter bypass call site.

### Query-filter bypass helpers

`Explore.Persistence/QueryFilters/QueryFilterExtensions.cs` contains:

- `IncludeDeleted(...)`: positive pattern; disables only `SoftDelete`.
- `IgnoreTenantFilter(...)`: high-risk and should require explicit cross-tenant/system use.
- `IgnoreAllFilters(...)`: disables all filters and should be restricted or removed from runtime paths.

### 2026-06-14 — Phase 1E bootstrap contract decisions

Phase 1E now has an explicit self-host bootstrap contract. `POST /api/instanceonboarding/complete` requires both an authenticated identity and a valid setup secret; the setup secret alone is not an identity or post-bootstrap admin authority. SingleTenant onboarding grants the first platform administrator default tenant-admin membership and branding defaults; MultiTenant onboarding grants platform admin only and requires explicit tenant-admin provisioning later. Setup-secret-protected endpoints return `410 Gone` after completion. Audit event names are documented in `tenant-execution-model.md`; audit emission, missing-provider ProblemDetails, and Keycloak backup-confirmed rotation/sync tests remain follow-up work. Verification: `Event.Application.UnitTests` passed 1356/1356 after adding MultiTenant first-admin coverage; `Event.API.IntegrationTests` compiled and started but hung during repeated host shutdown/OpenFeature lifecycle cleanup and was killed after 219s, so setup-secret API tests are present but not green in this environment.

### Placeholder authorization policies

The 2026-06-14 Phase 1D slices removed the authentication-only registrations for `template_admin`, `event_editor`, `property_governance_admin`, and `platform_namespace_editor` from `Explore.API/Extensions/AuthenticationExtensions.cs`. `template_admin` was replaced with existing `CustomPropertyTemplate` request metadata for sync diff/apply/history; the other three had no active C# `[Authorize(Policy = ...)]` usage and were removed rather than preserved as login-only privileged policies.

Future event edit/publish, custom-property governance, or platform namespace edit capability families must be introduced through explicit resource/action metadata, Cerbos/fallback parity, HAL link requirements, and tests instead of re-adding broad role-sounding policies.

### Fat controllers

`Explore.API/Controllers/EventController.cs` combines event listing, my-events, creation context, details, calendar export, create/readiness/publish/update/status/delete, Islamic aspect CRUD, and tech aspect CRUD. It also manually maps a large filter request and branches on command response messages/failure codes.

`Explore.API/Controllers/UserAppearanceController.cs` was previously listed as a hard-coded route-name hotspot, but 2026-06-14 readback confirmed all of its HTTP route names now use `RouteNames.Xxx`. Focused controller grep for literal HTTP route-name strings currently returns no matches, so remaining route-name work is reverse catalog cleanup rather than controller literal migration.

`Explore.API/Controllers/UserController.cs` contains a TODO for admin checks when viewing another user's organizations and returns ad hoc `BadRequest`/`Forbid` responses.

`Explore.API/Controllers/AiAssistantController.cs` exposes 10 endpoints for conversations, prompts, status, and confirmations, relying only on generic `[Authorize]` attributes without explicit capability policies. It integrates custom idempotency key validation; AI command failures now flow through central `CommandResponseResultMapper.ToAiAssistantProblem` instead of the deleted controller-local `AiAssistantProblemDetails` helper.

`Explore.API/Controllers/StorageObjectController.cs` contains 12 endpoints. It streams bytes directly, serves public image URLs with cache-headers, and manages upload sessions. Its upload-session failures now flow through the central `CommandResponseResultMapper.ToStorageUploadProblem` mapper instead of a controller-local ProblemDetails helper, but the controller still relies entirely on permissive `[Authorize]` or `[AllowAnonymous]` attributes.

`Explore.API/Controllers/EmailDispatchAdminController.cs` handles operator actions for pausing/resuming email delivery and replaying outbox items. Email status and command failures now flow through central `CommandResponseResultMapper.ToEmailDispatchValidationProblem` and `ToEmailDispatchProblem`, but the controller still relies on broad class-level `[Authorize]`.

### Route-name guardrails

`Event.API.IntegrationTests/Features/RouteNameCoverageTests.cs` now actively enforces both endpoint-to-`RouteNames` and constant-to-endpoint coverage. The reverse catalog cleanup guard is active after the R-006 orphan cleanup:

- `RouteNames_EveryConstantResolvesToExactlyOneEndpoint`

The final read-only RouteNames classification reported every catalog constant backed by exactly one registered endpoint, so controller route-name drift is guarded in both directions. Current focused grep also found no literal HTTP route-name strings in controller attributes.

### CQRS handler size and behavior bypass risk

`Explore.Application/Features/Events/Handlers/Commands/CreateEventCommandHandler.cs` has roughly thirty constructor dependencies and coordinates event creation, sessions, days, rooms, agenda, aspects, custom properties, projection refresh, metrics, cache invalidation, and unit-of-work behavior. It should be decomposed into narrow collaborators, not one large replacement application service.

Prior research also identified direct handler invocation in public experience shell query handling, which risks bypassing MediatR behaviors if confirmed during implementation.

### New Subsystem Infrastructure & Scheduling Hotspots

- `Explore.API/Scheduling/EmailDispatchTickerQJobs.cs` and `EventLifecycleTickerQJobs.cs`: cron/trigger-based outbox drains and reminder dispatches. They reference Postgres storage (`ApiTickerQDbContext`) for scheduling state.
- `Explore.Infrastructure/Messaging/EmailDispatchRabbitMqConsumerService.cs`: consumes messages from RabbitMQ and runs in a background thread context where tenant resolution requires careful quarantine.
- `Explore.Infrastructure/EmailDispatchDrainService.cs` and `EmailDispatchRabbitMqDeadLetterReplayService.cs`: manage state changes and retries on PostgreSQL tables (`EmailDispatchReceipt`), requiring strict database transaction boundaries and concurrency checks.

### Persistence health

`Explore.Persistence/Repositories/EventRepository.cs`, `OrganizationRepository.cs`, and related repositories use repeated offset pagination (`Skip((pageNumber - 1) * pageSize)`), broad include graphs, and many EF async calls without cancellation tokens. High-volume public feeds should move toward opaque cursor/keyset pagination; remaining offset paths need stable ordering and validation. The new AI conversation message histories and storage object list feeds also require cursor-pagination evaluations and index coverage.

## 7. External Documentation Findings

### ASP.NET Core via Context7

- Rate limiting requires configured middleware and named policies applied to endpoints/resources.
- Authorization policies should encode concrete requirements such as roles/claims/resource checks, not just endpoint naming.
- Protected endpoints should explicitly require authorization in the pipeline/metadata.
- ProblemDetails is the standard ASP.NET Core mechanism for consistent HTTP API errors.

### EF Core via Context7

- EF Core supports named query filters, including separate soft-delete and multi-tenancy filters.
- Multi-tenancy query filters should compare rows to the current tenant identifier, not treat missing tenant as a broad match in runtime code.
- EF Core 10 named filters can be disabled selectively, which supports `SoftDelete` inclusion without disabling `Tenant`.
- Query performance work should include query shape, stable ordering, pagination design, and supporting indexes.

### MediatR via Context7

- Pipeline behaviors are registered through `AddMediatR` package configuration patterns and execute in registration order depending on registration.
- Open generic behaviors are the intended mechanism for cross-cutting logging, validation, performance, and authorization behavior.
- Direct handler-to-handler calls can bypass expected pipeline behavior; use `IMediator` deliberately or extract query services when behavior bypass is intentional.

### Tavily research synthesis

The external research reinforced the same priorities as the repo: thin API boundary, RFC 7807 error consistency, resource/policy authorization, tenant-safe EF filters, cancellation-token propagation, keyset pagination for large datasets, OpenTelemetry-style observability, auditability, idempotency, optimistic concurrency, and layered architecture test guardrails.

## 7A. Implementation Notes

- 2026-06-05: `EventAgendaItemController`, `EventSessionAgendaItemController`, and `LocationRoomController` create/update failures no longer return raw command envelopes or anonymous `{ error }` objects for 400 responses. They now return RFC7807 `ValidationProblemDetails` with `application/problem+json`, `code`, `traceId`, `timestamp`, and optional `correlationId` without changing Application handlers.
- 2026-06-05: `ActorKeyStoreController` and `UserAuthenticationTokenController` create/update validation failures now return RFC7807 `ValidationProblemDetails`. `ActorKeyStoreController` delete not-found returns RFC7807 `ProblemDetails` with a `resource_not_found` fallback code instead of an ad hoc `Problem(...)` branch.
- 2026-06-06: the temporary `Explore.API/Controllers/CommandResponseProblemDetails.cs` helper was removed as technical debt. The durable pattern now lives in `Explore.API/ExceptionHandling`: `ApiProblemCodes` centralizes fallback codes, `ApiProblemTypes` centralizes RFC9110 type URIs, `ApiValidationProblemDescriptor`/`ApiNotFoundProblemDescriptor` give controllers named resource-specific contracts, `ApiProblemFactory` owns RFC7807 payload creation and standard extensions, and `CommandResponseResultMapper` maps Application `BaseCommandResponse<T>`/simple bool outcomes into API `ActionResult`s. Controllers should add typed descriptors and call the mapper rather than building `ProblemDetails`, duplicating strings, or returning raw command envelopes.
- 2026-06-14: Phase 2 API error-catalog slice completed the initial `ApiProblemCodes` constants from `api-error-catalog.md`, expanded `ApiProblemTypes` to cover the initial RFC9110 status families, and normalized central writers so `GlobalExceptionHandler`, `ValidationExceptionHandler`, and `ApiValidationProblemDetailsFactory` attach stable `code` extensions. `ApiContractArchitectureTests` now guards catalog completeness and central writer code-extension emission. Verification passed with `Event.Architecture.Tests` (193 total, 192 succeeded, 1 skipped).
- 2026-06-14: Phase 2 controller-local validation cleanup normalized `EventDayController` to the shared API error-contract pattern used by agenda controllers. Create/update command failures now use `ToCommandValidationProblem(...)`, and route/body ID mismatch now uses `ToValidationProblem(...)`, so these paths return RFC7807 validation details with catalog-backed `validation_failed` codes and standard extensions. Verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 controller-local cleanup continued with `EventSeriesController` and `ActorSubscriptionController`. Event series create/update/not-found branches now use shared RFC7807 helpers instead of raw command envelopes or strings, and actor subscription subscribe/update/unsubscribe failures now use `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem`. Verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 tenant-storage settings cleanup added the shared `forbidden` ProblemDetails path (`ApiProblemFactory.CreateForbiddenProblem` and `CommandResponseResultMapper.ToForbiddenProblem`) and migrated `TenantStorageSettingsController` update failures to central helpers. Missing resolved user identity now returns catalog-backed `authentication_required`, administrator denial returns catalog-backed `forbidden`, and validation failures return RFC7807 validation details. Verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 taxonomy controller cleanup migrated `TagController`, `CategoryController`, and `LocationController` create/update validation failures and ID mismatch branches to the central RFC7807 validation helpers. This removes raw `BadRequest(response)` and anonymous `{ error }` ID-mismatch payloads from those taxonomy write endpoints; architecture verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 actor controller cleanup migrated `ActorController` create/update command failures to the central RFC7807 validation helper pattern, removing raw `BadRequest(response)` envelopes from actor write endpoints. Readback and AFT diagnostics were clean, but architecture verification is blocked because the existing `bash-2d49c6b2` test task could not be queried/killed through the bridge and a fresh bounded architecture run also timed out while launching.
- 2026-06-14: Phase 2 simple-controller cleanup migrated `SyncStateController` and `IndexedDidController` update ID-mismatch and command-failure branches to central RFC7807 validation helpers. This removes anonymous `{ error }` mismatch payloads and raw `BadRequest(response)` update failures from those endpoints; architecture verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 group-member cleanup migrated `GroupMemberController` add/update/delete command failures to the central RFC7807 validation helper pattern. Missing-user checks already used `ToAuthenticationRequiredProblem`; this slice removes raw command-envelope 400s from the mutating group-member endpoints. Architecture verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 group cleanup migrated `GroupController` create/update/approval/delete command failures to the central RFC7807 validation helper pattern. Missing-user checks already used `ToAuthenticationRequiredProblem`; this slice removes raw command-envelope 400s from the mutating group endpoints. Readback and AFT diagnostics were clean, but architecture verification is blocked because the shell bridge timed out when inspecting the existing architecture-test task and when launching a fresh bounded architecture run.
- 2026-06-14: Phase 2 tenant-role-grant cleanup migrated `TenantUserRoleGrantController` create failures to the central RFC7807 validation helper pattern. The controller now uses a tenant-user-role-grant validation descriptor plus `ToCommandValidationProblem` instead of returning the raw command envelope on validation failure. Verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 managed-provider cleanup migrated `ManagedProviderProvisioningController` denial and validation paths to central RFC7807 helpers. Instance-admin denial now returns catalog-backed `forbidden` through `ToForbiddenProblem`, and provisioning command failures now use `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem`. Verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 tenant-onboarding cleanup migrated `TenantOnboardingController` complete/update-settings/save-step error paths to central RFC7807 helpers. Missing resolved user identity now uses `ToAuthenticationRequiredProblem`, tenant-admin denials use `ToForbiddenProblem`, and validation failures use operation-specific descriptors plus `ToCommandValidationProblem`. Verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 tenant-settings document cleanup migrated `TenantSettingsDocumentsController` branding replacement failure paths to central RFC7807 helpers. Missing documents now return `resource_not_found` ProblemDetails through `ToNotFoundProblem`, and replacement validation failures use `ToCommandValidationProblem`; readback and AFT diagnostics were clean, but architecture verification is blocked because the shell bridge timed out while checking the prior architecture task and launching a fresh bounded run.
- 2026-06-14: Phase 2 settings cleanup migrated the shared `SettingsController` `BaseCommandResponse<Guid>` helper to central RFC7807 mappers. Single-setting, reset, lock, and unlock failures now use `ToCommandValidationProblem`, and administrator denials use `ToForbiddenProblem`; batch DTO failures are intentionally left for a later result-contract slice. Verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 template-sync cleanup migrated `EventTemplateSyncController` and `EventSessionTemplateSyncController` diff failure paths to central RFC7807 helpers. Failed diff commands now use sync-specific descriptors plus `ToCommandValidationProblem`, and unexpected null diff payloads use `ToValidationProblem` instead of raw command envelopes. Readback and AFT diagnostics were clean, but architecture verification is blocked because `bash-df9852dc` could not be inspected through the shell bridge.
- 2026-06-14: Phase 2 event-template cleanup migrated `EventTemplateController` update ID mismatch from anonymous `{ error }` JSON to central RFC7807 validation details through `ApiValidationProblemDescriptor` plus `ToValidationProblem`. Create/update command failures already use the quota-aware mapper and were left unchanged. Readback and AFT diagnostics were clean, but architecture verification is blocked because the existing architecture-test task could not be inspected and a fresh bounded architecture run timed out through the shell bridge before returning a task id.
- 2026-06-14: Phase 2 organization cleanup migrated `OrganizationController` create/update/delete and missing-identity failures to central RFC7807 helpers. Missing resolved user identity now returns `authentication_required` via `ToAuthenticationRequiredProblem`; create/update failures use organization-specific validation descriptors plus `ToCommandValidationProblem`; delete failures use `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem`. Verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 user-external-login cleanup migrated `UserExternalLoginController` create failures to the central RFC7807 validation helper pattern. The create path now uses a `userExternalLogin` `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem` instead of returning the raw command envelope; the older update/not-found controller-local helpers remain follow-up consolidation debt. Verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 tenant controller cleanup migrated `TenantController` create/update and tenant-navigation create/update/delete/reorder error paths to central RFC7807 helpers. Tenant and navigation validation failures now use resource-specific descriptors plus `ToCommandValidationProblem`, ID mismatches use `ToValidationProblem`, and delete-navigation not-found uses `ToNotFoundProblem`; architecture verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 localization admin cleanup migrated `LocalizationAdminController` TMS connection test, governance update, and TMS export failures to central RFC7807 helpers. Each operation now uses a localization-specific validation descriptor plus `ToCommandValidationProblem`; architecture verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 footer controller cleanup migrated `FooterController` tenant footer link-group/link/settings write failures to central RFC7807 helpers. Missing current-user identity now uses `ToAuthenticationRequiredProblem`; link-group, link, reorder, and settings command failures use operation-specific descriptors plus `ToCommandValidationProblem`. Readback and AFT diagnostics were clean, but architecture verification is blocked because `bash-a6ec862c` could not be inspected through the shell bridge.
- 2026-06-14: Phase 2 contact-share consent cleanup migrated `ContactShareConsentController` missing current-user, unresolved recipient-organization, and export failure paths to central RFC7807 helpers. Missing identity now returns catalog-backed `authentication_required`, unresolved recipient organizations use `ToNotFoundProblem`, and export failures use `ToCommandValidationProblem`; architecture verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 UI theme admin cleanup migrated `UiThemeAdminController` create/update validation, ID-mismatch, and scope-denial paths to central RFC7807 helpers. Validation failures now use `ToCommandValidationProblem`, ID mismatch uses `ToValidationProblem`, and denied platform/tenant scope writes use `ToForbiddenProblem`; architecture verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 storage-object cleanup migrated authenticated upload-session missing-auth checks and storage-object update failures to central RFC7807 helpers. Upload-session create/content/cancel now use `ToAuthenticationRequiredProblem` for missing authentication, update ID mismatch uses `ToValidationProblem`, and update command failure uses `ToCommandValidationProblem`; architecture verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 settings batch cleanup migrated the remaining `SettingsController` user and tenant batch update failures from raw `BadRequest(result)` DTO envelopes to the existing `settings` validation descriptor plus `ToValidationProblem`. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- 2026-06-14: Phase 2 event-team cleanup migrated `EventTeamController` missing current-user checks to the shared `authentication_required` RFC7807 helper. Current-user permissions, assignable presets, role assignment, and revocation now use `ToAuthenticationRequiredProblem` instead of raw `Unauthorized()` responses; architecture verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 user controller cleanup migrated `UserController` local auth, forbidden, sync, and ID-mismatch responses to central RFC7807 helpers. Missing provider subjects and unresolved current users now return catalog-backed `authentication_required`, cross-user organization reads return catalog-backed `forbidden`, sync command failures use `ToCommandValidationProblem`, and update ID mismatch uses `ToValidationProblem`; architecture verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 event controller cleanup migrated `EventController.GetMyEvents` missing-identity handling to the shared `authentication_required` RFC7807 helper. The current-user events endpoint now uses `ToAuthenticationRequiredProblem` instead of raw `Unauthorized()`; per user direction, verification for this slice was limited to readback and AFT diagnostics.
- 2026-06-14: Phase 2 custom-property projection cleanup migrated selected `CustomPropertyProjectionAdminController` command-failure branches to central RFC7807 helpers. Single-event rebuild, dirty-scope drain, and single-session rebuild failures now use resource-specific validation descriptors plus `ToCommandValidationProblem`; architecture verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: User direction changed the continuation loop: stop launching redundant test suites and focus on implementation progress. For subsequent scoped Phase 2 controller-normalization slices, use direct readback and AFT diagnostics, and defer broad build/test verification to the later verification phase.
- 2026-06-14: Phase 2 AI assistant cleanup migrated the model-discovery authorization and validation branches to central RFC7807 helpers. Tenant/platform-admin denial now returns catalog-backed `forbidden`, and missing/invalid OpenAI-compatible endpoint inputs return validation ProblemDetails through the `aiAssistantModelDiscovery` descriptor. Provider gateway failures still use the existing 502 helper and remain a later gateway-error mapping slice. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- 2026-06-14: Phase 2 AI assistant gateway cleanup migrated model-discovery provider JSON/HTTP/timeout failures to a central 502 ProblemDetails path. `ToBadGatewayProblem` now uses `ApiProblemFactory.CreateBadGatewayProblem`, `ApiProblemTypes.BadGateway`, and `ApiProblemCodes.ProviderGateway`, removing the controller-local ad hoc `CreateProblem` helper from `AiAssistantController`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 module controller cleanup migrated the remaining controller-local `Problem(...)` calls in `ModuleController` to central RFC7807 helpers. Missing module schema now uses `ToNotFoundProblem`, and module enable/disable failures use module-scoped validation descriptors with `ToValidationProblem`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 event-registration cleanup removed `EventRegistrationController`'s controller-local ProblemDetails mapper. Create payload validation, update ID mismatch, update command failures, missing current-user identity, and registration not-found outcomes now use the central validation/authentication/not-found helpers; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 taxonomy not-found cleanup migrated `TagController`, `CategoryController`, and `LocationController` detail-read missing-resource branches from bare `NotFound()` responses to resource-specific not-found descriptors with `ToNotFoundProblem`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 schedule not-found cleanup migrated `EventAgendaItemController` and `EventDayController` read-side missing-resource branches to central RFC7807 not-found helpers. Agenda item detail, agenda projection, and event day detail now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem` instead of bare `NotFound()`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 session-language cleanup migrated `EventSessionLanguageController` create/delete missing-resource branches to central RFC7807 not-found helpers. Missing event sessions and event-session-language rows now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 location-room cleanup migrated `LocationRoomController` room detail missing-resource handling to the central RFC7807 not-found helper. Missing rooms now use a location-room `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 actor-subscription cleanup migrated `ActorSubscriptionController` actor-subscription detail missing-resource handling to the central RFC7807 not-found helper. Missing actor subscriptions now use an actor-subscription `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 group detail cleanup migrated `GroupController` group detail missing-resource handling to the central RFC7807 not-found helper. Missing groups now use a group `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 group-member/program-section cleanup migrated `GroupMemberController` member detail and `EventSessionGroupController` program-section detail missing-resource handling to central RFC7807 not-found helpers. Missing group members and event-session groups now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 tenant-role/member/indexed-DID cleanup migrated `TenantUserRoleGrantController`, `OrganizationMemberController`, and `IndexedDidController` missing-resource handling to central RFC7807 not-found helpers. Tenant role-grant detail/revoke, organization-member detail, and indexed DID detail now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 custom-property definition detail cleanup migrated the shared, event-local, and event-session-local custom-property definition detail endpoints to central RFC7807 not-found helpers. Missing definitions in `CustomPropertyDefinitionController`, `EventCustomPropertyController`, and `EventSessionCustomPropertyController` now use definition-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 template/session detail cleanup migrated event template, event-session template, and event-session detail endpoints to central RFC7807 not-found helpers. Missing templates and sessions now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 actor/organization detail cleanup migrated actor-by-id, actor-by-DID, and organization detail missing-resource handling to central RFC7807 not-found helpers. Missing actors and organizations now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 storage/settings/theme not-found cleanup migrated storage metadata/content/public image, tenant branding document, and UI theme detail/delete missing-resource handling to central RFC7807 not-found helpers. Missing storage objects, tenant branding documents, and UI themes now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 remaining broad not-found cleanup migrated the last bare `NotFound()` cluster found by direct controller grep. `ExternalApiKeyController`, `AuthContextProbeController`, `AiAssistantController`, and `EventController` now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem` for missing external API keys, disabled auth probes, missing AI conversations/runs, and missing event session-create/program/detail/calendar resources; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 final raw-return cleanup migrated the remaining raw controller-return cluster found by direct grep. `EventController` delete and `OrganizationMemberController` invitation email checks now use `ToAuthenticationRequiredProblem`; event publish concurrency now uses a central conflict helper; `InstanceOnboardingController` setup-secret validation after completion uses a central `Gone` RFC7807 helper with `setup_already_completed` code; authorization policy package download failures use a central service-unavailable helper; and `AnalyticsRelayController` rejected relay events use a validation descriptor with `analytics_relay_rejected`. A direct controller grep no longer finds raw `BadRequest`, `Unauthorized`, `Forbid`, `NotFound`, `Conflict`, `StatusCode`, or `Problem` calls; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 local ProblemDetails cleanup consolidated `ExternalApiKeyController` onto central RFC7807 descriptors and mapper helpers. The controller no longer defines local validation/not-found code constants or hand-builds `ProblemDetails`/`ValidationProblemDetails`; create/update validation, ID mismatch, and not-found outcomes now use API-layer descriptors with `ToCommandValidationProblem`, `ToValidationProblem`, and `ToNotFoundProblem`. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: Phase 2 local ProblemDetails cleanup continued with `UserExternalLoginController`. The controller no longer defines local validation/not-found code constants or hand-builds `ProblemDetails`/`ValidationProblemDetails`; create/update validation, ID mismatch, and not-found outcomes now use API-layer descriptors with `ToCommandValidationProblem`, `ToValidationProblem`, and `ToNotFoundProblem`. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: Phase 2 local ProblemDetails cleanup continued with `AtprotoRecordController`. Update ID mismatch and update command failures now use an ATProto-record validation descriptor with `ToValidationProblem` and `ToCommandValidationProblem`; the controller-local validation code constant and hand-built `ValidationProblemDetails` helper were removed. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: Phase 2 local ProblemDetails cleanup continued with `NotificationController`. Archive and snooze not-found outcomes now use a notification `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem`; the controller-local hand-built `ProblemDetails`/`application/problem+json` helper was removed. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: Phase 2 local ProblemDetails cleanup consolidated the remaining controller-local projection-status helper in `CustomPropertyProjectionAdminController`. Event and session projection status lookup failures now use resource-specific validation descriptors plus `ToCommandValidationProblem`; the hand-built projection `ValidationProblemDetails` helper was removed. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: Phase 2 local ProblemDetails cleanup consolidated `EventSessionController`, `EventSessionGroupController`, and `EventSessionLanguageController` away from the shared `ProgramValidationProblemDetails` helper. Program/session validation failures now use controller-local `ApiValidationProblemDescriptor` values plus central `ToCommandValidationProblem`/`ToValidationProblem`; `ProgramValidationProblemDetails.cs` was deleted and `ToProgramValidationProblem` has no references. Per user direction, verification was limited to readback and AFT diagnostics.
- 2026-06-14: Phase 2 local ProblemDetails cleanup moved storage upload-session failure mapping out of `StorageUploadProblemDetails.cs` and into `CommandResponseResultMapper`/`ApiProblemFactory`. `StorageObjectController` still calls `ToStorageUploadProblem`, but that extension now lives in the central mapper and preserves the storage-specific 404/409/413/422/503 response mapping; the controller-local helper file was deleted. Per user direction, verification was limited to readback, direct grep, and AFT diagnostics.
- 2026-06-14: Phase 2 local ProblemDetails cleanup consolidated the remaining email dispatch and AI assistant local ProblemDetails paths. `EmailDispatchAdminController` now delegates status and command failures to `CommandResponseResultMapper.ToEmailDispatchValidationProblem`/`ToEmailDispatchProblem`; `AiAssistantProblemDetails.cs` was deleted, and AI command failures now use central `ToAiAssistantProblem` while preserving provider, rate-limit, quota, status, and quota-extension metadata mappings. A controller grep for local ProblemDetails construction now returns no matches; verification was limited to readback, direct grep, and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 route-template cleanup normalized the obvious action-word approval-status routes in `OrganizationController` and `GroupController`. `updatestatustype/{id:guid}` is now `{id:guid}/approval-status` for both resources while preserving the existing `RouteNames.UpdateOrganizationApprovalStatus`/`UpdateGroupApprovalStatus` constants; direct organization integration tests were updated, and generated OpenAPI/client artifacts remain pending regeneration.
- 2026-06-14: Phase 2 response-metadata cleanup began on the same approval-status endpoints. `OrganizationController.UpdateApprovalStatus` now advertises ProblemDetails payloads for 401/403/404, and `GroupController.UpdateApprovalStatus` advertises `ValidationProblemDetails` for 400 plus ProblemDetails payloads for 401/403/404, aligning existing controller behavior with OpenAPI metadata without changing handler flow.
- 2026-06-14: Phase 2 response-metadata cleanup was extended across the rest of `OrganizationController` and `GroupController` list/detail/create/update/delete endpoints. Bare 400 metadata now advertises `ValidationProblemDetails`, bare authentication/authorization/not-found metadata now advertises `ProblemDetails`, and `GroupController.Delete` now declares its `BaseCommandResponse<Guid>` success payload.
- 2026-06-14: Phase 2 response-metadata cleanup was extended to `EventSeriesController` and `EventSessionAgendaItemController`. `EventSeriesController` success responses now advertise concrete DTO/command-response payloads, validation failures advertise `ValidationProblemDetails`, not-found failures advertise `ProblemDetails`, and the agenda-item list validation metadata now advertises `ValidationProblemDetails` instead of a bare 400.
- 2026-06-14: Phase 2 response-metadata cleanup tightened two single-endpoint gaps. `AnalyticsRelayController.Relay` and `CustomPropertyGovernanceController.GetGovernanceReport` now advertise `ValidationProblemDetails` for their existing 400 validation responses instead of bare status-only metadata.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `LocationController` and `LocationRoomController` metadata without changing action behavior. Location list/create/update validation paths now advertise `ValidationProblemDetails`, and location/location-room detail/update/delete auth and not-found paths advertise `ProblemDetails`, matching the controllers' existing central RFC7807 helper flow.
- 2026-06-14: Phase 2 response-metadata cleanup tightened seven read-only lookup detail endpoints without changing behavior. `GroupPositionController`, `FileTypeController`, `EventStatusController`, `AudienceGenderController`, `AudienceAgeController`, `VisibilityTypeController`, and `ActorTypeController` now advertise `ProblemDetails` for their existing 404 metadata instead of bare status-only responses.
- 2026-06-14: Phase 2 response-metadata cleanup tightened three additional detail-endpoint 404 metadata entries without changing behavior. `DidCustodyTypeController`, `RoleController`, and `ActorKeyStoreController.GetById` now advertise `ProblemDetails` for their existing 404 metadata instead of bare status-only responses.
- 2026-06-14: Phase 2 response-metadata cleanup tightened three more read-only lookup detail endpoints without changing behavior. `RegistrationModeController`, `OrganizationPositionController`, and `MadhabController` now advertise `ProblemDetails` for their existing 404 metadata instead of bare status-only responses.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `UserAuthenticationTokenController`, `UserExternalLoginController`, and `EventTeamController` metadata without changing action behavior. Existing not-found metadata now advertises `ProblemDetails`, external-login create validation advertises `ValidationProblemDetails`, and event-team missing-auth metadata advertises `ProblemDetails` to match central RFC7807 helper usage.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `TenantOnboardingController` and `ModuleController` metadata without changing action behavior. Tenant onboarding command validation paths now advertise `ValidationProblemDetails`, tenant admin denial paths advertise `ProblemDetails`, module schema not-found metadata advertises `ProblemDetails`, and module enable/disable validation/auth/forbidden metadata now matches central RFC7807 helper usage.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `ExternalApiKeyController`, `TenantUserRoleGrantController`, and `ManagedProviderProvisioningController` metadata without changing action behavior. Existing not-found and forbidden metadata now advertises `ProblemDetails`, and command validation metadata now advertises `ValidationProblemDetails`, matching the controllers' central RFC7807 helper usage.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `TagController`, `CategoryController`, and `OrganizationMemberController` metadata without changing action behavior. Taxonomy validation paths now advertise `ValidationProblemDetails`, taxonomy authentication and not-found metadata advertises `ProblemDetails`, and organization-member detail not-found metadata now matches central RFC7807 helper usage.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `EventTemplateController` and `EventSessionTemplateController` metadata without changing action behavior. Template list/create/update validation paths now advertise `ValidationProblemDetails`, authentication and not-found metadata advertises `ProblemDetails`, and existing event-template quota responses continue to advertise `ProblemDetails` 422.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `EventSessionLanguageController`, `EventSessionGroupController`, and `EventAgendaItemController` metadata without changing action behavior. Existing authentication and not-found metadata now advertises `ProblemDetails`, while validation metadata remains `ValidationProblemDetails`, matching the controllers' central RFC7807 helper usage.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `EventSessionController` and `EventDayController` metadata without changing action behavior. Event-session authentication/not-found metadata now advertises `ProblemDetails`; event-day create/update validation metadata now advertises `ValidationProblemDetails`, and event-day authentication/not-found metadata now advertises `ProblemDetails`.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `CustomPropertyDefinitionController`, `EventCustomPropertyController`, and `EventSessionCustomPropertyController` metadata without changing action behavior. Custom-property list/create/update/purge/value validation metadata now advertises `ValidationProblemDetails`; authentication, authorization, and not-found metadata now advertises `ProblemDetails`; existing quota/conflict responses remain typed as `ProblemDetails`.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `TenantStorageSettingsController`, `ActorSubscriptionController`, and `TenantSettingsDocumentsController` metadata without changing action behavior. Tenant storage and actor-subscription command validation now advertises `ValidationProblemDetails`; tenant branding document authentication/authorization/not-found metadata now advertises `ProblemDetails`; existing conflict metadata remains typed as `ProblemDetails`.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `GroupMemberController` and `ContactShareConsentController` metadata without changing action behavior. Group-member authentication/not-found metadata now advertises `ProblemDetails`; contact-share withdraw authentication metadata advertises `ProblemDetails`; shared-contact list/export validation metadata advertises `ValidationProblemDetails`.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `EventFormatController` and `EventTemplateSyncController` metadata without changing action behavior. Event-format detail not-found metadata now advertises `ProblemDetails`, and template-sync diff/apply/history authentication and authorization metadata now advertises `ProblemDetails` while existing validation/not-found/conflict metadata remains typed.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `SettingsController` and `StorageObjectController` metadata without changing action behavior. Settings validation metadata now advertises `ValidationProblemDetails`, settings authentication/authorization metadata advertises `ProblemDetails`, and storage list/upload/object validation plus authentication/not-found metadata now advertises the central RFC7807 payload types.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `EventSessionTemplateSyncController` and `UiThemeAdminController` metadata without changing action behavior. Event-session template sync authentication and authorization metadata now advertises `ProblemDetails`; UI theme admin validation metadata now advertises `ValidationProblemDetails`, and authentication/authorization/not-found metadata now advertises `ProblemDetails`.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `NotificationController` metadata without changing action behavior. Notification list validation metadata now advertises `ValidationProblemDetails`, and existing authentication/not-found metadata now advertises `ProblemDetails`, including the read, archive, snooze, delete, unread-count, and refresh-stream endpoints.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `ActorController` metadata without changing action behavior. Actor list and tenant-list validation metadata now advertises `ValidationProblemDetails`; actor detail/DID not-found metadata and create/update/delete authentication, authorization, and not-found metadata now advertises `ProblemDetails`.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `LocalizationAdminController` and `EventRegistrationController` metadata without changing action behavior. Localization test/governance/export validation metadata now advertises `ValidationProblemDetails`, localization admin authentication metadata advertises `ProblemDetails`, and event-registration list/create validation plus authentication/not-found metadata now advertises central RFC7807 payload types.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `CustomPropertyProjectionAdminController` metadata without changing action behavior. Projection rebuild, single-rebuild, dirty-scope, and projection lookup validation metadata now advertises `ValidationProblemDetails`, while existing quota responses remain typed as `ProblemDetails` 422.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `FooterController` metadata without changing action behavior. Footer link-group, link, reorder, and tenant footer settings validation metadata now advertises `ValidationProblemDetails`, while existing link-group/link not-found metadata now advertises `ProblemDetails`.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `InstanceOnboardingController` command response metadata without changing action behavior. Onboarding completion, auth-provider save/bootstrap, authorization-provider save/sync, and endpoint verification validation failures now advertise `ValidationProblemDetails` instead of command envelopes.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `TenantController` metadata without changing action behavior. Tenant create/update and tenant-navigation create/update/reorder validation metadata now advertises `ValidationProblemDetails`; tenant and navigation-link not-found metadata now advertises `ProblemDetails` to match central RFC7807 helper usage.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `UserAppearanceController` metadata without changing action behavior. Appearance profile creation validation now advertises `ValidationProblemDetails`; existing authenticated/not-found metadata for appearance reads, profile actions, palette generation, archiving, and duplication now advertises `ProblemDetails`.
- 2026-06-14: Phase 2 response-metadata cleanup tightened `EventController` metadata without changing action behavior. Event list/auth, creation, publish, draft/status update, delete, calendar/program/detail, readiness, and Islamic/tech aspect endpoints now advertise `ValidationProblemDetails` for validation failures and `ProblemDetails` for authentication, authorization, not-found, and publish-conflict failures.
- 2026-06-15: Phase 2 OpenAPI enum-schema cleanup fixed the string-enum catalog root cause. `OpenApiStringEnumSchemaCatalog` now includes the DTO-exposed Domain enum set plus Application DTO/model enums surfaced by the guard (`SettingSource`, `CustomPropertyFilterOperator`, `ManagedProviderOrganizerKindDto`, `BatchUpdateMode`, public-experience CTA/home-block enums, `PublicExperienceMode`, `PublicExperiencePrimaryOrganizationState`, and `KeycloakBootstrapMode`). `ApiContractArchitectureTests.DtoEnumProperties_MustBeRegisteredIn_OpenApiStringEnumSchemaCatalog` scans public DTO contract enum properties and excludes validator implementation types so future catalog omissions fail architecture tests before OpenAPI drifts from the API JSON string-enum contract. Verification: scoped AFT diagnostics were clean; project-level `Event.Architecture.Tests` no longer reports the enum-catalog test as failing, but still exits non-zero because of pre-existing agent-context manifest/dead-link failures under `.claude/`.
- 2026-06-15: Phase 2 response-metadata cleanup completed the deferred `InstanceSettingsController` metadata pass without changing action behavior. Instance admin/setup-secret forbidden paths now advertise `ProblemDetails`, while settings, authentication, storage, provider, authorization, and deployment command-validation paths now advertise `ValidationProblemDetails` instead of command envelopes. Focused and broad controller greps for the stale 4xx/command-envelope metadata pattern now return no matches under `Explore.API/Controllers`.
- 2026-06-15: Phase 2 route-template and preview-contract documentation was closed for the current inventory. The only confirmed action-word route risk was the organization/group approval-status route already normalized to `{id:guid}/approval-status`; generated OpenAPI/client regeneration remains pending for that recorded contract change. `docs/API.md` now makes the pre-v1.0 stance explicit: breaking API/HAL/OpenAPI contract changes may be accepted before v1.0, but every intentional break must be captured in `docs/API_CHANGELOG.md`, reflected through OpenAPI/inventory/generated-client workflows as applicable, and backed by contract-governance evidence; generated artifacts must not be hand-edited.
- 2026-06-15: Phase 2 metadata/classification review confirmed the response-metadata sweep is complete for the stale 4xx/command-envelope target pattern, endpoint classification is emitted to OpenAPI as `x-endpoint-class`, and mutating/admin auth alignment is guarded by `EndpointClassificationArchitectureTests`. Rate-limit, cache, and broad tenant-mode metadata are not yet OpenAPI-emitted contract metadata; they remain inventory/planning fields until a vendor-extension design, attribute/policy mapping, and tests are added.
- 2026-06-15: Phase 2 OpenAPI posture metadata was implemented in `EndpointClassificationTransformer`. OpenAPI operations now emit `x-rate-limit-policy`, `x-output-cache-policy`, and `x-tenant-mode` from `[EnableRateLimiting]`, `[OutputCache]`, and `[RequireMultiTenant]` endpoint metadata alongside the existing `x-endpoint-class`. `ContractInvariantsTests.OpenApiDocument_EndpointPostureExtensionsExposeRateCacheAndTenantMetadata` guards representative emission for `AiAssistant`, `ListData`, and `multi-tenant-required`. Verification: scoped AFT diagnostics were clean, and the integration test project list-only invocation built successfully with zero tests run; runtime integration execution remains deferred to the blocked verification item.
- 2026-06-15: Phase 2 verification status is partial/blocked rather than green. Scoped AFT diagnostics for the latest slices are clean, and `Event.Architecture.Tests` was run with supported project-level TUnit/MTP invocation during the OpenAPI enum-catalog slice; the enum-catalog guard passed, but the project still exits non-zero because of unrelated pre-existing `.claude` agent-context manifest/dead-link failures. API integration tests remain blocked by known Docker/Testcontainers and host-shutdown/OpenFeature instability, while full build/Blazor verification remains blocked by the pre-existing Razor syntax error in `Explore.Blazor.Client/Components/Shell/AiAssistant/AiActionResultCard.razor`.
- 2026-06-15: Phase 2 missing-identity authorization response cleanup is closed. Controllers now use the catalog-backed `authentication_required` ProblemDetails helper for missing supported user identifiers, focused grep found no raw `Unauthorized(...)`, `Forbid()`, or `"User ID not found"` controller returns, and `ApiContractArchitectureTests.Controllers_MustNotReturn_RawMissingUserUnauthorizedStrings` guards the original anti-pattern. The old bridge-timeout verification blocker is superseded by the later supported architecture run; this guard is not among the remaining unrelated `.claude` context failures.
- 2026-06-15: Phase 2 reverse `RouteNames` cleanup began by reading the skipped `RouteNames_EveryConstantResolvesToExactlyOneEndpoint` guard and classifying candidate orphan constants. Truly unused catalog entries were removed from `RouteNames.cs`, including legacy lookup aliases, unused approval-status/event-type CRUD constants, stale user and organization-member CRUD names, the stale current-user appearance update name, and unused event-team ownership/window constants. Constants still referenced by HATEOAS policies but missing registered endpoints remain unresolved; those require reattaching endpoints or removing stale affordances before the reverse coverage test can be unskipped.
- 2026-06-15: Phase 2 reverse `RouteNames` cleanup continued by fixing stale policy-used names instead of deleting HAL affordances blindly. ATProto links now target registered `*AtprotoRecordEntry` routes and the unsupported `by-uri` link was removed because no endpoint exists. Event-registration links now target registered `*EventRegistration` route names. The obsolete ATProto and registration aliases were removed from `RouteNames.cs`; focused grep found no remaining references, and AFT diagnostics were clean. Other policy-used/no-endpoint candidates remain to classify before the reverse coverage test can be unskipped.
- 2026-06-15: Phase 2 reverse `RouteNames` cleanup removed unsupported arbitrary-user and tenant-by-slug HAL affordances. User, tenant-user-role, organization-review, organization-member, group-member, event-registration, and tenant policies no longer emit links backed by missing `GetUserById`, `GetUsers`, `UpdateUser`, or `GetTenantBySlug` endpoints; supported current-user, actor, user organization/registration, tenant-by-id, tenant collection, and tenant create links remain. The obsolete constants were removed from `RouteNames.cs`; focused grep found no remaining references, and AFT diagnostics were clean. Other policy-used/no-endpoint candidates remain to classify before the reverse coverage test can be unskipped.
- 2026-06-15: Phase 2 reverse `RouteNames` cleanup removed unsupported relationship/review HAL affordances that had no registered endpoints. Organization, actor, event, category, tag, and organization-review policies no longer emit links backed by missing `GetOrganizationEvents`, `GetActorEvents`, `GetEventCategories`, `GetEventTags`, `GetCategoryChildren`, `GetCategoryEvents`, `GetTagEvents`, `GetTagTagTypes`, `GetOrganizationReviewById`, `UpdateOrganizationReview`, or `DeleteOrganizationReview` routes. The obsolete constants were removed from `RouteNames.cs`; focused grep found no remaining references, and AFT diagnostics were clean. Other policy-used/no-endpoint candidates remain to classify before the reverse coverage test can be unskipped.
- 2026-06-15: Phase 2 reverse `RouteNames` cleanup retargeted or removed the remaining event-session, organization-member, and user-registration affordance drift. `EventSessionLinkPolicy` no longer emits the unsupported `GetEventSessionSpeakers` link, `OrganizationLinkPolicy` now targets registered `GetOrganizationMembersByOrganization` with `organizationId`, and `UserLinkPolicy` now targets registered `GetRegistrationsByUser`. The obsolete `GetEventSessionSpeakers`, `GetOrganizationMembers`, and `GetUserRegistrations` constants were removed from `RouteNames.cs`; focused grep found no remaining references, and AFT diagnostics were clean. Other policy-used/no-endpoint candidates remain to classify before the reverse coverage test can be unskipped.
- 2026-06-15: Phase 2 reverse `RouteNames` cleanup closed after the final read-only RouteNames classification reported 452/452 constants backed by exactly one registered endpoint with no missing or duplicate route names. `RouteNames_EveryConstantResolvesToExactlyOneEndpoint` is now unskipped, so reverse constant-to-endpoint coverage joins the already-active endpoint-to-constant guardrail. Full API integration execution remains deferred to the blocked verification item.
- 2026-06-15: Phase 2 implementation-side exit gate is confirmed with explicit verification caveats. RFC7807 controller cleanup, route-template cleanup, bidirectional route-name coverage, response metadata, endpoint classification/auth alignment, OpenAPI string-enum catalog coverage, and OpenAPI rate/cache/tenant posture extensions are implemented with clean scoped diagnostics. Runtime/API integration and full build verification remain blocked by pre-existing environment/context issues, so Phase 3 decomposition can proceed only with those blockers carried forward and without claiming full-plan completion.
- 2026-06-14: Phase 2 custom-property projection lookup cleanup migrated event/session projection lookup failures in `CustomPropertyProjectionAdminController` to central RFC7807 helpers. Event and session lookup failures now use dedicated validation descriptors plus `ToCommandValidationProblem`; readback caught and corrected an initial descriptor mix-up before the slice was recorded. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- 2026-06-14: Phase 2 instance-onboarding cleanup migrated completion and setup-secret command failure paths in `InstanceOnboardingController` to central RFC7807 helpers. Missing resolved user identity now returns `authentication_required`, launch preflight blockers use `ToValidationProblem`, and complete/auth-provider/keycloak-bootstrap/authorization-provider/policy-sync/endpoint-verify failures use operation-specific descriptors plus `ToCommandValidationProblem`. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- 2026-06-14: Phase 2 event controller cleanup continued past `GetMyEvents` by migrating event create, publish, draft update, status update, Islamic aspect upsert, and tech aspect upsert failures to central RFC7807 helpers. Operation-specific validation descriptors now drive `ToCommandValidationProblem`, and event-not-found aspect failures use a shared not-found descriptor with `ToNotFoundProblem`; publish concurrency conflicts remain a later conflict-mapping slice. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- 2026-06-14: Phase 2 instance settings cleanup migrated the repeated admin/setup/identity helper paths in `InstanceSettingsController` to central RFC7807 helpers. Setup-or-instance-admin denials now use `ToForbiddenProblem`, unresolved current users use `ToAuthenticationRequiredProblem`, deployment-mode operator-control refusal uses an `instanceDeploymentMode` validation descriptor with `DeploymentModeChangeRequiresOperatorConfiguration`, and `HandleCommandResponse` now maps command failures through `ToCommandValidationProblem`. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- 2026-06-14: Phase 2 organization-member cleanup removed the remaining controller-local unauthorized helper in `OrganizationMemberController`. Member add, role update, invitation accept/decline, and delete now return catalog-backed `authentication_required` ProblemDetails through `ToAuthenticationRequiredProblem`; verification was limited to readback and AFT diagnostics per user direction.
- 2026-06-14: Phase 2 custom-property definition cleanup migrated shared, event-local, and event-session-local update ID mismatches in `CustomPropertyDefinitionController`, `EventCustomPropertyController`, and `EventSessionCustomPropertyController` to central RFC7807 validation helpers. Each controller now uses a resource-specific `ApiValidationProblemDescriptor` with `ToValidationProblem`; purge fallback branches remain separate result-contract work. Architecture verification passed with `Event.Architecture.Tests` (194 total, 193 succeeded, 1 skipped).
- 2026-06-14: Phase 2 custom-property purge cleanup migrated the shared, event-local, and event-session-local purge fallback branches in the same controller family to central RFC7807 helpers. Known not-found purge outcomes now use `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem`, and other purge failures use `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem`; readback confirmed the targeted raw `NotFound(result)`/`BadRequest(result)` branches are gone. Architecture verification is currently blocked because `bash-3ad2ba99` could not be inspected through the shell bridge.
- 2026-06-14: Phase 2 controller-authentication-error slice added a shared `authentication_required` ProblemDetails path through `ApiProblemFactory.CreateAuthenticationRequiredProblem` and `CommandResponseResultMapper.ToAuthenticationRequiredProblem`. `GroupController` and `GroupMemberController` now return catalog-backed RFC7807 401 responses for missing supported user-id claims instead of raw string `Unauthorized(...)` responses, and `ApiContractArchitectureTests.Controllers_MustNotReturn_RawMissingUserUnauthorizedStrings` guards against reintroducing that exact anti-pattern. Verification is currently blocked because the shell/tool bridge timed out while starting and inspecting `Event.Architecture.Tests`; rerun the architecture project when the bridge is responsive.
- 2026-06-13: Phase 1P first implementation slice stopped anonymous access to identity-bearing registration/member/grant reads by requiring authentication and removing shared `OutputCache` from `EventRegistrationController` list/detail/by-session/by-user reads, `TenantUserRoleGrantController` list/detail reads, and `OrganizationMemberController` list/detail reads. The DTOs still contain full identity metadata for authenticated callers, so public-safe DTO splitting and handler/resource authorization remain follow-up work.
- 2026-06-13: `FooterController` write endpoints now fail closed when the authenticated principal lacks a supported user identifier claim. The slice removed `CurrentUserId ?? Guid.Empty` from link-group, link, reorder, and settings writes and returns a 401 ProblemDetails through a controller-local helper. `Footer.Manage` policy/resource metadata and handler-level authorization remain follow-up work.
- 2026-06-13: Footer tenant-scoped write commands now carry Application-layer resource authorization metadata. `Create/Update/Delete/ReorderFooterLinkGroup*`, `Create/Update/DeleteFooterLink*`, and `UpdateTenantFooterSettingsCommand` use `[AuthorizeResource(ResourceKinds.Tenant, AuthorizationActions.Update)]` and implement `ISecureRequest`, matching the existing tenant-governed catalog pattern used by event templates and shared custom-property definitions. `UpdateFooterGovernanceSettingsCommand` was intentionally left unchanged for the later instance-settings governance slice. Application unit tests passed 1,349/1,349 with the new metadata guard.
- 2026-06-14: Tenant query-filter review confirmed the active `ExploreDbContext` runtime filters are already fail-closed: tenant-scoped filters compare rows to `TenantFilterTenantId` and only broaden through explicit bypass state, while `PersistenceTenantFilterArchitectureTests` prevents the old `TenantContext == null ||` broad-read pattern. No production filter behavior change was made in this slice; the active docs now mark Phase 1B's permissive-filter replacement as complete and leave richer execution-mode/logging semantics for Phase 1C.
- 2026-06-14: Phase 1C bypass quarantine gained a controller-boundary guardrail. `PersistenceTenantFilterArchitectureTests.ApiControllers_ShouldNotCall_TenantFilterBypassHelpersDirectly` now scans `Explore.API/Controllers` and fails if controllers call EF tenant-filter bypass helpers or reference `TenantFilterBypassReasons`; cross-tenant reads must flow through reviewed Application/Persistence services instead of HTTP boundary code. Verification passed with `Event.Architecture.Tests` (191 total, 190 succeeded, 1 skipped) and `Event.Persistence.IntegrationTests` (155/155).
- 2026-06-14: Phase 1C semantic bypass proof started with `TenantSettingsDocumentRepository`. `TenantSettingsDocumentRepository_WhenAmbientTenantDiffers_ReturnsOnlyExplicitTenantDocument` now covers the case where the ambient DbContext tenant differs from the explicitly requested tenant after `IgnoreTenantFilter(TenantScopedRepositoryExactTenantPredicate)`: the repository must still return only the explicit tenant document. Verification is blocked in the current environment because `Event.Persistence.IntegrationTests` cannot connect to Docker/Testcontainers at `unix:///home/amir/.docker/desktop/docker.sock`; earlier tenant-isolation integration coverage passed before Docker became unavailable, but this new test still needs a Docker-backed rerun.
- 2026-06-14: Phase 1D template sync policy replacement removed the `template_admin` authentication-only policy from API registration and from `EventTemplateSyncController`/`EventSessionTemplateSyncController`. The controllers now provide only coarse `[Authorize]`, while MediatR request metadata enforces `ResourceKinds.CustomPropertyTemplate` actions: diff/apply already used `SyncDiff`/`SyncApply`, and sync-history queries now use `CustomPropertyTemplates.View` with `ISecureRequest`. The corresponding HAL `sync-history` links now require the same view permission.
- 2026-06-14: Phase 1D unused placeholder cleanup removed the remaining auth-only registrations for `event_editor`, `property_governance_admin`, and `platform_namespace_editor` after repo-wide C# search found no active `[Authorize(Policy = ...)]` usage. Stale comments on custom-property projection/governance code were corrected to reference explicit resource metadata instead of the removed placeholder policy; future event/governance/platform namespace capabilities must be introduced through full resource/action parity rather than login-only API policies.
- 2026-06-13: Blazor HAL affordance drift was hardened defensively. `MyEvents` no longer uses organization-role inference for edit/delete/publish/registrations affordances and removed unconditional create CTAs; `MyOrganizations` removed unconditional create CTAs and uses a HAL rel check for per-organization event creation; `EventTemplateListPage` no longer shows create on an empty list without a surfaced affordance. Collection-level HAL link surfacing and explicit rels for publish/registrations/create-event remain follow-up work.
- 2026-06-13 verification: `Event.Architecture.Tests` passed (190 total, 189 succeeded, 1 skipped). `dotnet build` and `Explore.Blazor.Client.Tests` are blocked by a pre-existing Razor syntax error in `Explore.Blazor.Client/Components/Shell/AiAssistant/AiActionResultCard.razor` line 26. `Event.API.IntegrationTests` is blocked by Docker/Testcontainers unavailability; the run also exposed stale anonymous-OK assertions in `LinkTableControllerTests`, which were updated to expect 401 for the newly protected reads.

## 8. Implementation Risks

- Tenant fail-closed changes can break tests or seed/design-time flows that accidentally rely on `TenantContext == null`; introduce explicit execution modes rather than reintroducing permissive filters.
- System/background context can become an unsafe replacement for `IgnoreAllFilters()` unless it is reason-coded, logged, restricted, and tested.
- Authorization hardening can expose intentionally-public endpoints that were never documented; classify endpoints before changing attributes.
- Bootstrap/admin hardening can lock out self-hosted deployments if first-admin behavior is not defined and tested before policy changes land.
- Controller decomposition can break HATEOAS links if route names are not migrated atomically with tests.
- Error normalization can expose client assumptions about old string errors; development-mode compatibility waiver permits breaking changes, but docs/API changelog must be updated.
- Idempotency and concurrency changes can affect command response contracts; model duplicate and conflict errors explicitly.
- Cursor pagination may require indexes, filter-hash binding, and response contract changes; coordinate with OpenAPI contract work.
- Database index changes need migration/model assertions so performance fixes do not drift.

## 9. Verification Commands to Prefer

Use targeted project checks:

```bash
dotnet build --configuration Release --verbosity quiet
dotnet test --project Event.Architecture.Tests/Event.Architecture.Tests.csproj --configuration Release --verbosity quiet
dotnet test --project Event.API.IntegrationTests/Event.API.IntegrationTests.csproj --configuration Release --verbosity quiet
dotnet test --project Event.Application.UnitTests/Event.Application.UnitTests.csproj --configuration Release --verbosity quiet
dotnet test --project Event.Persistence.IntegrationTests/Event.Persistence.IntegrationTests.csproj --configuration Release --verbosity quiet
```

Avoid solution-level `dotnet test` unless repo policy changes.
