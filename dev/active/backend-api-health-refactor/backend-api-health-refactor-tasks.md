<!-- ABOUTME: Executable task checklist for the backend/API health refactor workstream. -->
<!-- ABOUTME: Breaks the refactor into verifiable backend/API implementation slices. -->

# Backend API Health Refactor Tasks

Last Updated: 2026-06-13 Europe/Brussels

## Status Summary — 2026-06-13 Audit Reprioritization

- Security/data exposure outranks broad refactoring. Start with anonymous identity/membership/role endpoints, footer write authorization, and HAL affordance drift.
- Existing Phase 0 artifacts remain valid but must be updated with the audit rows before behavior-changing work resumes.
- Code implementation must not begin until the exact endpoint classification, policy matrix rows, risk-register rows, and verification tests for each P0 are recorded.

## Phase 0A — Mandatory Inventory Only

- [x] Create `dev/active/backend-api-health-refactor/endpoint-inventory.md`.
- [x] Populate endpoint inventory columns: `Endpoint`, `Method`, `Route`, `RouteName`, `Current Auth`, `Target Classification`, `AuthClassification`, `TenantMode`, `RateLimitPolicy`, `CachePolicy`, `HAL Links`, `OpenAPI OperationId`, `Risk`, `Action`. Include the 10 AI Assistant, 12 Storage Object, 2 Tenant Storage Settings, and 5 Email Dispatch endpoints.
- [x] Create `dev/active/backend-api-health-refactor/endpoint-classification.md`.
- [ ] Classify each endpoint as Public, Authenticated, Admin, host-admin, setup/bootstrap, or system/background. Ensure background job trigger boundaries are explicitly cataloged.
- [ ] Mark every anonymous endpoint as intentional, suspicious, or queued for hardening. P0 audit targets must be marked suspicious until fixed: `EventRegistration` reads, `TenantUserRoleGrant` reads, and `OrganizationMember` reads.
- [x] Create `dev/active/backend-api-health-refactor/backend-contract-risk-register.md`.
- [ ] Add a risk-register row for every known breaking API, auth, route, response, HAL, OpenAPI, pagination, and tenant-behavior change. Include R-016 through R-031 audit rows before code changes.
- [x] Create `dev/active/backend-api-health-refactor/authorization-policy-matrix.md`.
- [ ] Fill policy matrix columns: `Resource`, `Action`, `API Policy`, `Handler Attribute`, `Cerbos Resource`, `HAL Rel`, `Default Roles`. Expand to cover registration reads/manage, tenant role-grant reads/manage, organization member reads/manage, footer management, AI conversations, upload sessions, storage overrides, and email outbox administration.
- [x] Create `dev/active/backend-api-health-refactor/tenant-execution-model.md`.
- [x] Define execution modes: `RuntimeTenantRequired`, `RuntimeTenantOptionalPublicRead`, `HostAdministration`, `BackgroundSystem`, `MigrationOrSeeding`, `DesignTime`.
- [x] Define absent-tenant behavior for each endpoint category and execution mode.
- [x] Create `dev/active/backend-api-health-refactor/api-error-catalog.md`.
- [x] Define initial typed problem codes: `validation_failed`, `tenant_required`, `resource_not_found`, `resource_conflict`, `forbidden`, `authentication_required`, `concurrency_conflict`, `duplicate_request`.

## Phase 0B — Non-Invasive Guardrails

- [x] Add/enable endpoint-to-`RouteNames` coverage for named endpoints (including the new AI, Storage, and Email dispatch routes); keep the reverse constant-to-endpoint test skipped until legacy orphaned constants are pruned or reattached.
- [x] Add architecture test: controllers must not inject repositories directly.
- [x] Add architecture test: API actions must not return Domain entities.
- [x] Add architecture test: API controllers must not call raw tenant-filter bypass helpers.
- [x] Add architecture test: write endpoints require auth metadata or documented exception.
- [x] Add architecture test: endpoint classification matches auth metadata.
- [x] Add skipped architecture test inventory for current missing baseline response metadata, linked to R-005.
- [x] Add architecture test: actions with existing response metadata must declare a 2xx success response.
- [ ] Enable architecture test: every action declares baseline response metadata.
- [ ] Add architecture test: every action declares relevant ProblemDetails error responses.
- [ ] Add architecture/integration guardrail: anonymous endpoints cannot return DTOs containing user email, user full name, user id, role, membership, grant, revocation, or invitation metadata unless explicitly allowlisted in endpoint classification and risk register.
- [ ] Add Blazor client architecture guardrail or targeted tests: edit/delete/publish/create action affordances are gated by HAL `_links`, not roles/claims/local organization role helpers or empty-list fallbacks.
- [x] Add architecture test: privileged API policies cannot be authentication-only unless intentionally named as authenticated-only. Ensure AI, Storage, and Email admin controller endpoints are covered.
- [x] Add architecture test: tenant filter bypass APIs require approved naming/attributes/tests.
- [x] Add test inventory for current ProblemDetails shape and required extensions.
- [ ] Add tenant-filter tests for present tenant, absent runtime tenant, explicit host-admin context, background system context (such as RabbitMQ consumers and TickerQ jobs), migration/seeding, and design-time context.
- [x] If any guardrail initially fails or remains skipped, link the skip/failure reason to `backend-contract-risk-register.md` and an implementation task.


## Phase 1P — P0 Data Exposure and HAL/Auth Contract Fixes

- [x] Reclassify `EventRegistrationController` anonymous list/detail/by-session/by-user reads. 2026-06-13 slice changed the identity-bearing reads to authenticated endpoints and removed shared output caching. Split genuinely public registration aggregate data from identity-bearing registration data remains follow-up work.
- [~] Protect or redesign registration DTOs so anonymous responses never include `UserId`, `UserFullName`, `UserEmail`, user PII, or tenant/member identity unless explicitly approved and tested. Anonymous exposure is stopped; authenticated DTO splitting/public-safe projections remain pending.
- [x] Reclassify `TenantUserRoleGrantController` read endpoints from anonymous public reads. 2026-06-13 slice removed action-level `[AllowAnonymous]`, changed endpoint classification to authenticated, and removed shared output caching; admin/resource policy refinement remains pending.
- [x] Reclassify `OrganizationMemberController` member-list/detail reads from anonymous public reads. 2026-06-13 slice removed action-level `[AllowAnonymous]`, changed endpoint classification to authenticated, and removed shared output caching; organization-scoped resource policy refinement remains pending.
- [x] Add API integration tests for all three P0 endpoint families proving anonymous access fails or returns only approved public fields. Added anonymous-denied matrix tests for event registrations, tenant role grants, and organization members; full API run is currently blocked by local Docker/Testcontainers availability.
- [~] Add OpenAPI/endpoint-inventory updates for changed response DTOs, auth metadata, cache variance, and generated-client impact. 2026-06-13 continuation manually corrected the active endpoint inventory/classification for EventRegistration and OrganizationMember P0 reads and recorded the TenantUserRoleGrant inventory regeneration gap; generated OpenAPI/client metadata still needs regeneration.
- [~] Harden `FooterController` writes: remove `CurrentUserId ?? Guid.Empty`, fail closed on missing actor identity, add footer policy/resource metadata, and align handlers with `ISecureRequest`/`[AuthorizeResource]` or the chosen authorization convention. 2026-06-13 first slice completed fail-closed actor identity. 2026-06-13 continuation added tenant-scoped `[AuthorizeResource(ResourceKinds.Tenant, AuthorizationActions.Update)]` plus `ISecureRequest` metadata to tenant footer link-group/link/settings commands; instance footer governance and richer tenant-setting lock-aware authorization remain follow-up work.
- [~] Fix Blazor HAL affordance drift in `MyEvents`, `MyOrganizations`, and `EventTemplateListPage`: action buttons must use HAL `_links`; the empty-template-list create fallback must not show create without a collection-level affordance or an explicitly modeled API capability. 2026-06-13 slice removed local role gating and unsafe empty-list/create fallbacks; collection-level HAL surfacing and explicit rels for publish/registrations/create-event remain pending.
- [~] Run API authorization tests and Blazor client architecture/tests for the touched affordance paths. `Event.Architecture.Tests` passed; API integration tests are blocked by Docker/Testcontainers after stale anonymous-OK assertions were updated; build and Blazor client tests are blocked by a pre-existing `AiActionResultCard.razor` syntax error.

## Phase 1A — Tenant Execution Model

- [ ] Implement explicit tenant execution mode representation in the appropriate Persistence/Infrastructure boundary. 2026-06-14 tenant-filter review confirmed this is not required before preserving fail-closed runtime filters; keep it for richer bypass logging/scope semantics.
- [x] Ensure `RuntimeTenantOptionalPublicRead` cannot query all tenant-scoped rows by default. 2026-06-14 verification found active EF filters compare tenant-scoped rows to `TenantFilterTenantId`; missing tenant context yields no tenant rows unless an explicit bypass reason is enabled.
- [~] Define allowed call sites for `HostAdministration`, `BackgroundSystem` (RabbitMQ background consumer threads, TickerQ outbox drains, reminder triggers), `MigrationOrSeeding` (TickerQ DbContext migrations), and `DesignTime`. Seeding/design-time lanes are already explicit; richer background/host-admin mode taxonomy remains pending.
- [ ] Add structured logging fields for tenant execution mode, operation name, reason, actor user ID when available, tenant ID when available, and correlation ID.
- [x] Add tests for absent-tenant behavior: no rows, 400, 401, 403, startup/config failure, or explicit system path as appropriate. `TenantQueryFilterFailClosedTests` already proves absent tenant returns no tenant-scoped rows and explicit bypass returns rows; endpoint-level 401/403 cases remain covered per endpoint slices.

## Phase 1B — Query Filter Fail-Closed Implementation

- [x] Replace permissive `TenantContext == null || ...` runtime query-filter semantics. 2026-06-14 review found the active `ExploreDbContext.QueryFilters.cs` already uses fail-closed tenant equality (`e.TenantId == TenantFilterTenantId`) plus explicit bypass state; architecture guard prevents the old null-broadening pattern from returning.
- [~] Preserve migration/seeding/design-time behavior through explicit execution modes rather than null checks. Current behavior is preserved through design-time construction plus explicit seeding bypass/clear; a formal execution-mode abstraction remains deferred to Phase 1C if richer audit/logging is needed.
- [x] Verify pooled `ExploreDbContext` custom state is reset or scoped safely. 2026-06-14 review confirmed `PersistenceServicesRegistration` clears tenant bypass state before property-injecting scoped tenant/current-user services when a pooled context is leased. `ApiTickerQDbContext` remains a separate follow-up if custom state is added there.
- [~] Add persistence integration tests for tenant-scoped entities under present tenant, absent runtime tenant, host-admin, and background modes. Existing `TenantQueryFilterFailClosedTests` covers present tenant, absent tenant, and explicit bypass; 2026-06-14 `Event.Persistence.IntegrationTests` passed 155/155. Host-admin/background execution-mode-specific cases remain pending until the mode abstraction exists.

## Phase 1C — Filter Bypass Quarantine

- [~] Restrict `IgnoreTenantFilter()` usage to explicitly named cross-tenant/system services. 2026-06-14 added the first semantic bypass proof for `TenantSettingsDocumentRepository.GetByTenantAndDocumentKey`: an integration test now creates two tenants with the same branding document key, sets the ambient EF tenant to tenant B, requests tenant A explicitly, and asserts tenant A's document is returned rather than leaking tenant B. Additional bypass call sites still need the same semantic proof.
- [~] Remove or quarantine `IgnoreAllFilters()` from normal runtime paths. 2026-06-14 search found no production runtime `IgnoreAllFilters(` call sites outside `QueryFilterExtensions`; the remaining known use is integration-test coverage for full bypass behavior. Keep this partially open until CI enforces the guardrail and each bypass reason is reviewed.
- [ ] Introduce reason enum/API such as `BeginSystemTenantScope(SystemTenantScopeReason reason, string operationName, Guid? actorUserId = null)` if this is the chosen design. The current reason-string API remains in place; the 2026-06-14 work added guardrails/tests rather than changing runtime scope semantics.
- [ ] Add audit logging for every tenant-filter bypass and cross-tenant admin read. No audit emission was added in the 2026-06-14 semantic-proof slice.
- [x] Add tests proving controllers cannot call raw tenant-filter bypass helpers. 2026-06-14 added `ApiControllers_ShouldNotCall_TenantFilterBypassHelpersDirectly` to `PersistenceTenantFilterArchitectureTests`, blocking `IgnoreTenantFilter`, `IgnoreAllFilters`, raw `IgnoreQueryFilters`, `EnableTenantFilterBypass`, and direct `TenantFilterBypassReasons` usage from API controllers. `Event.Architecture.Tests` passed 191 total, 190 succeeded, 1 skipped.

## Phase 1D — Authorization Policy Replacement

- [~] Replace `template_admin` with resource-backed template-sync authorization. 2026-06-14 removed the `template_admin` auth-only policy registration and replaced the two template sync controllers with coarse `[Authorize]`; diff/apply requests already use `ResourceKinds.CustomPropertyTemplate` `SyncDiff`/`SyncApply`, and history queries now use `CustomPropertyTemplates.View` plus `ISecureRequest`. A broader `Templates.Manage` policy family remains deferred until a real template resource/action parity slice is added.
- [~] Replace `event_editor` with capability policies such as `Events.Edit` and `Events.Publish` where appropriate. 2026-06-14 cleanup found no active `[Authorize(Policy = "event_editor")]` usage, so the unused auth-only registration was removed instead of preserving a privileged login-only policy. Concrete event edit/publish capability mapping remains pending where endpoint inventory identifies active gaps.
- [~] Replace `property_governance_admin` with `CustomProperties.Govern`. 2026-06-14 cleanup found no active policy usage; projection/governance request types already carry explicit resource metadata, stale comments were corrected, and the unused auth-only registration was removed. Remaining work is endpoint-by-endpoint parity for governance/admin surfaces.
- [~] Replace `platform_namespace_editor` with `PlatformNamespaces.Edit`. 2026-06-14 cleanup found no active policy usage, so the unused auth-only registration was removed; a concrete platform-namespace capability family remains future work if inventory finds active namespace edit endpoints.
- [ ] Add policies for `Modules.Manage`, `StorageObjects.ReadPresigned`, and `TenantSettings.Manage` where inventory requires them.
- [ ] Add new policies for:
  - `EventRegistrations.ReadOwn` / `EventRegistrations.ReadForEvent` / `EventRegistrations.Manage` on registration reads and mutations
  - `TenantUserRoleGrants.Read` / `TenantUserRoleGrants.Manage` on tenant role-grant reads and mutations
  - `OrganizationMembers.Read` / `OrganizationMembers.Manage` on organization member reads and mutations
  - [~] Footer link group/link/settings writes. 2026-06-13 continuation validated that no `ResourceKinds.Footer` or `AuthorizationActions.Manage` exists yet, so tenant-scoped footer writes now use the existing tenant-governed catalog convention: `[AuthorizeResource(ResourceKinds.Tenant, AuthorizationActions.Update)]` with `ISecureRequest`. A distinct `Footer.Manage` resource family remains deferred until parity work adds resource kind, action, policies, schemas, fallback, scopes, and tests.
  - `AiAssistant.Interact` / `AiAssistant.Manage` on `AiAssistantController`
  - `StorageObjects.Read` / `StorageObjects.Write` on `StorageObjectController`
  - `TenantStorage.Manage` on `TenantStorageSettingsController`
  - `Email.Dispatch` / `Email.Manage` on `EmailDispatchAdminController`
  - `InstanceSettings.Manage` on storage and Keycloak sync configurations
- [~] Map every changed API policy to `[AuthorizeResource]`/`ISecureRequest`, Cerbos resource/action, HAL rel, and default roles in `authorization-policy-matrix.md`. Footer tenant-write mapping, template sync custom-property-template mapping, and removal of unused auth-only placeholder registrations are documented; remaining rows still need full endpoint-by-endpoint parity.
- [ ] Audit `ModuleController`, template controllers, property governance endpoints, namespace endpoints, storage/presigned endpoints, and tenant settings endpoints for privileged requirements.
- [ ] Add/extend authorization parity tests for changed resources.

## Phase 1E — Self-Hosted Bootstrap/Admin Verification

- [x] Define how the first platform/tenant admin is created in self-hosted deployments. 2026-06-14: `complete` requires authenticated identity plus setup secret; SingleTenant also grants default tenant admin, MultiTenant grants platform admin only.
- [x] Decide whether setup secret, Keycloak group/role mapping, or both are authoritative during bootstrap. 2026-06-14: setup secret is bootstrap-only; it is never an identity or post-bootstrap admin authority. Provider claims/resource authorization are authoritative after activation.
- [ ] Integrate Keycloak client secret rotation and backup-confirmed sync flows.
- [x] Define how bootstrap mode is disabled after completion. 2026-06-14: setup-secret-protected endpoints return `410 Gone` after completion; safe status/sanitized public reads remain explicit exceptions.
- [~] Define audit events for bootstrap start, completion, failure, and disablement. 2026-06-14 taxonomy documented in `tenant-execution-model.md`; emission implementation and tests remain.
- [x] Define SingleTenant and MultiTenant differences. 2026-06-14: SingleTenant grants default tenant admin/branding defaults; MultiTenant grants platform admin only and requires explicit per-tenant admin provisioning later.
- [~] Define behavior when auth provider configuration is missing. Setup-secret-only writes remain setup-time only; missing-provider typed ProblemDetails and recovery behavior need follow-up tests.
- [~] Add API/integration tests for bootstrap/admin setup behavior. 2026-06-14 added focused setup-secret authority tests and MultiTenant first-admin unit coverage. `Event.Application.UnitTests` passed 1356/1356. `Event.API.IntegrationTests` compiled and began execution but hung during repeated host shutdown/OpenFeature lifecycle cleanup and had to be killed after 219s; API integration verification remains environment-blocked until the shutdown hang is resolved.

## Phase 2 — API Contract, Error Catalog, and Result Mapping

- [x] Introduce `ApiProblemCodes` with stable typed codes from `api-error-catalog.md`. 2026-06-14: catalog constants now cover all initial codes: `validation_failed`, `tenant_required`, `authentication_required`, `forbidden`, `resource_not_found`, `resource_conflict`, `concurrency_conflict`, `duplicate_request`, `rate_limited`, and `unexpected_error`.
- [~] Introduce `ApiProblemFactory` with stable problem type/code/status mapping. 2026-06-14: central RFC9110 type constants were expanded and central exception/model-validation writers now attach catalog-backed `code` extensions. `CreateForbiddenProblem`/`ToForbiddenProblem` now cover catalog-backed 403 responses for controller authorization denials. AI Assistant, storage upload, and email outbox transition mappings remain follow-up.
- [ ] Introduce a shared ProblemDetails writer for middleware-generated errors.
- [~] Ensure every ProblemDetails response includes `traceId`, `timestamp`, and `correlationId` when available. 2026-06-14: `ValidationExceptionHandler` now adds `code`, `traceId`, `timestamp`, and optional `correlationId`; model-validation factory already emits standard extensions and now also emits `code`. Remaining ad hoc controller helpers still need cleanup.
- [ ] Introduce `CommandResponseResultMapper` for `BaseCommandResponse<T>`, bool deletes, conflicts, duplicate requests, not-found, validation failures, and concurrency conflicts.
- [ ] Replace raw `BadRequest(string)` and ad hoc `Problem(...)` in high-risk controllers (like `UserController`, `AiAssistantController`, `StorageObjectController`, `EmailDispatchAdminController`).
- Progress 2026-06-05: normalized `EmailDispatchAdminController` pause/resume failure paths to the existing RFC7807 email-dispatch ProblemDetails helper; broader controller cleanup remains pending.
- Progress 2026-06-05: normalized `ProgramValidationProblemDetails` helper responses to include standard `traceId`, `timestamp`, and optional `correlationId` extensions; broader controller/helper cleanup remains pending.
- Progress 2026-06-05: normalized `NotificationController` archive/snooze not-found branches from anonymous `{ error }` JSON to RFC7807 ProblemDetails with `code`, `traceId`, `timestamp`, and optional `correlationId`; broader controller cleanup remains pending.
- Progress 2026-06-05: normalized `CustomPropertyProjectionAdminController` event/session status-query failure paths from raw `BadRequest(result)` envelopes to RFC7807 `ValidationProblemDetails` with `code`, `traceId`, `timestamp`, and optional `correlationId`; broader projection command cleanup remains pending.
- Progress 2026-06-05: normalized `ExternalApiKeyController` create/update validation and update not-found failure paths from raw command envelopes/anonymous objects to RFC7807 ProblemDetails with `code`, `traceId`, `timestamp`, and optional `correlationId`; broader controller cleanup remains pending.
- Progress 2026-06-05: normalized `AtprotoRecordController` update validation failures from anonymous/raw 400 responses to RFC7807 `ValidationProblemDetails` with `code`, `traceId`, `timestamp`, and optional `correlationId`; broader ATProto controller cleanup remains pending.
- Progress 2026-06-05: normalized `UserExternalLoginController` update validation and not-found failure paths from anonymous/raw command responses to RFC7807 ProblemDetails with `code`, `traceId`, `timestamp`, and optional `correlationId`; broader account-linking controller cleanup remains pending.
- Progress 2026-06-05: normalized `EventRegistrationController` update validation and not-found failure paths from raw `Problem(...)`/`BadRequest(response)` branches to RFC7807 ProblemDetails with `code`, `traceId`, `timestamp`, and optional `correlationId`; broader registration create/delete cleanup remains pending.
- Progress 2026-06-05: normalized `EventAgendaItemController`, `EventSessionAgendaItemController`, and `LocationRoomController` create/update validation failures from raw `BaseCommandResponse`/anonymous 400 payloads to RFC7807 `ValidationProblemDetails` with `validation_failed` fallback code, `traceId`, `timestamp`, and optional `correlationId`.
- Progress 2026-06-05: normalized `ActorKeyStoreController` and `UserAuthenticationTokenController` create/update validation failures from raw command envelopes/anonymous or ad hoc `Problem(...)` 400 responses to RFC7807 `ValidationProblemDetails`; `ActorKeyStoreController` delete not-found now returns RFC7807 `ProblemDetails` with `resource_not_found` fallback code.
- Progress 2026-06-06: replaced the temporary controller-scoped `CommandResponseProblemDetails` helper with API-layer error-contract components under `Explore.API/ExceptionHandling`: `ApiProblemCodes`, `ApiProblemTypes`, `ApiValidationProblemDescriptor`/`ApiNotFoundProblemDescriptor`, `ApiProblemFactory`, and `CommandResponseResultMapper`. This keeps controllers thin, centralizes RFC7807 standard extensions/content type/type URIs, and establishes the planned factory/mapper direction; broader mappings for conflicts, duplicate requests, concurrency, middleware errors, AI Assistant, storage upload, and email outbox states remain pending.
- Progress 2026-06-14: completed the initial `ApiProblemCodes` catalog, expanded `ApiProblemTypes`, and added architecture guardrails requiring central ProblemDetails writers to emit stable `code` extensions. `GlobalExceptionHandler`, `ValidationExceptionHandler`, and `ApiValidationProblemDetailsFactory` now attach catalog-backed codes for their central paths. `Event.Architecture.Tests` passed 193 total, 192 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `EventDayController` create/update validation failures and ID mismatch from raw `BadRequest(...)` payloads to central RFC7807 `ValidationProblemDetails` via `ApiValidationProblemDescriptor`, `ToCommandValidationProblem`, and `ToValidationProblem`. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `EventSeriesController` create/update validation failures, update ID mismatch, and update/delete not-found branches from raw `BadRequest(...)`/`NotFound(response)` payloads to central RFC7807 helpers (`ToCommandValidationProblem`, `ToValidationProblem`, and `ToNotFoundProblem`). `ActorSubscriptionController` subscribe/update/unsubscribe failures now use `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem` instead of raw `BadRequest(response)`. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped after these controller cleanups.
- Progress 2026-06-14: normalized `TenantStorageSettingsController` update failures: unresolved current-user identity now returns `authentication_required` via `ToAuthenticationRequiredProblem`, administrator denial now returns `forbidden` via `ToForbiddenProblem`, and remaining command validation failures use `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem`. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized taxonomy write validation in `TagController`, `CategoryController`, and `LocationController`. Create/update command failures now use `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem`, and route/body ID mismatches now use `ToValidationProblem` instead of anonymous `{ error }` JSON. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `ActorController` create/update command failures from raw `BadRequest(response)` envelopes to `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem`. Readback and AFT diagnostics were clean, but architecture verification is blocked because the existing `bash-2d49c6b2` test task could not be queried/killed through the bridge and a fresh bounded architecture run also timed out while launching.
- Progress 2026-06-14: normalized `SyncStateController` and `IndexedDidController` update ID-mismatch and command-failure branches to central RFC7807 validation helpers (`ToValidationProblem` and `ToCommandValidationProblem`). `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `GroupMemberController` add/update/delete command failures from raw `BadRequest(response)` envelopes to resource-specific `ApiValidationProblemDescriptor` values plus `ToCommandValidationProblem`. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `GroupController` create/update/approval/delete command failures from raw `BadRequest(response)` envelopes to resource-specific `ApiValidationProblemDescriptor` values plus `ToCommandValidationProblem`. Readback and AFT diagnostics were clean, but architecture verification is blocked because the existing architecture-test task could not be inspected through the shell bridge and a fresh bounded architecture run timed out while launching.
- Progress 2026-06-14: normalized `TenantUserRoleGrantController` create failure from a raw `BadRequest(response)` envelope to `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem`. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `ManagedProviderProvisioningController` instance-admin denial and provisioning command failures to central RFC7807 helpers. Non-admin callers now receive catalog-backed `forbidden` via `ToForbiddenProblem`, and command validation failures use `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem`. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `TenantOnboardingController` complete/update-settings/save-step failure paths to the central RFC7807 helpers. Missing resolved user identity now returns `authentication_required` via `ToAuthenticationRequiredProblem`, tenant-admin denials return catalog-backed `forbidden` via `ToForbiddenProblem`, and remaining command failures use operation-specific `ApiValidationProblemDescriptor` values plus `ToCommandValidationProblem`. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `TenantSettingsDocumentsController` branding replacement failures. Missing branding documents now use `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem`, and replacement validation failures use `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem` instead of raw `NotFound(response)`/`BadRequest(response)` envelopes. Readback and AFT diagnostics were clean, but architecture verification is blocked because the architecture-test status request and a fresh bounded launch both timed out through the shell bridge.
- Progress 2026-06-14: normalized the shared `SettingsController` `BaseCommandResponse<Guid>` helper. Tenant/user single-setting, reset, lock, and unlock failures now use `ToCommandValidationProblem`; administrator denials now use catalog-backed `forbidden` via `ToForbiddenProblem`. Batch DTO failure mapping remains a later result-contract slice. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: completed the remaining `SettingsController` batch DTO cleanup. User and tenant batch update failures now use the existing `settings` validation descriptor plus `ToValidationProblem` instead of raw `BadRequest(result)` DTO envelopes. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized `EventTemplateSyncController` and `EventSessionTemplateSyncController` diff failure branches from raw `BadRequest(response)` envelopes to central RFC7807 validation helpers. Failed diff commands now use sync-specific `ApiValidationProblemDescriptor` values plus `ToCommandValidationProblem`, and unexpected null diff payloads fail closed through `ToValidationProblem`. Readback and AFT diagnostics were clean, but architecture verification is blocked because the running `bash-df9852dc` architecture-test task could not be inspected through the shell bridge.
- Progress 2026-06-14: normalized `EventTemplateController` update ID mismatch from anonymous `{ error }` JSON to central RFC7807 validation details through `ApiValidationProblemDescriptor` plus `ToValidationProblem`. Create/update command failures already use the quota-aware mapper and were left unchanged. Readback and AFT diagnostics were clean, but architecture verification is blocked because the existing `bash-9aacb3b8` architecture-test task could not be inspected and a fresh bounded architecture run timed out through the shell bridge before returning a task id.
- Progress 2026-06-14: normalized shared, event-local, and event-session-local custom-property definition update ID mismatches in `CustomPropertyDefinitionController`, `EventCustomPropertyController`, and `EventSessionCustomPropertyController` from anonymous `{ error }` JSON to central RFC7807 validation details through resource-specific `ApiValidationProblemDescriptor` values plus `ToValidationProblem`. Purge fallback branches remain a separate result-contract slice. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized the corresponding shared, event-local, and event-session-local custom-property purge fallback branches. Known not-found purge outcomes now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem`, and remaining purge validation failures use `ApiValidationProblemDescriptor` values plus `ToCommandValidationProblem` instead of raw `NotFound(result)`/`BadRequest(result)` command envelopes. Readback confirmed no raw purge fallback branches remain in the three target controllers; architecture verification is blocked because the running `bash-3ad2ba99` task could not be inspected through the shell bridge.
- Progress 2026-06-14: normalized `OrganizationController` create/update/delete and missing-identity failure paths. Missing resolved user identity now uses `ToAuthenticationRequiredProblem`, create/update command failures use organization-specific `ApiValidationProblemDescriptor` values plus `ToCommandValidationProblem`, and delete failures use `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem` instead of raw command envelopes or a controller-local unauthorized helper. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `UserExternalLoginController` create failures from raw `BadRequest(response)` envelopes to the central RFC7807 validation helper. The create path now uses `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem`; existing update/not-found local helpers remain a later consolidation target. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `TenantController` create/update and tenant-navigation create/update/delete/reorder failures to central RFC7807 helpers. Tenant create/update and navigation create/update/reorder failures now use resource-specific `ApiValidationProblemDescriptor` values plus `ToCommandValidationProblem`; tenant/navigation ID mismatches use `ToValidationProblem`; delete-navigation not-found uses `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem`. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `LocalizationAdminController` TMS connection test, governance update, and TMS export failures from raw `BadRequest(result)` envelopes to central RFC7807 validation helpers. The controller now uses operation-specific `ApiValidationProblemDescriptor` values plus `ToCommandValidationProblem`; `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `FooterController` tenant footer link-group/link/settings write failures to central RFC7807 helpers. Missing current-user identity now uses `ToAuthenticationRequiredProblem`; link-group, link, reorder, and settings command failures use operation-specific `ApiValidationProblemDescriptor` values plus `ToCommandValidationProblem`. Readback and AFT diagnostics were clean, but architecture verification is blocked because the running `bash-a6ec862c` task could not be inspected through the shell bridge.
- Progress 2026-06-14: normalized `ContactShareConsentController` missing-identity, missing-recipient-organization, and export failure paths to central RFC7807 helpers. Missing current-user identity now uses `ToAuthenticationRequiredProblem`, unresolved recipient organizations use `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem`, and export command failures use `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem`. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `UiThemeAdminController` create/update validation and authorization-denial paths to central RFC7807 helpers. Create/update validation failures now use `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem`, route/payload ID mismatch uses `ToValidationProblem`, and scope-denial responses use catalog-backed `forbidden` via `ToForbiddenProblem`. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `StorageObjectController` authenticated upload-session and update failure paths. Upload-session create/content/cancel missing-auth checks now return catalog-backed `authentication_required` via `ToAuthenticationRequiredProblem`, update ID mismatch uses `ToValidationProblem`, and update command failures use `ApiValidationProblemDescriptor` plus `ToCommandValidationProblem`. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `EventTeamController` missing-user branches to the shared authentication ProblemDetails helper. Current-user permission, assignable-preset, assignment, and revocation paths now return catalog-backed `authentication_required` via `ToAuthenticationRequiredProblem` instead of raw `Unauthorized()` responses. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `UserController` sync/current-user/admin-authority/organization/update/delete local auth and validation responses to central RFC7807 helpers. Missing provider subjects and unresolved current users now use `ToAuthenticationRequiredProblem`, sync command failures use `ToCommandValidationProblem`, cross-user organization reads use `ToForbiddenProblem`, and update ID mismatch uses `ToValidationProblem`. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: normalized `EventController.GetMyEvents` missing-identity handling. The current-user events endpoint now returns catalog-backed `authentication_required` through `ToAuthenticationRequiredProblem` instead of raw `Unauthorized()`. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized selected `CustomPropertyProjectionAdminController` command failures. Single-event rebuild, dirty-scope drain, and single-session rebuild failures now use resource-specific `ApiValidationProblemDescriptor` values plus `ToCommandValidationProblem` instead of raw `BadRequest(result)` command envelopes. `Event.Architecture.Tests` passed 194 total, 193 succeeded, 1 skipped.
- Progress 2026-06-14: per user direction, subsequent Phase 2 controller-normalization slices stop launching redundant test suites. Continue using direct readback and AFT diagnostics for scoped edits, and leave full build/test verification to the later verification phase once the implementation plan has advanced further.
- Progress 2026-06-14: normalized the `AiAssistantController` model-discovery authorization and validation branches. Tenant/platform-admin denial now uses `ToForbiddenProblem`, while missing/invalid provider endpoint input and URI format failures use the `aiAssistantModelDiscovery` validation descriptor plus `ToValidationProblem`; provider gateway failures remain a later 502/gateway-error mapping slice. Per user direction, this slice was verified by readback and AFT diagnostics only, not by launching a test suite.
- Progress 2026-06-14: completed the `AiAssistantController` model-discovery gateway-error follow-up. Provider JSON/HTTP/timeout failures now use a central `ToBadGatewayProblem` helper backed by `ApiProblemFactory.CreateBadGatewayProblem`, `ApiProblemTypes.BadGateway`, and `ApiProblemCodes.ProviderGateway` instead of controller-local ad hoc 502 ProblemDetails. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized the remaining ad hoc `ModuleController` ProblemDetails responses. Missing module schema now uses `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem`, while module enable/disable failures use module-scoped validation descriptors plus `ToValidationProblem` instead of controller-local `Problem(...)` calls. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized `EventRegistrationController` create/update/delete failure mapping to central RFC7807 helpers. Missing request payload and ID mismatch now use event-registration validation descriptors, missing current user uses `ToAuthenticationRequiredProblem`, update command failures use `ToCommandValidationProblem`, and registration not-found outcomes use `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem`; the controller-local ProblemDetails builder was removed. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: continued taxonomy controller cleanup by normalizing `TagController`, `CategoryController`, and `LocationController` detail-read missing-resource branches. Each controller now uses a resource-specific `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem` instead of bare `NotFound()` for detail reads. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized schedule read not-found responses in `EventAgendaItemController` and `EventDayController`. Agenda item detail, agenda projection, and event day detail missing-resource branches now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem` instead of bare `NotFound()`; per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized `EventSessionLanguageController` missing session/language resources. Create/delete session lookups and delete language lookups now use event-session and event-session-language `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem` instead of bare `NotFound()`; per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized `LocationRoomController` room detail missing-resource handling. The room detail endpoint now uses a location-room `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem` instead of a bare `NotFound()`; per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized `ActorSubscriptionController` actor-subscription detail missing-resource handling. The actor subscription lookup endpoint now uses an actor-subscription `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem` instead of a bare `NotFound()`; per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized `GroupController` group detail missing-resource handling. The group detail endpoint now uses a group `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem` instead of a bare `NotFound()`; per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized `GroupMemberController` member detail and `EventSessionGroupController` program-section detail missing-resource handling. Both endpoints now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem` instead of bare `NotFound()` responses; per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized `TenantUserRoleGrantController`, `OrganizationMemberController`, and `IndexedDidController` missing-resource handling. Tenant role-grant detail/revoke, organization-member detail, and indexed DID detail now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem` instead of bare `NotFound()` responses; per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized shared, event-local, and event-session-local custom-property definition detail missing-resource handling. `CustomPropertyDefinitionController`, `EventCustomPropertyController`, and `EventSessionCustomPropertyController` now use definition-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem` instead of bare `NotFound()` responses; per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized event template, event-session template, and event-session detail missing-resource handling. `EventTemplateController`, `EventSessionTemplateController`, and `EventSessionController` now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem` instead of bare `NotFound()` responses; per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized `ActorController` and `OrganizationController` detail missing-resource handling. Actor-by-id, actor-by-DID, and organization detail endpoints now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem` instead of bare `NotFound()` responses; per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized `StorageObjectController`, `TenantSettingsDocumentsController`, and `UiThemeAdminController` missing-resource handling. Storage metadata/content/public image, tenant branding document, and UI theme detail/delete paths now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem` instead of bare `NotFound()` responses; per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized the remaining broad bare `NotFound()` cluster found by direct controller grep. `ExternalApiKeyController`, `AuthContextProbeController`, `AiAssistantController`, and `EventController` now use resource-specific `ApiNotFoundProblemDescriptor` values plus `ToNotFoundProblem` for missing external API keys, disabled auth probes, missing AI conversations/runs, and missing event session-create/program/detail/calendar resources; per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized the remaining raw controller-return cluster found by direct grep. `EventController` delete and `OrganizationMemberController` invitation email checks now use `ToAuthenticationRequiredProblem`; event publish concurrency now uses a central conflict helper; `InstanceOnboardingController` setup-secret validation after completion uses a central `Gone` RFC7807 helper with `setup_already_completed` code; authorization policy package download failures use a central service-unavailable helper; and `AnalyticsRelayController` rejected relay events use a validation descriptor with `analytics_relay_rejected`. A direct controller grep no longer finds raw `BadRequest`, `Unauthorized`, `Forbid`, `NotFound`, `Conflict`, `StatusCode`, or `Problem` calls; verification for this slice was limited to readback and AFT diagnostics per user direction.
- Progress 2026-06-14: consolidated `ExternalApiKeyController` away from its controller-local ProblemDetails mapper. Create/update validation, ID mismatch, and not-found outcomes now use central RFC7807 descriptors and mapper helpers; local validation/not-found code constants and hand-built `ProblemDetails`/`ValidationProblemDetails` helpers were removed. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: consolidated `UserExternalLoginController` away from its controller-local ProblemDetails mapper. Create/update validation, ID mismatch, and not-found outcomes now use central RFC7807 descriptors and mapper helpers; local validation/not-found code constants and hand-built `ProblemDetails`/`ValidationProblemDetails` helpers were removed. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: consolidated `AtprotoRecordController` away from its controller-local validation ProblemDetails mapper. Update ID mismatch and update command failures now use an ATProto-record validation descriptor with `ToValidationProblem` and `ToCommandValidationProblem`; the local validation code constant and hand-built `ValidationProblemDetails` helper were removed. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: consolidated `NotificationController` away from its controller-local notification not-found ProblemDetails builder. Archive and snooze not-found outcomes now use a notification `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem`; the local hand-built `ProblemDetails`/`application/problem+json` helper was removed. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: consolidated the remaining controller-local `CustomPropertyProjectionAdminController` projection-status ProblemDetails helper. Event and session projection status lookup failures now use resource-specific validation descriptors plus `ToCommandValidationProblem`; the hand-built projection `ValidationProblemDetails` helper was removed. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: consolidated `EventSessionController`, `EventSessionGroupController`, and `EventSessionLanguageController` away from the shared controller-local `ProgramValidationProblemDetails` helper. Program/session validation failures now use controller-local `ApiValidationProblemDescriptor` values plus central `ToCommandValidationProblem`/`ToValidationProblem`; `ProgramValidationProblemDetails.cs` was deleted and `ToProgramValidationProblem` has no references. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: moved storage upload-session failure mapping out of `Explore.API/Controllers/StorageUploadProblemDetails.cs` and into the central RFC7807 mapper/factory path. `CommandResponseResultMapper.ToStorageUploadProblem` now preserves storage-specific 404/409/413/422/503 mappings, `ApiProblemFactory.CreateProblem` emits standard extensions/content type, and the controller-local helper file was deleted. Per user direction, verification for this slice was limited to readback, direct grep, and AFT diagnostics.
- Progress 2026-06-14: consolidated the remaining controller-local email dispatch and AI assistant ProblemDetails construction. `EmailDispatchAdminController` no longer hand-builds or serializes ProblemDetails; email status and command failures now flow through `CommandResponseResultMapper.ToEmailDispatchValidationProblem` and `ToEmailDispatchProblem`. `AiAssistantProblemDetails.cs` was deleted; AI assistant command failures now flow through central `ToAiAssistantProblem`, preserving provider, rate-limit, quota, status, and quota-extension metadata mappings. A controller grep for local ProblemDetails construction now returns no matches; per user direction, verification for this slice was limited to readback, direct grep, and AFT diagnostics.
- Progress 2026-06-14: normalized the obvious action-word approval-status route templates in `OrganizationController` and `GroupController`. `updatestatustype/{id:guid}` is now `{id:guid}/approval-status` while preserving `RouteNames.UpdateOrganizationApprovalStatus` and `RouteNames.UpdateGroupApprovalStatus`; direct organization integration tests that hard-coded the old path were updated. Generated OpenAPI/client artifacts still require regeneration in the later contract workflow.
- Progress 2026-06-14: started the OpenAPI response-metadata pass on the same approval-status endpoints. `OrganizationController.UpdateApprovalStatus` now declares ProblemDetails payload types for 401/403/404, and `GroupController.UpdateApprovalStatus` now declares `ValidationProblemDetails` for validation failure plus ProblemDetails payload types for 401/403/404 instead of bare status-only error metadata.
- Progress 2026-06-14: extended the response-metadata pass across `OrganizationController` and `GroupController` list/detail/create/update/delete endpoints. Existing 400 validation paths now advertise `ValidationProblemDetails`, existing authentication/authorization/not-found paths advertise `ProblemDetails`, and `GroupController.Delete` advertises its `BaseCommandResponse<Guid>` success payload.
- Progress 2026-06-14: extended the response-metadata pass to `EventSeriesController` and `EventSessionAgendaItemController`. `EventSeriesController` success responses now advertise concrete DTO/command-response payloads, validation failures advertise `ValidationProblemDetails`, not-found failures advertise `ProblemDetails`, and the agenda-item list validation metadata now advertises `ValidationProblemDetails` instead of a bare 400.
- Progress 2026-06-14: tightened two single-endpoint response-metadata gaps. `AnalyticsRelayController.Relay` and `CustomPropertyGovernanceController.GetGovernanceReport` now advertise `ValidationProblemDetails` for their existing 400 validation responses instead of bare status-only metadata.
- Progress 2026-06-14: tightened `LocationController` and `LocationRoomController` response metadata without changing action behavior. Location list/create/update validation paths now advertise `ValidationProblemDetails`, while location/location-room detail/update/delete auth and not-found paths advertise `ProblemDetails`, matching the existing central RFC7807 helper flow.
- Progress 2026-06-14: tightened seven read-only lookup detail endpoints without changing behavior. `GroupPositionController`, `FileTypeController`, `EventStatusController`, `AudienceGenderController`, `AudienceAgeController`, `VisibilityTypeController`, and `ActorTypeController` now advertise `ProblemDetails` for their existing 404 metadata instead of bare status-only responses.
- Progress 2026-06-14: tightened three additional detail-endpoint 404 metadata entries without changing behavior. `DidCustodyTypeController`, `RoleController`, and `ActorKeyStoreController.GetById` now advertise `ProblemDetails` for their existing 404 metadata instead of bare status-only responses.
- Progress 2026-06-14: tightened three more read-only lookup detail endpoints without changing behavior. `RegistrationModeController`, `OrganizationPositionController`, and `MadhabController` now advertise `ProblemDetails` for their existing 404 metadata instead of bare status-only responses.
- Progress 2026-06-14: tightened `UserAuthenticationTokenController`, `UserExternalLoginController`, and `EventTeamController` response metadata without changing action behavior. Existing not-found metadata now advertises `ProblemDetails`, external-login create validation advertises `ValidationProblemDetails`, and event-team missing-auth metadata advertises `ProblemDetails` to match central RFC7807 helper usage.
- Progress 2026-06-14: tightened `TenantOnboardingController` and `ModuleController` response metadata without changing action behavior. Tenant onboarding command validation paths now advertise `ValidationProblemDetails`, tenant admin denial paths advertise `ProblemDetails`, module schema not-found metadata advertises `ProblemDetails`, and module enable/disable validation/auth/forbidden metadata now matches the central RFC7807 helper flow.
- Progress 2026-06-14: tightened `ExternalApiKeyController`, `TenantUserRoleGrantController`, and `ManagedProviderProvisioningController` response metadata without changing action behavior. Existing not-found/forbidden metadata now advertises `ProblemDetails`, and command validation metadata now advertises `ValidationProblemDetails`, aligning these admin/authenticated endpoints with their central RFC7807 helper flow.
- Progress 2026-06-14: tightened `TagController`, `CategoryController`, and `OrganizationMemberController` response metadata without changing action behavior. Taxonomy validation paths now advertise `ValidationProblemDetails`, taxonomy authentication/not-found metadata advertises `ProblemDetails`, and organization-member detail not-found metadata now matches its central RFC7807 helper flow.
- Progress 2026-06-14: tightened `EventTemplateController` and `EventSessionTemplateController` response metadata without changing action behavior. Template list/create/update validation paths now advertise `ValidationProblemDetails`, authentication/not-found metadata advertises `ProblemDetails`, and existing event-template quota responses continue to advertise `ProblemDetails` 422.
- Progress 2026-06-14: tightened `EventSessionLanguageController`, `EventSessionGroupController`, and `EventAgendaItemController` response metadata without changing action behavior. Existing authentication and not-found metadata now advertises `ProblemDetails`, while validation metadata remains `ValidationProblemDetails`, matching the central RFC7807 helper flow.
- Progress 2026-06-14: tightened `EventSessionController` and `EventDayController` response metadata without changing action behavior. Event-session authentication/not-found metadata now advertises `ProblemDetails`; event-day create/update validation metadata now advertises `ValidationProblemDetails`, and event-day authentication/not-found metadata now advertises `ProblemDetails`.
- Progress 2026-06-14: tightened `CustomPropertyDefinitionController`, `EventCustomPropertyController`, and `EventSessionCustomPropertyController` response metadata without changing action behavior. Custom-property list/create/update/purge/value validation metadata now advertises `ValidationProblemDetails`; authentication, authorization, and not-found metadata now advertises `ProblemDetails`; existing quota/conflict responses remain typed as `ProblemDetails`.
- Progress 2026-06-14: tightened `TenantStorageSettingsController`, `ActorSubscriptionController`, and `TenantSettingsDocumentsController` response metadata without changing action behavior. Tenant storage and actor-subscription command validation now advertises `ValidationProblemDetails`; tenant branding document authentication/authorization/not-found metadata now advertises `ProblemDetails`; existing conflict metadata remains typed as `ProblemDetails`.
- Progress 2026-06-14: tightened `GroupMemberController` and `ContactShareConsentController` response metadata without changing action behavior. Group-member authentication/not-found metadata now advertises `ProblemDetails`; contact-share withdraw authentication metadata advertises `ProblemDetails`; shared-contact list/export validation metadata advertises `ValidationProblemDetails`.
- Progress 2026-06-14: tightened `EventFormatController` and `EventTemplateSyncController` response metadata without changing action behavior. Event-format detail not-found metadata now advertises `ProblemDetails`, and template-sync diff/apply/history authentication and authorization metadata now advertises `ProblemDetails` while existing validation/not-found/conflict metadata remains typed.
- Progress 2026-06-14: tightened `SettingsController` and `StorageObjectController` response metadata without changing action behavior. Settings validation metadata now advertises `ValidationProblemDetails`, settings authentication/authorization metadata advertises `ProblemDetails`, and storage list/upload/object validation plus authentication/not-found metadata now advertises the central RFC7807 payload types.
- Progress 2026-06-14: tightened `EventSessionTemplateSyncController` and `UiThemeAdminController` response metadata without changing action behavior. Event-session template sync authentication and authorization metadata now advertises `ProblemDetails`; UI theme admin validation metadata now advertises `ValidationProblemDetails`, and authentication/authorization/not-found metadata now advertises `ProblemDetails`.
- Progress 2026-06-14: tightened `NotificationController` response metadata without changing action behavior. Notification list validation metadata now advertises `ValidationProblemDetails`, and existing authentication/not-found metadata now advertises `ProblemDetails`, including the read, archive, snooze, delete, unread-count, and refresh-stream endpoints.
- Progress 2026-06-14: tightened `ActorController` response metadata without changing action behavior. Actor list and tenant-list validation metadata now advertises `ValidationProblemDetails`; actor detail/DID not-found metadata and create/update/delete authentication, authorization, and not-found metadata now advertises `ProblemDetails`.
- Progress 2026-06-14: tightened `LocalizationAdminController` and `EventRegistrationController` response metadata without changing action behavior. Localization test/governance/export validation paths now advertise `ValidationProblemDetails`, localization admin authentication metadata advertises `ProblemDetails`, and event-registration list/create validation plus authentication/not-found metadata now advertise central RFC7807 payload types.
- Progress 2026-06-14: tightened `CustomPropertyProjectionAdminController` response metadata without changing action behavior. Projection rebuild, single-rebuild, dirty-scope, and projection lookup validation metadata now advertises `ValidationProblemDetails`, while existing quota responses remain typed as `ProblemDetails` 422.
- Progress 2026-06-14: tightened `FooterController` response metadata without changing action behavior. Footer link-group, link, reorder, and tenant footer settings validation metadata now advertises `ValidationProblemDetails`, while existing link-group/link not-found metadata now advertises `ProblemDetails`.
- Progress 2026-06-14: tightened `InstanceOnboardingController` command response metadata without changing action behavior. Onboarding completion, auth-provider save/bootstrap, authorization-provider save/sync, and endpoint verification validation failures now advertise `ValidationProblemDetails` instead of command envelopes.
- Progress 2026-06-14: tightened `TenantController` response metadata without changing action behavior. Tenant create/update and tenant-navigation create/update/reorder validation metadata now advertises `ValidationProblemDetails`; tenant and navigation-link not-found metadata now advertises `ProblemDetails` to match central RFC7807 helper usage.
- Progress 2026-06-14: tightened `UserAppearanceController` response metadata without changing action behavior. Appearance profile creation validation now advertises `ValidationProblemDetails`; existing authenticated/not-found metadata for appearance reads, profile actions, palette generation, archiving, and duplication now advertises `ProblemDetails`.
- Progress 2026-06-14: tightened `EventController` response metadata without changing action behavior. Event list/auth, creation, publish, draft/status update, delete, calendar/program/detail, readiness, and Islamic/tech aspect endpoints now advertise `ValidationProblemDetails` for validation failures and `ProblemDetails` for authentication, authorization, not-found, and publish-conflict failures.
- Progress 2026-06-15: fixed the OpenAPI string-enum root-cause catalog drift. `OpenApiStringEnumSchemaCatalog` now registers the DTO-exposed Domain enum set plus DTO/model-level Application enums discovered by the new guard (`SettingSource`, `CustomPropertyFilterOperator`, `ManagedProviderOrganizerKindDto`, `BatchUpdateMode`, public-experience CTA/home-block enums, `PublicExperienceMode`, `PublicExperiencePrimaryOrganizationState`, and `KeycloakBootstrapMode`). `ApiContractArchitectureTests.DtoEnumProperties_MustBeRegisteredIn_OpenApiStringEnumSchemaCatalog` scans public DTO contract enum properties, excludes validator implementation types, and guards that every DTO enum remains registered so generated OpenAPI schemas keep the API's string-enum contract. Verification: scoped AFT diagnostics were clean; project-level `Event.Architecture.Tests` no longer reports the enum-catalog test as failing, but still exits non-zero because of pre-existing agent-context manifest/dead-link failures under `.claude/`.
- Progress 2026-06-15: completed the deferred `InstanceSettingsController` response-metadata cleanup without changing action behavior. Instance admin/setup-secret forbidden paths now advertise `ProblemDetails`, and the settings, authentication, storage, provider, authorization, and deployment command-validation paths now advertise `ValidationProblemDetails` instead of command envelopes. Focused and broad controller greps for the stale 4xx/command-envelope metadata pattern now return no matches under `Explore.API/Controllers`.
- Progress 2026-06-15: closed the current Phase 2 route-template/documented-contract stance slice. The current inventory has no remaining action-word route-risk row beyond the already-normalized organization/group approval-status routes; generated OpenAPI/client regeneration remains pending for that recorded route change. The preview/v1.0 API stance is now explicit in `docs/API.md`: pre-v1.0 breaking contract changes are allowed for cleaner API/HAL/OpenAPI contracts only when recorded in `docs/API_CHANGELOG.md`, reflected through regenerated OpenAPI/inventory/generated-client workflows as applicable, and reviewed through contract-governance evidence; generated artifacts must not be hand-edited.
- Progress 2026-06-15: reviewed the remaining Phase 2 metadata/classification scope. Response metadata is complete for the stale 4xx/command-envelope target pattern, endpoint classification is emitted as `x-endpoint-class` and guarded by `EndpointClassificationArchitectureTests`, and mutating/admin auth alignment is architecture-guarded. Rate-limit, cache, and broad tenant-mode metadata are not yet emitted as OpenAPI metadata; they remain future work because only endpoint class is currently exported, while rate/cache/tenant-mode values still live in inventory/planning docs.
- Progress 2026-06-15: implemented the Phase 2 OpenAPI posture vendor extensions in `EndpointClassificationTransformer`. Runtime OpenAPI operations now emit `x-rate-limit-policy`, `x-output-cache-policy`, and `x-tenant-mode` alongside `x-endpoint-class` when the endpoint carries `[EnableRateLimiting]`, `[OutputCache]`, or `[RequireMultiTenant]` metadata. `ContractInvariantsTests.OpenApiDocument_EndpointPostureExtensionsExposeRateCacheAndTenantMetadata` guards representative emission for `AiAssistant` rate limiting, `ListData` output caching, and `multi-tenant-required`. Verification: scoped AFT diagnostics were clean, and `dotnet test --project Event.API.IntegrationTests/Event.API.IntegrationTests.csproj --configuration Release --verbosity quiet -- --list-tests` built the integration test project successfully with zero tests run; runtime integration execution remains deferred to the blocked verification item.
- Progress 2026-06-15: recorded Phase 2 verification status instead of re-launching known-blocked suites. Scoped AFT diagnostics for the latest docs/code slices are clean, and the supported project-level `Event.Architecture.Tests` run no longer reports the enum-catalog guard as failing; however, the architecture project still exits non-zero because of pre-existing `.claude` agent-context manifest/dead-link failures. API integration verification remains blocked by known Docker/Testcontainers and host-shutdown/OpenFeature issues, and full build/Blazor verification remains blocked by the pre-existing Razor syntax error in `Explore.Blazor.Client/Components/Shell/AiAssistant/AiActionResultCard.razor`.
- Progress 2026-06-15: closed the missing-identity authorization response slice. Focused controller grep found no raw `Unauthorized(...)`, `Forbid()`, or `"User ID not found"` controller returns, and `ApiContractArchitectureTests.Controllers_MustNotReturn_RawMissingUserUnauthorizedStrings` now guards the original missing-user anti-pattern. The old bridge-timeout blocker is superseded by the later supported project-level architecture run: this guard is not among the remaining failures, which are unrelated `.claude` agent-context issues.
- Progress 2026-06-15: started the reverse `RouteNames` constant-to-endpoint cleanup for R-006. The skipped `RouteNames_EveryConstantResolvesToExactlyOneEndpoint` test was read back, candidate orphan constants were classified by reference usage, and truly unused catalog entries were pruned from `RouteNames.cs`: legacy lookup aliases (`GetApprovalStatuses`, `GetEventFormats`, `GetAudienceAges`, `GetAudienceGenders`), unused approval-status/event-type CRUD names, stale user/organization-member CRUD names, the stale current-user appearance update name, and unused event-team ownership/window names. Policy-used route names without endpoints remain a separate design decision and the reverse coverage test stays skipped until those are reattached or stale affordances are removed.
- Progress 2026-06-15: continued R-006 by realigning stale HAL policy route names for ATProto records and event registrations. `AtprotoRecordLinkPolicy` now targets the registered `GetAtprotoRecordEntries`/`GetAtprotoRecordEntryById`/`CreateAtprotoRecordEntry`/`DeleteAtprotoRecordEntry` routes, and the unsupported `by-uri` affordance was removed because no endpoint exists behind it. `EventRegistrationLinkPolicy` and `EventLinkPolicy` now target the registered `GetEventRegistrations`/`GetEventRegistrationById`/`CreateEventRegistration`/`UpdateEventRegistration`/`DeleteEventRegistration` routes. The obsolete ATProto and registration aliases were removed from `RouteNames.cs`; focused grep found no remaining references, and AFT diagnostics were clean.
- Progress 2026-06-15: continued R-006 by removing unsupported arbitrary-user and tenant-by-slug HAL affordances. `UserLinkPolicy`, tenant/user-role/organization-review/member/group-member/event-registration policies, and `TenantLinkPolicy` no longer emit links backed by missing `GetUserById`, `GetUsers`, `UpdateUser`, or `GetTenantBySlug` endpoints; supported current-user, actor, user organization/registration, tenant-by-id, tenant collection, and tenant create links remain. The obsolete constants were removed from `RouteNames.cs`; focused grep found no remaining references, and AFT diagnostics were clean.
- Progress 2026-06-15: continued R-006 by removing unsupported relationship/review HAL affordances that had no registered endpoints. Organization, actor, event, category, tag, and organization-review policies no longer emit links backed by missing `GetOrganizationEvents`, `GetActorEvents`, `GetEventCategories`, `GetEventTags`, `GetCategoryChildren`, `GetCategoryEvents`, `GetTagEvents`, `GetTagTagTypes`, `GetOrganizationReviewById`, `UpdateOrganizationReview`, or `DeleteOrganizationReview` routes. The obsolete constants were removed from `RouteNames.cs`; focused grep found no remaining references, and AFT diagnostics were clean.
- Progress 2026-06-15: continued R-006 by retargeting/removing the remaining event-session, organization-member, and user-registration affordance drift found in HAL policies. `EventSessionLinkPolicy` no longer emits the unsupported `GetEventSessionSpeakers` link, `OrganizationLinkPolicy` now targets the registered `GetOrganizationMembersByOrganization` route with the expected `organizationId` parameter, and `UserLinkPolicy` now targets the registered `GetRegistrationsByUser` route. The obsolete `GetEventSessionSpeakers`, `GetOrganizationMembers`, and `GetUserRegistrations` constants were removed from `RouteNames.cs`; focused grep found no remaining references, and AFT diagnostics were clean.
- Progress 2026-06-15: closed R-006 reverse route-name coverage after the final read-only RouteNames classification reported 452/452 constants backed by exactly one registered endpoint with no missing or duplicate route names. `RouteNames_EveryConstantResolvesToExactlyOneEndpoint` is now unskipped, so both endpoint-to-constant and constant-to-endpoint route-name guardrails are active. Full API integration execution remains deferred to the blocked verification item.
- Progress 2026-06-15: Phase 2 implementation-side exit gate is confirmed with explicit verification caveats. RFC7807/controller-local ProblemDetails cleanup, route-template cleanup, route-name bidirectional guardrails, response metadata, endpoint classification/auth alignment, OpenAPI string-enum catalog coverage, and OpenAPI rate/cache/tenant posture extensions are implemented and scoped diagnostics are clean. Runtime/API integration and full build verification remain documented as blocked by pre-existing environment/context issues, so Phase 3 may begin only with those blockers carried forward and without claiming the full plan complete.
- Progress 2026-06-14: continued `CustomPropertyProjectionAdminController` cleanup by normalizing event/session projection lookup failures. Event projection lookup and session projection lookup failures now use resource-specific validation descriptors plus `ToCommandValidationProblem`; readback corrected a descriptor mix-up before handoff, and AFT diagnostics were clean. Per user direction, no test suite was launched for this follow-up slice.
- Progress 2026-06-14: normalized `InstanceOnboardingController` completion and setup-secret command failure paths. Missing resolved user identity now returns catalog-backed `authentication_required`, launch preflight blockers use `ToValidationProblem`, and complete/auth-provider/keycloak-bootstrap/authorization-provider/policy-sync/endpoint-verify command failures use operation-specific `ApiValidationProblemDescriptor` values plus `ToCommandValidationProblem`. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: continued `EventController` cleanup beyond `GetMyEvents`. Event create, publish, draft update, status update, Islamic aspect upsert, and tech aspect upsert failures now use operation-specific `ApiValidationProblemDescriptor` values plus `ToCommandValidationProblem`; event-not-found aspect failures now use `ApiNotFoundProblemDescriptor` plus `ToNotFoundProblem`. The existing publish concurrency conflict branch remains a later conflict-mapping slice. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized the large `InstanceSettingsController` helper surface. Repeated setup/admin denials now return catalog-backed `forbidden` through `ToForbiddenProblem`, unresolved current-user branches now use `ToAuthenticationRequiredProblem`, deployment-mode operator-control refusal uses the `instanceDeploymentMode` validation descriptor with `DeploymentModeChangeRequiresOperatorConfiguration`, and the shared `HandleCommandResponse` helper now maps administrator denials and command failures through central RFC7807 helpers. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- Progress 2026-06-14: normalized the remaining `OrganizationMemberController` missing-identity paths. Member add, role update, invitation accept/decline, and delete now use the shared `authentication_required` ProblemDetails helper instead of the removed controller-local unauthorized helper. Per user direction, verification for this slice was limited to readback and AFT diagnostics.
- [x] Normalize authorization failure responses for missing identity vs forbidden resource access. 2026-06-15: added `ApiProblemFactory.CreateAuthenticationRequiredProblem` and `CommandResponseResultMapper.ToAuthenticationRequiredProblem`, migrated missing-user branches to catalog-backed RFC7807 responses with `authentication_required`, and added `ApiContractArchitectureTests.Controllers_MustNotReturn_RawMissingUserUnauthorizedStrings`. Focused controller grep now finds no raw `Unauthorized(...)`, `Forbid()`, or `"User ID not found"` returns; the later supported architecture run did not report this guard as failing, while remaining architecture failures are unrelated `.claude` context issues.
- [x] Migrate hard-coded route names to `RouteNames.Xxx` constants.
- [x] Unskip endpoint-to-`RouteNames` route-name coverage after constants/controllers are aligned (including new AI, storage, and outbox routes); reverse constant-to-endpoint coverage is also active through `RouteNames_EveryConstantResolvesToExactlyOneEndpoint` after R-006 cleanup.
- [x] Normalize obvious action-word routes to resource-oriented route templates where inventory marks them risky. 2026-06-15: current inventory/grep evidence shows the only confirmed risky action-word route was the organization/group approval-status route, already normalized from `updatestatustype/{id:guid}` to `{id:guid}/approval-status`; generated OpenAPI/client regeneration remains pending for the recorded contract change.
- [x] Update OpenAPI response metadata for ProblemDetails, endpoint classifications, auth classifications, rate-limit policies, cache policies, and tenant modes. 2026-06-15: response metadata is complete for the stale 4xx/command-envelope target pattern under `Explore.API/Controllers`; endpoint classification is emitted as `x-endpoint-class` and guarded; mutating/admin auth alignment is architecture-guarded; and `EndpointClassificationTransformer` now emits representative rate-limit/cache/tenant posture extensions (`x-rate-limit-policy`, `x-output-cache-policy`, `x-tenant-mode`) with contract-invariant coverage. Broad per-endpoint inventory completeness remains future governance, but the Phase 2 OpenAPI posture mechanism is implemented.
- [x] Document preview/v1.0 contract stance: breaking changes allowed before v1.0, but documented and regenerated through OpenAPI/client workflows. 2026-06-15: `docs/API.md` now states pre-v1.0 breaking contract changes may be accepted for cleaner API/HAL/OpenAPI contracts only with `docs/API_CHANGELOG.md`, OpenAPI/inventory/generated-client workflow, and governance evidence; at v1.0 breaking schema diffs become blocking per governance.
- [~] Run API integration and architecture tests. 2026-06-15: scoped AFT diagnostics are clean and project-level `Event.Architecture.Tests` was run with supported TUnit/MTP invocation during the enum-catalog slice; the enum guard passed, but the project remains non-zero because of unrelated pre-existing `.claude` agent-context failures. API integration tests remain blocked by Docker/Testcontainers/host-shutdown instability, and full build/Blazor verification remains blocked by the pre-existing `AiActionResultCard.razor` syntax error.

## Phase 3 — Controller Decomposition Behind Stable Contracts

- [x] Confirm Phase 0 and Phase 2 exit criteria before splitting major controllers. 2026-06-15: implementation-side Phase 2 contract gates are complete, while runtime/API integration and full build verification remain explicitly blocked by pre-existing issues; Phase 3 controller decomposition may proceed with route names, ProblemDetails contracts, OpenAPI posture, and verification caveats preserved.
- [ ] Split by resource/use-case cohesion, not method count alone.
- [ ] Evaluate event controller split candidates: `EventsController`, `MyEventsController`, `EventLifecycleController`, `EventPublishingController`, `EventCalendarController`, `EventAspectsController`.
- [ ] Ensure `AiAssistantController`, `StorageObjectController`, and `EmailDispatchAdminController` remain thin dispatch boundaries with logic delegated to Application handlers.
- [ ] Implement only controller splits justified by inventory/risk-register entries.
- [ ] Extract event filter request mapping into API mapper/binder classes.
- [ ] Move calendar filename/canonical URL/file descriptor construction out of the controller.
- [ ] Split settings/admin controllers only where governance, infrastructure diagnostics, auth provider config, setup/onboarding, and footer/settings governance are clearly separate resources/use cases.
- [ ] Extract repeated current-user resolution into shared controller helper or handler authorization path.
- [ ] Ensure all new actions use `RouteNames.Xxx`, explicit response metadata, endpoint classification, auth classification, rate-limit policy, and cache policy metadata.
- [ ] Add behavior-level API tests for decomposed controllers.
- [ ] Update `docs/API.md` and `docs/API_CHANGELOG.md` for route/contract changes.

## Phase 4 — CQRS/Application Use-Case Refactor

- [ ] Decompose `CreateEventCommandHandler` into narrow collaborators, not a single replacement god service.
- [ ] Candidate collaborators: `EventDraftFactory`, `EventScheduleGraphWriter`, `EventTaxonomyAssignmentWriter`, `EventTemplateInstantiationService`, `EventCustomPropertyInitializer`, `EventProjectionRefreshCoordinator`, `EventCreationCacheInvalidator`, `EventCreationMetricsRecorder`.
- [ ] Keep the create-event handler responsible for validation, authorization context, transaction orchestration, idempotency boundary, and response composition only.
- [ ] Decompose/standardize AI assistant prompt and confirmation commands; specifically reduce `SendAiMessageCommandHandler` and `ProcessAiRunCommandHandler` into validation/auth/settings/model/idempotency/quota/provider/action collaborators without bypassing MediatR behaviors.
- [ ] Decompose/standardize storage upload session creation, stream finalization, and quota reservation commands.
- [ ] Define transaction boundary for aggregate creation.
- [ ] Ensure extracted collaborators do not independently call `SaveChangesAsync` unless explicitly part of the unit-of-work design.
- [ ] Define idempotency behavior for create, publish, update, registration, approval/cancellation, setup/bootstrap, sending AI assistant messages, finalizing uploads, and payment-adjacent actions.
- [ ] Add duplicate-submit protection for create-event browser submits.
- [ ] Add optimistic concurrency behavior for event lifecycle/status/settings mutations and AI assistant run state changes.
- [ ] Standardize `concurrency_conflict` and `duplicate_request` ProblemDetails outcomes.
- [ ] Remove obsolete `IAuthorizedRequest` compatibility path if no longer needed.
- [ ] Standardize authorization metadata on sensitive commands with `[AuthorizeResource]` and `ISecureRequest`.
- [ ] Replace direct handler-to-handler calls with `IMediator` dispatch for independent use cases or explicit Application query/read services for internal reads.
- [ ] Normalize query handlers to return DTOs, nullable DTOs, `IReadOnlyList<TDto>`, or `PaginatedResult<TDto>`, not command envelopes. Initial audit targets: `GetEmailDispatchStatusQuery`, `GetEventWithSessionsAggregateViewQuery`, `GetEventListAggregateViewQuery`, `GetEventCustomPropertyProjectionsForEventQuery`, `GetEventCustomPropertyProjectionStatusQuery`, `GetEventSessionCustomPropertyProjectionsForSessionQuery`, and session projection status queries.
- [ ] Centralize cache key/tag construction and mutation invalidation.
- [ ] Extract repeated storage-object URL/presigned URL mapping into a cancellation-aware service.
- [ ] Define audit event taxonomy for tenant/admin/security-sensitive operations.
- [ ] Add audit logging for event publish/unpublish, auth-relevant data changes (Keycloak rotations), presigned URL generation, upload sessions, custom-property governance, templates, modules, namespaces, outbox paused/resumed/parked/replayed states, and tenant settings changes.
- [ ] Ensure audit events include tenant ID, actor ID, resource ID, action, outcome, correlation ID, and reason where relevant.
- [ ] Add unit tests for decomposed event creation, authorization metadata, idempotency, concurrency, cache invalidation, audit logging, and public shell composition.
- [ ] Run Application unit and architecture tests.

## Phase 5A — Persistence Cancellation Tokens

- [ ] Add `CancellationToken` parameters to repository contracts and implementations touched by this workstream (including AI conversations, storage query APIs, outbox transitions, and TickerQ db operations).
- [ ] Pass cancellation tokens into EF async calls (`ToListAsync`, `FirstOrDefaultAsync`, `CountAsync`, `ExecuteDeleteAsync`, `SaveChangesAsync`).
- [ ] Add architecture tests or targeted tests for cancellation-aware repository async methods.

## Phase 5B — Tenant-Safe Repository and Query Contracts

- [ ] Preserve entity-first repositories except for explicitly named read-model/query-store ports.
- [ ] Replace `IEventAggregateViewRepository` input DTO coupling (`AggregateViewFilterDto`) with an Application specification/query object or explicitly named read-model port contract.
- [ ] Quarantine or replace `IGenericRepository.HardDelete`; add authorization/lifecycle guardrails and architecture tests before any caller can hard-delete production data.
- [ ] Convert projection/report-returning repositories to explicitly named read-model/query-store ports.
- [ ] Ensure cross-tenant read models require reason, authorization, and audit logging.
- [ ] Add persistence tests for tenant isolation and soft delete across changed contracts.

## Phase 5C — Query Shape Cleanup

- [ ] Split large query/filter logic in `EventRepository` and `EventSessionRepository` into specification appliers/query objects.
- [ ] Replace broad include graphs with projection-specific read models or explicit detail query methods only where hotspots are confirmed.
- [ ] Verify `AsNoTracking` for read-only queries unless mutation/tracking is required. Include `GenericRepository.GetById`/`Exists` and `TenantSettingRepository.GetByTenantAndKey` in the review.
- [ ] Batch `TenantPolicySettingService.Read` setting lookups through bulk repository APIs instead of dozens of sequential per-key queries.

## Phase 5D — Cursor Pagination Contract

- [ ] Define opaque versioned cursor format.
- [ ] Bind cursors to tenant ID and filter/sort hash where needed to prevent unsafe cursor reuse (particularly for AI conversation history and storage object lists).
- [ ] Define sort direction, previous/next behavior, filter-change behavior, deletion/insertion stability, and total-count policy.
- [ ] Define which endpoints remain offset-based and why.
- [ ] Introduce cursor/keyset pagination for high-volume public event/session/list feeds.
- [ ] Add stable ordering and page-input validation for remaining offset pagination.
- [ ] Update OpenAPI response schema for cursor-paginated endpoints.

## Phase 5E — Index and Migration Review

- [ ] Review indexes for high-volume event/session/feed queries.
- [ ] Ensure tenant-scoped high-volume queries have tenant ID in useful composite indexes.
- [ ] Review public event listing indexes around `TenantId`, `Status`, `Visibility`, `StartDate`/`CreatedAt`, `Id`, and `DeletedAt`/`IsDeleted`.
- [ ] Verify composite index coverage for AI assistant runs (`ConversationId`, `Status`, `CreatedAt`), storage objects (`TenantId`, `Id`, `CreatedAt`), and email outbox receipts.
- [ ] Ensure soft-delete predicates remain index-friendly.
- [ ] Add migration tests/model assertions for critical indexes.

## Phase 5F — Transaction Retry Strategy

- [ ] Wrap manual transactions in EF execution strategies or route them through `IUnitOfWork` (including RabbitMQ outbox updates and TickerQ operational state transitions).
- [ ] Add persistence integration tests for transaction retry behavior.
- [ ] Verify outbox/background dispatch remains idempotent where durable side effects are involved.
- [ ] Run Persistence integration and architecture tests.

## Phase 6 — Final Guardrails, Docs, and Cleanup

- [ ] Convert Phase 0B temporary skipped/failing tests into passing hard gates or explicitly deferred risks.
- [ ] Add/finalize architecture test: protected writes have endpoint auth and handler resource metadata where applicable.
- [ ] Add/finalize architecture test: endpoint classification matches auth metadata.
- [ ] Add/finalize architecture test: every action declares success and ProblemDetails responses.
- [ ] Add/finalize architecture test: no direct handler injection into handlers/controllers unless explicitly allowed.
- [ ] Add/finalize architecture test: Domain files do not use `System.ComponentModel.DataAnnotations`, `System.ComponentModel.DataAnnotations.Schema`, `[ForeignKey]`, or persistence mapping attributes; migrate confirmed mappings into Persistence EF configuration.
- [ ] Add/finalize architecture test: query request types do not return `BaseCommandResponse<T>` unless explicitly allowlisted as a command-side operation.
- [ ] Add/finalize architecture/client tests: Blazor action affordances use HAL `_links` as source of truth.
- [ ] Add/finalize architecture test: repository interfaces expose cancellation-aware async methods.
- [ ] Add/finalize architecture test: tenant filter bypass APIs require approved naming/attributes/tests.
- [ ] Add/finalize architecture test: high-risk POSTs have idempotency posture documented/tested.
- [ ] Add/finalize architecture or integration tests: lifecycle/status/settings mutations have optimistic concurrency posture documented/tested.
- [ ] Add/finalize audit coverage tests for tenant/admin/security-sensitive operations.
- [ ] Update `docs/API.md` for new controller/error/route/rate-limit/cache conventions.
- [ ] Update `docs/SECURITY-MODEL.md` and `docs/AUTHORIZATION.md` for new policy/resource authorization rules and bootstrap/admin behavior.
- [ ] Update `docs/MULTI_TENANCY.md` for fail-closed tenant context semantics and execution modes.
- [ ] Update `docs/OPERATIONS.md` for ProblemDetails/observability/rate-limit/audit behavior if changed.
- [ ] Update `docs/API_CHANGELOG.md` with development-mode breaking API changes.
- [ ] Regenerate OpenAPI/generated clients where API surface changes require it.
- [ ] Run full targeted verification set: build, architecture, API integration, Application unit, Persistence integration.

## Definition of Done

- [ ] All mandatory Phase 0 artifacts exist and are current.
- [ ] All planned backend/API slices implemented or explicitly deferred with rationale in `backend-contract-risk-register.md`.
- [ ] No known tenant-isolation fail-open paths remain in runtime queries.
- [ ] No privileged policy name maps to authentication-only behavior unless intentionally named as authenticated-only.
- [ ] Self-host first-admin/bootstrap behavior is documented, tested, auditable, and disableable.
- [ ] Route-name coverage tests are enabled and passing.
- [ ] ProblemDetails contract and typed error catalog are centralized and tested.
- [ ] Controllers comply with thin-controller conventions and were decomposed only after stable contracts existed.
- [ ] CQRS handlers comply with repo response, authorization, validation, transaction, idempotency, concurrency, and cancellation conventions.
- [ ] Persistence repositories comply with entity-first, tenant-safe, cancellation-aware, query-shape, pagination, index, and transaction conventions.
- [ ] Audit logging covers tenant/admin/security-sensitive operations.
- [ ] Duplicated event/session template diff/sync/instantiation services and custom-property projection filters are consolidated or explicitly deferred with owner/risk rationale.
- [ ] Production TODO/stub hygiene reviewed: `ImageStorageService` delete, `EventSessionSpeakerService`, `AtprotoAuthenticationHandler` 501 challenge, `UserController` admin-check TODO, `TenantMembersSection` mock data, and empty catch in `CreateOrganization.razor.cs`.
- [ ] High-risk POSTs and lifecycle/status/settings updates have idempotency/concurrency posture implemented or explicitly deferred with risk rationale.
- [ ] OpenAPI, generated clients, docs, and API changelog reflect all breaking changes.
- [ ] Canonical targeted verification passes.
