<!-- ABOUTME: Senior CTO implementation plan for full input validation and sanitization hardening. -->
<!-- ABOUTME: Defines the API, BFF, Blazor, testing, and documentation contract for the workstream. -->

# Full Input Validation & Sanitization - Implementation Plan

Last Updated: 2026-07-03 Europe/Brussels
Status: Re-baselined for implementation
Planning mode: Senior CTO feedback rewrite
Primary workstream: `dev/active/full-input-validation-sanitization/`

---

## 0. Planning Metadata

### User Request

Update the implementation plan so it is relevant, correct, enterprise-grade, and focused on both validation and sanitization on the API and Blazor sides. Remove bad tasks that do not follow best practices. Use Tavily MCP for research, Context7 MCP for current framework documentation, follow repository conventions, and apply `$senior-cto-feedback`.

### Matched Repo Intents

There is no single intent for "platform-wide validation and sanitization hardening". This workstream is a cross-cutting security and correctness program. Apply these intents by slice:

| Slice | Intent(s) | Notes |
|---|---|---|
| Application validators, handlers, DTOs | `add-cqrs-handler`, `add-write-endpoint` | Validators are manually instantiated. Repositories return entities. Handlers own DTO mapping and semantic checks. |
| API request contracts and OpenAPI | `add-write-endpoint`, `openapi-contract-change` | Controllers stay thin. API model-state errors use the canonical problem-details shape. Generated artifacts are regenerated by the implementer. |
| BFF boundaries and proxy requests | `bff-auth-bug`, `blazor-component-affordance` | Tokens stay server-side. BFF endpoints validate only their own boundary contracts and antiforgery/compensating controls. |
| Blazor forms and HAL affordances | `blazor-component-affordance`, `add-hal-link` when links change | Blazor validation is UX and accessibility support, never the authority. Actions are gated by HAL links. |
| Sanitization and raw rendering | Security-sensitive local slice | Prefer output encoding. Use sanitization only for approved rich-content seams with a documented allowlist. |

### Required Docs and Rules Loaded

Authoritative project docs and rules reviewed for this rewrite:

- `AGENTS.md`
- `docs/QUICK_REFERENCE.md`
- `docs/GOVERNANCE.md`
- `docs/OPERATIONS.md`
- `docs/ARCHITECTURE.md`
- `docs/API.md`
- `docs/BLAZOR.md`
- `docs/SECURITY-MODEL.md`
- `docs/AUTHORIZATION.md`
- `docs/TESTING.md`
- `docs/UI_GOVERNANCE.md`
- `docs/ACCESSIBILITY.md`
- `docs/DESIGN_SYSTEM.md`
- `.claude/contract/intents.yaml`
- `.claude/rules/api-controllers.md`
- `.claude/rules/application-layer.md`
- `.claude/rules/api-hateoas.md`
- `.claude/rules/blazor-client.md`
- `.claude/rules/blazor-server.md`
- `.claude/rules/tests.md`
- `.claude/commands/dev-docs.md`
- `.agents/skills/senior-cto-feedback/SKILL.md`
- `.agents/skills/clean-architecture-rules/SKILL.md`
- `.agents/skills/cqrs-mediatr-guidelines/SKILL.md`
- `.agents/skills/auth-patterns/SKILL.md`
- `.agents/skills/blazor-bff-patterns/SKILL.md`
- `.agents/skills/blazor-ui-conventions/SKILL.md`
- `.agents/skills/blazor-css-isolation/SKILL.md`
- `.agents/skills/source-command-check/SKILL.md`

`RTK.md` is referenced from the incoming agent context but was not present at repository root during this rebaseline.

### External Research Loaded

Tavily MCP was used for industry guidance and primary security references:

- OWASP Input Validation Cheat Sheet
- OWASP Cross-Site Scripting Prevention Cheat Sheet
- OWASP File Upload Cheat Sheet
- OWASP Logging Cheat Sheet
- IETF HTTPAPI Idempotency-Key draft

Context7 MCP was used for current framework documentation:

- `/dotnet/aspnetcore.docs`: `[ApiController]`, `InvalidModelStateResponseFactory`, `ProblemDetails`, Blazor `EditContext`, `ValidationMessageStore`, antiforgery, file uploads.
- `/fluentvalidation/fluentvalidation`: manual validation, `ValidateAsync`, async rule caveats, ASP.NET automatic validation limitations.
- `/websites/mudblazor`: MudBlazor form-validation capabilities. Repo governance overrides generic MudBlazor examples: use `EditForm` + `EditContext`; do not introduce `MudForm`.

### Complexity

Complexity: XL.

Reasons:

- The work spans API, Application, Infrastructure/Persistence, Blazor Server/BFF, Blazor Client, generated API clients, and test projects.
- Validation is both syntactic and semantic. Sanitization is not interchangeable with validation and must be scoped by content type.
- Some implementation already exists, so the plan must protect completed work instead of restarting from stale assumptions.
- The repo has strict architectural invariants: manual validators, thin controllers, HAL affordance gating, BFF token isolation, no DTO-returning repositories, and no solution-level `dotnet test`.

---

## 1. Executive Summary

This workstream is a controlled hardening program, not a generic cleanup. The target state is an explicit input contract for every untrusted boundary, with authoritative server-side validation, safe user-facing errors, predictable Blazor form UX, and narrowly scoped sanitization where the system intentionally accepts renderable markup.

The corrected architecture is:

1. API and Application are authoritative for command/query validation.
2. BFF endpoints validate BFF-only seams, proxy shape, antiforgery/compensating controls, upload/session headers, and token-boundary behavior.
3. Blazor Client performs responsive UX validation, accessibility support, server-error mapping, and HAL affordance display. It never becomes the security authority.
4. Sanitization is not a blanket middleware. Default defense against XSS is Razor/component output encoding plus context-aware encoding. Sanitizers are only introduced for approved rich-content seams with an explicit allowlist, tests, and ownership.
5. OpenAPI and generated clients are part of the implementation contract. Future implementation agents run the generation commands when contract changes require it; they do not ask the user to regenerate artifacts manually.

Bad patterns removed from this plan:

- Global or response-side sanitization filters.
- Validator auto-registration through DI.
- Business rules in controllers or Blazor components.
- Treating Blazor validation as sufficient security.
- `MudForm` standardization in conflict with local UI governance.
- Direct Application validator reuse from `Explore.Blazor.Client`.
- Asking the user to run required generation or verification steps on behalf of the implementation agent.

---

## 2. Source-Grounded Current State

This section distinguishes verified codebase reality from the desired future state.

| Area | Current Evidence | Status |
|---|---|---|
| API model-state normalization | `Explore.API/Program.cs` configures JSON unknown-member rejection and a custom `InvalidModelStateResponseFactory`. `Explore.API/ExceptionHandling/ApiValidationProblemDetailsFactory.cs` owns the canonical shape. | Implemented baseline. Extend through tests, do not duplicate in controllers. |
| Public query validation | `Explore.API/Models/EventFilterRequest.cs`, `EventSessionFilterRequest.cs`, `PaginatedQueryRequests.cs`, and `QueryValidationRules.cs` implement query validation. `Event.API.IntegrationTests` include public query validation coverage. | Implemented baseline. Audit remaining query DTOs before declaring complete. |
| Idempotency validation | `Explore.API/Middleware/IdempotencyRequestIdentity.cs` and `IdempotencyMiddleware.cs` compute and enforce request identity. Tests exist for fingerprint mismatch behavior. | Implemented baseline. Keep aligned with documented idempotency contract and IETF semantics. |
| Direct upload validation | `Explore.Application/DTOs/StorageObject/Validators/UploadRequestDtoValidator.cs`, `CreateStorageUploadSessionDtoValidator.cs`, and storage upload tests exist. Upload-session reservation metadata now validates MIME syntax, unsafe filenames/display names, unsafe extension tokens, and normalizes content type before storage policy resolution. | Partially implemented. Needs semantic storage-object, object-key, metadata key/value/count, ownership, and logging hardening. |
| Blazor server-error mapping | `Explore.Blazor.Client/Components/Forms/ServerValidationErrorStore.cs` exists and multiple forms use `FormSubmissionGuard`, `AppValidationSummary`, and `EditContext`. | Implemented baseline. Extend convergence after backend field keys are stable. |
| Blazor form governance | Existing forms such as organization details, event create/edit, session create/edit, template editor, and API key dialog use the repo's form primitives. | Partially converged. Do not introduce `MudForm`. |
| BFF boundary hardening | Auth setup, preference, storage proxy, and YARP security tests exist in the Blazor integration suite. | Partially implemented. Audit residual endpoints and document exceptions. |
| Raw markup rendering | `CommunityGuidelines.razor` was previously reviewed as controlled markup. `Explore.Blazor.Client/Components/Shell/AiAssistantRail.razor` now also uses `MarkupString` for prompt-reference highlight markup. | Risk changed. Add a high-priority raw-rendering review and tests. |
| Generated API clients | Repo docs define OpenAPI/client generation. | Must be implementer-owned when contracts change. Remove any task that delegates this to the user. |
| Tenant-aware semantic validation | Tenant isolation is documented and tested in key areas, but this workstream has not fully proven every validation path fails closed before persistence. | Remaining work. |
| Logging/redaction | OWASP logging guidance and repo security docs require safe validation failure logs. | Remaining audit. |

---

## 3. Target Future State

Every external input route has an explicit row in the input matrix and a matching implementation or decision record:

- Trust boundary and caller type.
- Owning layer and owning component/class.
- Allowed shape, size, range, enum values, format, and normalization rules.
- Semantic rules that require persistence, tenant, authorization, or cross-field knowledge.
- Whether the input is renderable content, plain text, structured data, identifier, file metadata, file bytes, URL, email, phone, cursor, idempotency key, header, cookie, or query parameter.
- Validation owner: API model binder/request model, Application validator, handler semantic guard, BFF endpoint validator, Blazor UX validator, or persistence uniqueness constraint.
- Sanitization/encoding owner: default output encoding, URL encoding, HTML encoding, sanitizer allowlist, safe filename generation, log encoding, or "not renderable".
- Error contract: RFC 7807 problem details for API/BFF, field-key stability for Blazor mapping, no sensitive value reflection.
- Tests: unit, integration, persistence, architecture, and Blazor component tests as appropriate.

Implementation does not need to restart completed slices. It must build from the verified baseline above and finish remaining gaps slice by slice.

---

## 4. Non-Negotiable Constraints

These constraints override generic framework examples and external blog guidance.

1. Repositories return entities, never DTOs.
2. Application validators are manually instantiated. Do not register all validators in DI.
3. Use `ValidateAsync` for FluentValidation. Do not rely on ASP.NET automatic validation for FluentValidation rules, especially async rules.
4. Controllers stay thin. They bind requests, call MediatR/services, and return results.
5. Business and tenant-aware validation belongs in Application handlers/services or persistence constraints, not controllers or Blazor components.
6. API and BFF errors must use the canonical problem-details contract. Do not return ad hoc strings or leak raw input values.
7. GET endpoints are `[AllowAnonymous]`; write endpoints are `[Authorize]`, unless an existing documented exception applies.
8. HAL links are the single source of truth for UI action affordances. Blazor must gate Edit/Delete and similar actions by `_links` presence, not local role or claim checks.
9. Tokens stay server-side in the Blazor BFF. `Explore.Blazor.Client` must not receive bearer tokens.
10. Antiforgery is required for unsafe BFF form/API-post paths unless the route is one of the documented exceptions in `docs/SECURITY-MODEL.md` and has compensating controls covered by tests.
11. Prefer output encoding over sanitization. Add a sanitizer only for approved rich HTML or Markdown-to-HTML seams.
12. Never add global input mutation middleware or global HTML sanitization middleware.
13. Never sanitize by stripping arbitrary characters from identifiers or names without an explicit canonicalization rule. Reject invalid input or canonicalize in a named normalizer/handler step.
14. Never write implementation tasks that ask the user to regenerate OpenAPI clients, run required tests, or finish mandatory verification manually.

---

## 5. Architecture Decisions

### A1. Server-Side Validation Is Authoritative

All API request bodies, query objects, route IDs, headers, and uploaded metadata are validated on the server. Client-side validation improves user feedback but has no security authority.

Application validators enforce command/query syntax and cross-field shape. Handlers enforce semantic checks that need repositories, authorization context, tenant context, clock, feature state, or idempotency state.

### A2. API Model Binding Normalizes Transport Errors Only

The API layer owns:

- Unknown JSON member rejection.
- Malformed JSON and model-binding errors.
- Required route/query/body shape errors.
- Canonical validation problem details.

The API layer does not own domain or tenant rules.

### A3. BFF Validation Protects BFF-Only Seams

Blazor Server/BFF endpoints own:

- Proxy route and request shape validation.
- Upload-session header and cookie contracts.
- Setup-secret and operational endpoint input shape.
- Antiforgery or documented compensating control tests.
- Token-boundary checks proving tokens are not exposed to the browser.

BFF code should not duplicate Application command validators for normal API commands. It should pass canonical DTOs through generated clients/services and map API problem details back into Blazor forms.

### A4. Blazor Validation Is UX, Accessibility, and Error Mapping

Blazor Client owns:

- Immediate field-level feedback.
- Preventing duplicate submit UX.
- Accessible validation summaries and focus behavior.
- Mapping API field errors into `EditContext` through `ServerValidationErrorStore`.
- Hiding actions when HAL links are absent.

Blazor Client must not:

- Become the only validation layer.
- Inspect local roles/claims to decide affordances.
- Directly reference Application-layer validators.
- Introduce `MudForm` as the standard pattern.

### A5. Canonicalization Is Explicit and Separate From Validation

Validators may compare normalized values for validation, but they should not silently mutate commands or request DTOs. Persistent canonicalization belongs in named normalizers, handlers, value objects, or persistence constraints so behavior is testable and auditable.

Examples:

- Trim display names before persistence only through an explicit handler/normalizer step.
- Normalize emails consistently before uniqueness checks if the product contract requires it.
- Generate safe storage object names instead of trusting user-supplied filenames.
- Preserve user-visible text when trimming would alter meaning unless the product contract explicitly says otherwise.

### A6. Sanitization Is Content-Type Specific

Most input is not sanitized. It is validated, stored as data, and encoded on output.

Sanitization is only appropriate for approved renderable content such as rich HTML or Markdown-derived HTML. Each approved seam needs:

- A decision record.
- Sanitizer library/package and version.
- Allowlist of elements, attributes, URI schemes, and protocols.
- Tests for script, event handler, dangerous URL, malformed HTML, and style injection payloads.
- Clear ownership for server-side and display-side behavior.

Do not sanitize structured data, identifiers, URLs, filenames, object keys, or JSON by running them through an HTML sanitizer.

### A7. Idempotency Is Part Of Input Validation

Idempotency keys and fingerprints validate retry intent. Behavior must align with the repo contract and IETF draft semantics:

- Same key + same fingerprint may replay the stored result after completion.
- Same key + different fingerprint is a conflict.
- Same key while the original request is still in progress is a conflict or documented pending response.
- Expiry behavior is explicit and tested.

### A8. Generated Artifacts Are Implementation-Owned

When an API contract changes, the implementation agent runs the documented commands to regenerate OpenAPI and API client artifacts, reviews the diff, and verifies generated clients compile. Asking the user to regenerate the client is not an acceptable task.

---

## 6. Implementation Phases

### Phase 0 - Rebaseline And Research

Status: Complete for this planning update.

Deliverables:

- Senior CTO rewrite applied to plan, context, and tasks.
- Tavily MCP research incorporated from OWASP and IETF sources.
- Context7 MCP documentation incorporated for ASP.NET Core, FluentValidation, and MudBlazor.
- Existing implementation evidence separated from remaining work.
- Stale/bad tasks removed or corrected.

### Phase 1 - Remaining API And Application Contract Hardening

Goal: Finish server-authoritative validation for request DTOs, queries, headers, route IDs, and command bodies without moving business logic into controllers.

Scope:

- Audit API request models and Application DTOs against `full-input-validation-sanitization-input-matrix.md`.
- Fill missing FluentValidation validators or handler semantic guards.
- Ensure validators are manually instantiated and called with `ValidateAsync`.
- Ensure validation failures are returned through canonical problem details.
- Ensure unknown JSON members remain rejected unless an explicit compatibility exception exists.
- Review intentional exception `UpdateTenantPolicyRequest` using `JsonUnmappedMemberHandling.Skip`; document why it is safe or change it.

Acceptance:

- Every touched command/query has syntactic validation tests.
- Cross-tenant, not-found, duplicate, invalid-state, and unauthorized semantic checks occur before side effects.
- Controllers remain thin.
- No validator DI auto-registration is introduced.

Primary tests:

- `dotnet test --project Event.Application.UnitTests/Event.Application.UnitTests.csproj --configuration Release --verbosity quiet`
- Relevant `Event.API.IntegrationTests` slices using TUnit filters when needed.

### Phase 2 - Storage, Upload, Metadata, And Object-Key Hardening

Goal: Finish high-risk validation for direct-upload and storage metadata boundaries.

2026-07-03 progress:

- Upload-session reservation metadata validation now rejects malformed or wildcard MIME hints, control characters, path separators, dot segments, reserved Windows device names for file/display names, and unsafe extension tokens.
- Upload-session creation now normalizes `ContentType` before both storage policy resolution and persistence, so routing decisions match the stored session contract.
- Regression coverage exists in `CreateStorageUploadSessionDtoValidatorTests` and `StorageUploadSessionCommandHandlerTests`.
- Remaining Phase 2 work still includes direct `UploadRequestDtoValidator` review, metadata key/value/count limits, object-key validation, tenant/user/session ownership checks, BFF residual upload boundaries, and storage logging/display safety.

Scope:

- Validate user-supplied filename display values, content type hints, byte sizes, object-key references, provider metadata, upload-session IDs, and storage object IDs.
- Treat `Content-Type` and filenames as untrusted.
- Use server-generated storage names/object keys where possible.
- Ensure storage objects are bound to tenant/user/session before issue, proxy, completion, and download.
- Ensure logs do not write raw object keys, filenames, or metadata values without safe encoding/redaction.

Acceptance:

- Invalid metadata fails before presign/session issuance.
- Spoofed content type, extension mismatch, oversize values, invalid object IDs, tenant mismatch, and replayed session cases are covered.
- The UI displays filenames through normal encoding and never uses them as markup.

Primary tests:

- `Event.Application.UnitTests` for validators and handlers.
- `Event.API.IntegrationTests` for upload and storage endpoints.
- `Explore.Blazor.IntegrationTests` for storage proxy boundaries.

### Phase 3 - Tenant-Aware Semantic Validation And Persistence Backstops

Goal: Prove validation cannot be bypassed into cross-tenant or invalid aggregate mutations.

Scope:

- Review write handlers that accept IDs for tenant-bound aggregates.
- Add tests proving tenant mismatch fails closed before mutation.
- Verify uniqueness and race-sensitive rules have database or transactional enforcement where appropriate.
- Ensure repository queries include tenant predicates and return entities.

Acceptance:

- Each high-risk handler has at least one tenant-mismatch test.
- Race-sensitive uniqueness checks are backed by persistence constraints or transaction-safe logic.
- No handler maps untrusted DTOs directly into persisted state without explicit validation/canonicalization.

Primary tests:

- `Event.Application.UnitTests`
- `Event.Persistence.IntegrationTests`
- Focused API integration tests for externally observable behavior.

### Phase 4 - BFF Boundary Residual Audit

Goal: Complete BFF-specific input validation without duplicating Application command rules.

Scope:

- Audit Blazor Server endpoints, YARP transforms, setup endpoint, preference API, auth diagnostics, and upload proxy.
- Verify unsafe BFF routes use antiforgery or a documented exception with compensating controls from `docs/SECURITY-MODEL.md`.
- Validate BFF-specific headers, route parameters, setup secrets, continuation paths, and proxy target paths.
- Confirm tokens never cross into `Explore.Blazor.Client`.
- Confirm validation and auth failures return safe problem details or safe Blazor errors.

Acceptance:

- BFF route matrix is complete.
- Tests exist for missing/invalid antiforgery where applicable and for documented exceptions.
- Tests prove no token leakage into browser-visible responses, markup, or client state.

Primary tests:

- `dotnet test --project Explore.Blazor.IntegrationTests/Explore.Blazor.IntegrationTests.csproj --configuration Release --verbosity quiet`

### Phase 5 - Blazor Form Validation Convergence

Goal: Make Blazor validation predictable, accessible, and aligned with API field keys.

Scope:

- Use `EditForm`, `EditContext`, `FormSubmissionGuard`, `FormSubmitState`, `ServerValidationErrorStore`, and `AppValidationSummary`.
- Keep client rules UX-focused: required fields, simple ranges, local formatting hints, duplicate-submit prevention.
- Map API problem-details field keys to the matching `EditContext` fields.
- Ensure focus moves to the validation summary or first invalid field as the local pattern requires.
- Gate action buttons by HAL `_links`.

Out of scope:

- Introducing `MudForm`.
- Calling Application validators from `Explore.Blazor.Client`.
- Re-creating every server semantic rule in the UI.

Acceptance:

- Forms touched by this workstream show server errors inline and in the summary.
- No role/claim-based local affordance decisions are added.
- Component tests cover invalid local input, API problem-details mapping, duplicate-submit behavior, and link-gated actions.

Primary tests:

- `dotnet test --project Explore.Blazor.Client.Tests/Explore.Blazor.Client.Tests.csproj --configuration Release --verbosity quiet`
- Targeted accessibility assertions where existing test helpers support them.

### Phase 6 - Sanitization And Raw Rendering Review

Goal: Reduce XSS risk by eliminating unnecessary raw rendering and documenting approved rich-content seams.

Scope:

- Inventory all `MarkupString`, `RenderFragment` from user-controlled values, raw HTML display, Markdown-to-HTML, email HTML, template HTML, and JavaScript interop DOM injection.
- Promote `Explore.Blazor.Client/Components/Shell/AiAssistantRail.razor` to high-priority review because it now builds prompt-reference highlight markup with `MarkupString`.
- Re-check `CommunityGuidelines.razor` as controlled static/known markup.
- Classify `PublicExperienceHomeBlockKind.RichText` and `EmailMessage.HtmlBody` as renderable content seams requiring decision records.
- Prefer replacing raw markup with component composition and normal Razor encoding.
- If rich HTML must remain, select a sanitizer in the owning layer and add allowlist tests.

Acceptance:

- Every raw-rendering seam is classified as controlled, encoded, sanitized, or removed.
- User-controlled text is never inserted into raw HTML without encoding or sanitization.
- Tests include script tags, event handlers, dangerous URLs, malformed markup, and encoded display cases.

Primary tests:

- `Explore.Blazor.Client.Tests` for component rendering.
- Application/unit tests for sanitizer services if introduced.
- Architecture/static checks if a reusable raw-markup rule is added.

### Phase 7 - Observability, Logging, And Error Safety

Goal: Make validation failures useful to operators without leaking sensitive input.

Scope:

- Audit logs around validation failures, upload failures, auth/BFF failures, tenant mismatch, idempotency conflicts, and sanitizer rejections.
- Use structured event names and safe identifiers.
- Avoid raw request bodies, tokens, cookies, setup secrets, full object keys, raw filenames, and rich text in logs.
- Ensure user-facing messages are stable and non-sensitive.

Acceptance:

- Tests or focused review prove sensitive values are not echoed in common failure responses.
- Logging updates use existing structured logging patterns.
- Any new metrics avoid unbounded labels and PII.

### Phase 8 - OpenAPI, Generated Clients, And Documentation

Goal: Keep contracts, generated clients, and docs aligned with implementation.

Scope:

- When API contracts change, run:

```bash
dotnet build Explore.API/Explore.API.csproj --configuration Release --verbosity minimal --no-restore -maxcpucount:1
dotnet msbuild Explore.Blazor.Client/Explore.Blazor.Client.csproj /t:GenerateApiClient /p:Configuration=Release /p:Restore=false /m:1 /v:minimal
```

- Review generated diffs.
- Update `docs/API.md`, `docs/BLAZOR.md`, `docs/SECURITY-MODEL.md`, or `docs/TESTING.md` only when behavior or commands change.
- Update this workstream's matrix/tasks/context as slices complete.

Acceptance:

- Generated clients compile.
- No hand-edited generated artifacts.
- No task delegates required generation to the user.

### Phase 9 - Release Verification Gate

Goal: Leave the repository in a defensible state after each implementation slice.

Minimum per-slice verification:

- Run the relevant project tests for touched layers.
- Run `Event.Architecture.Tests` when architecture, context docs, rules, or project structure are touched.
- Run `dotnet build --configuration Release --verbosity quiet` before broad completion claims when the work is not docs-only.
- Do not run solution-level `dotnet test`.

Full final verification follows `docs/OPERATIONS.md` and `.agents/skills/source-command-check/SKILL.md`.

---

## 7. Testing Strategy

| Layer | What To Prove | Test Projects |
|---|---|---|
| Application validators | Required fields, ranges, enum allowlists, cross-field rules, async validation, safe messages. | `Event.Application.UnitTests` |
| Application handlers | Tenant mismatch, invalid aggregate state, duplicate/race-sensitive rules, side-effect ordering. | `Event.Application.UnitTests`, `Event.Persistence.IntegrationTests` |
| API model binding | Unknown JSON members, malformed JSON, invalid queries/routes, problem-details shape. | `Event.API.IntegrationTests` |
| API middleware | Idempotency key/fingerprint conflict, replay, pending/in-progress behavior, expiry. | `Event.API.IntegrationTests` |
| Storage/upload | Spoofed content type, invalid extension, size limits, invalid object keys, session ownership. | `Event.Application.UnitTests`, `Event.API.IntegrationTests`, `Explore.Blazor.IntegrationTests` |
| BFF | Antiforgery or documented exceptions, token isolation, proxy path/header validation. | `Explore.Blazor.IntegrationTests` |
| Blazor forms | Local UX validation, server error mapping, duplicate-submit guard, validation summary/focus. | `Explore.Blazor.Client.Tests` |
| HAL UI | Actions are shown/hidden by `_links`, not roles/claims. | `Explore.Blazor.Client.Tests`, API integration tests when HAL changes. |
| Raw rendering | Encoded display, sanitizer allowlist, dangerous payload rejection/removal. | `Explore.Blazor.Client.Tests`, sanitizer unit tests if introduced. |
| Context/rules | AGENTS/rules/skills/docs stay structurally valid. | `Event.Architecture.Tests` |

---

## 8. Documentation And Operations

Docs update rules:

- Update `full-input-validation-sanitization-input-matrix.md` as each seam is implemented or intentionally deferred.
- Update `full-input-validation-sanitization-tasks.md` immediately when a task is completed, split, removed, or blocked.
- Update `full-input-validation-sanitization-context.md` at handoff with verified evidence and exact next steps.
- Update canonical docs only for durable behavior changes:
  - `docs/API.md` for problem-details, OpenAPI, idempotency, request contract, or client generation changes.
  - `docs/BLAZOR.md` for BFF or client form workflow changes.
  - `docs/SECURITY-MODEL.md` for token, antiforgery, upload, sanitizer, or logging security behavior changes.
  - `docs/TESTING.md` for new required verification commands.

Operational constraints:

- Keep implementation slices small enough for focused tests.
- Do not mix unrelated refactors.
- Do not change generated files without the generator command and a reviewed diff.
- Log durable non-obvious findings in `dev/_journal/journal.md` using the repo's finding workflow.

---

## 9. Security, Authorization, Privacy, And Abuse Controls

Security posture:

- Treat every browser, API client, BFF request, webhook, header, cookie, query string, route segment, uploaded filename, MIME type, storage metadata value, idempotency key, and generated callback/continuation URL as untrusted.
- Validate before side effects.
- Authorize before exposing tenant data or performing mutations.
- Use tenant predicates and ownership checks for IDs supplied by clients.
- Return safe, stable validation errors. Do not echo secrets or raw rich content.
- Encode on output by default.
- Sanitize only approved renderable content.

Privacy posture:

- Avoid logging raw validation payloads.
- Redact setup secrets, tokens, cookies, email verification tokens, upload object keys, user-supplied filenames where sensitive, and rich content bodies.
- Keep validation metrics low-cardinality.

Abuse posture:

- Enforce size and count limits.
- Validate pagination cursors, date ranges, search strings, and filter combinations.
- Preserve idempotency protections for retried writes.
- Avoid expensive sanitizer/parser work before cheap size checks.

---

## 10. Multi-Tenancy, Federation, Localization, Accessibility, And Product

Multi-tenancy:

- Every tenant-bound ID supplied by a client must be resolved through tenant-aware queries or ownership checks.
- Validation failures should not reveal whether a resource exists in another tenant.

Federation:

- Federation inputs are external trust boundaries. Apply the same validation/sanitization rules to federation payloads, signatures, IDs, URLs, and callbacks.
- Do not trust remote display names, summaries, URLs, or HTML.

Localization:

- Validation message keys should stay stable. User-facing text can be localized later, but field keys and machine-readable codes must not drift casually.
- Avoid culture-sensitive parsing without explicit culture rules.

Accessibility:

- Blazor validation must expose errors through accessible summaries/messages.
- Error focus behavior must be predictable.
- Do not rely on color alone to communicate invalid state.

Product:

- Do not degrade authoring UX by stripping meaningful text silently.
- Prefer clear rejection messages over hidden mutation.
- Rich-content support is a product decision with a security cost, not a default.

---

## 11. Risk Register

| Risk | Severity | Mitigation |
|---|---|---|
| Blazor-only validation gives a false sense of security. | High | Keep server validators authoritative. Add API/Application tests for each rule. |
| Global sanitization corrupts valid data or misses context-specific XSS. | High | Do not add global sanitizer. Use output encoding and seam-specific sanitizer decisions. |
| Raw `MarkupString` displays user-controlled values. | High | Inventory, classify, and test all raw-rendering seams, especially `AiAssistantRail.razor`. |
| Storage metadata or object keys allow tenant confusion or unsafe logs. | High | Server-generated keys, ownership checks, metadata validation, redaction tests. |
| API field keys drift from Blazor forms. | Medium | Stabilize problem-details keys before Blazor convergence; test `ServerValidationErrorStore` mapping. |
| Generated clients drift from API contracts. | Medium | Implementer runs OpenAPI/client generation and reviews diffs. |
| Tenant validation exists only in UI. | High | Add Application/API/persistence tests proving fail-closed behavior. |
| Documentation conflicts around form wrappers cause churn. | Medium | For this workstream, follow `docs/UI_GOVERNANCE.md`: `EditForm` + `EditContext`, no `MudForm`. Resolve broader design-system wording separately. |

---

## 12. Definition Of Done

This workstream is done when:

- The input matrix has a current row for every externally controlled input seam in scope.
- Every row has an owner, validation strategy, sanitization/encoding decision, error contract, and tests or an explicit deferred decision.
- API/Application validators and semantic guards cover all write commands and high-risk query/filter inputs.
- BFF endpoints have boundary validation and antiforgery/exception tests.
- Blazor forms use the repo form primitives, map server errors, prevent duplicate submits, and gate actions by HAL links.
- Raw rendering is eliminated or approved with sanitizer/encoding tests.
- Logging and problem-details responses do not leak sensitive values.
- OpenAPI and generated clients are regenerated when contracts change.
- Relevant project tests and architecture/context tests pass.
- `full-input-validation-sanitization-context.md` contains a final handoff with completed slices and residual risks.

---

## 13. Implementation Agent Contract

Before editing code in this workstream, an implementation agent must:

1. Read this plan, the context, the tasks file, the matrix, and contract decisions.
2. Classify the intended slice using `.claude/contract/intents.yaml`.
3. Read the matching `.claude/rules/*.md` and relevant `.agents/skills/*/SKILL.md`.
4. Confirm the current code state with source reads because this workstream is already partially implemented.
5. Update the task checklist before or during implementation, not only at the end.
6. Keep changes within the slice unless the source evidence proves a dependency is required.
7. Run the minimum relevant tests for the touched layer.
8. Update context with exact completed work, commands run, failures, and next steps.

Do not:

- Revert unrelated dirty worktree changes.
- Ask the user to perform required generation or verification steps.
- Replace established local patterns with generic framework samples.
- Change authorization, tenant isolation, BFF token handling, or HAL affordance behavior without matching tests.

---

## 14. Research Evidence Summary

Tavily MCP research:

- OWASP Input Validation Cheat Sheet: validate all untrusted input, use syntactic and semantic validation, prefer allowlists, and do not treat input validation as the primary XSS defense.
- OWASP Cross-Site Scripting Prevention Cheat Sheet: use framework output encoding and context-aware encoding; sanitization is needed for safe HTML, but global/interceptor-style approaches are fragile.
- OWASP File Upload Cheat Sheet: validate extension, content type, signature, filename safety, size, storage location, and authorization; treat client-provided `Content-Type` as spoofable.
- OWASP Logging Cheat Sheet: validate/sanitize/encode event data before logging and avoid logging sensitive data.
- IETF HTTPAPI Idempotency-Key draft: idempotency keys protect retried POST/PATCH requests; fingerprint mismatch and concurrent same-key requests require explicit conflict behavior.

Context7 MCP documentation:

- ASP.NET Core docs support the existing use of `[ApiController]`, `InvalidModelStateResponseFactory`, `ProblemDetails`, Blazor `EditContext`, `ValidationMessageStore`, antiforgery, and secure file-upload validation.
- FluentValidation docs support manual validation and `ValidateAsync`; automatic MVC validation is not suitable for async rules and conflicts with this repo's manual-validator invariant.
- MudBlazor docs show generic `MudForm` support, but this repo's UI governance is more specific and authoritative for this codebase.
